# Applied Migrations

All migrations have been applied to production Supabase unless noted otherwise.

> **Warning — "applied" claims in this file are not authoritative.** Always verify against the live database (`information_schema.tables` / `information_schema.columns`) before relying on a row here. Example: `0108_rent_buddy_spec_tables.sql` was recorded as applied on 2026-07-05 but was never actually run (it contains invalid `ADD CONSTRAINT IF NOT EXISTS` syntax and would have failed), and the whole `0047`–`0113` rent_buddy chain was found unapplied on 2026-07-16 despite being listed here — the live tables still had a pre-0047 legacy shape. See `0134_rent_buddy_schema_rebuild.sql`.

> **Full audit 2026-07-17:** every migration in `artifacts/api-server/src/migrations/` was mechanically diffed against the live schema (information_schema / pg_proc / pg_policy / pg_constraint via the Management API). ~40 files had missing objects — whole table groups (hidden gems, compass foundation/pipeline/feed-intelligence, post_hides, pulse_geo_tags, safe_return, circle age settings) and dozens of RLS policies — all applied that day. Remaining known-unapplied: `0050_rent_a_buddy.sql` (superseded by the rent_buddy rebuild). `0105_compass_performance_indexes.sql` was rewritten against the live schema and applied 2026-07-17. Note: `information_schema.triggers` does not list `TRUNCATE` triggers — verify those via `pg_trigger`.

> **Contributing reminder:** When you apply a new migration to Supabase, immediately append a row to this table before merging. Include the filename, a plain-English description of every table/column/index/policy created or altered, and the date applied. Skipping this step is the primary cause of log drift.

## Migration directory map

The repo has **three** SQL migration directories. Only one is canonical. Knowing which tree owns each table prevents the class of bug where a column exists in one tree's file but never in the live schema.

| Directory | Status | Audited by |
|-----------|--------|------------|
| `artifacts/api-server/src/migrations/` | **Canonical** — all new migrations go here | `auditMigrationsVsLive.ts` (always) |
| `artifacts/api-server/migrations/` | **Frozen legacy** — frozen 2026-07-17, no new files permitted | Frozen-dir guard in `auditMigrationsVsLive.ts` and `checkFrozenDir.ts`; audited against live with `--include-legacy` flag |
| `migrations/` (repo root) | **Archived historical** — archived 2026-08-08, no new files permitted | Frozen-dir guard in `auditMigrationsVsLive.ts` and `checkFrozenDir.ts`; **not** included in live-schema audit |

### Which tree is the source of truth when they disagree?

The **live schema is always authoritative**. When two trees contain files with the same number that create conflicting columns, whichever column actually exists live wins — the other tree's file is wrong. Known divergences:

- `tags.created_at` — exists live; comes from **root** `0043_tags_hashtags.sql`.  
  The **canonical** `0043_tags_hashtags.sql` created `tags.tagged_at`, which was never applied and does not exist live. `tagged_at` is in the audit allowlist.
- `user_location_state.lat/lng/accuracy_meters/source` — live column names; the canonical `0025_location_system.sql` used `latitude/longitude/accuracy/location_source` (in allowlist).
- `passport_stamps.awarded_at` — live name; canonical file used `earned_at` (in allowlist).
- `hashtags.slug` — live name; canonical file used `normalized_name` (in allowlist).

### Why the root `migrations/` tree is not included in the live-schema audit

The root tree partially overlaps with both the canonical and legacy chains and contains a mix of files that were applied in the very early production schema, files superseded by later migrations, and files that would produce massive false-positive drift if mechanically compared against live. Including it would require a large allowlist expansion and could mask genuine canonical-chain regressions. The correct fix for any drift found in the root tree is to add the affected column to the canonical chain's allowlist (with a comment), not to add a new audit pass over the root tree.

The frozen-dir guard (run in CI via `check:frozen-dir` and at startup of `audit:schema`) ensures no one silently adds new files to the root tree.

## Prefix collisions

Migration files are applied in lexicographic order, so two files sharing a
numeric prefix have no defined order relative to each other. `check:migration-prefixes`
fails on any collision.

**There is no `schema_migrations` table.** Verified 2026-08-09: no table matching
`%migration%` exists in `public` or `supabase_migrations`. Nothing records what
has been applied, so the only way to know whether a migration ran is to check
whether the objects it creates exist in the live database. **Never renumber a
migration without doing that check first** — renaming a file that has already
been applied does not change the database, it only makes the record wrong.

### `2059` — both files applied; documented, not renumbered

| File | Verified live 2026-08-09 | Logged |
|---|---|---|
| `2059_content_distribution_stats.sql` | **Applied.** `content_distribution_stats` carries the four columns this file adds (`item_id`, `negative_signal_count`, `first_evaluated_at`, `last_updated_at`); the guarded `DO`-block defaults are all present (`content_type` → `'item'`, `content_id` → `gen_random_uuid()`, counters → `0`, `evaluation_complete` → `false`, `created_at`/`updated_at` → `now()`); `content_distribution_stats_item_id_idx` exists as a UNIQUE index; and `increment_distribution_stats(text, text, boolean, integer, double precision)` exists in `pg_proc`. | Yes — row dated 2026-08-01 |
| `2059_stamp_artwork_generation_source_placeholder.sql` | **Applied.** `stamp_artwork_versions_generation_source_check` exists as a *named* constraint whose definition is exactly `generation_source = ANY (ARRAY['ai_generated','admin_upload','placeholder','recomposed'])`. The pre-migration state was an unnamed inline check permitting only the first two values, so both the rename and the two added values are this file's work. | **No — applied but never logged.** Added by this entry. |

Both are applied, so there is no unapplied file to renumber and the collision is
permanent. It is also harmless: the two files touch entirely unrelated objects
(`content_distribution_stats` and its RPC vs. a check constraint on
`stamp_artwork_versions`), neither reads or depends on the other, so their
relative order is immaterial on a fresh replay.

Prefix `2059` is therefore recorded in `DOCUMENTED_COLLISIONS` in
`src/scripts/checkMigrationPrefixes.ts`. That allowlist matches on the **exact
file set**: a third file taking prefix `2059` fails the check like any other
collision (proven, not assumed — see below). It excuses only the pair that was
investigated.

### `2089` — both files applied; documented, not renumbered

Two agents worked `bughunt-20260805` concurrently on 2026-08-14 and both took
prefix `2089` within hours of each other. Neither file could see the other until
both had merged, so this was not a missed check — it was a genuine race that only
becomes visible after the second merge.

| File | Verified live 2026-08-14 | Applied by |
|---|---|---|
| `2089_revoke_post_media_public_read.sql` | **Applied.** `pg_policies` shows `post_media_storage_public_read` ABSENT on `storage.objects`, and `pnpm run audit:post-media-public-read` exits 3 (AFTER state) with anonymous GET 400 / LIST 0 and the signing control still 200. | Unit C, via the Management API |
| `2089_media_assets_ready_requires_dimensions.sql` | **Applied.** `pg_constraint` shows `media_assets_ready_has_dimensions` on `public.media_assets` with `convalidated = false` — exactly what its `NOT VALID` clause produces, and distinguishable from a validated constraint that some other change might have added. | The other agent |

**Both verified applied BEFORE documenting**, because the rule this repo prints
is *"renumber the one that has NOT been applied"*, and documenting instead of
renumbering is only correct when both have been. With no `schema_migrations`
table, the filename is the only record that a migration ran; renaming an applied
one would make that record wrong.

The collision is harmless on replay: the two files touch entirely unrelated
objects — an RLS policy on `storage.objects` versus a `CHECK` constraint on
`public.media_assets` — and neither reads or depends on the other, so their
relative order is immaterial.

Prefix `2089` is recorded in `DOCUMENTED_COLLISIONS` in
`src/scripts/checkMigrationPrefixes.ts`, which matches on the exact file set: a
third file taking `2089` fails the check like any other collision.

**The general lesson, since this is the second documented collision:** numeric
prefixes allocated by two writers who cannot see each other's uncommitted work
will collide, and no amount of care by either writer prevents it. The check
catches it after the fact, which is the correct place to catch it — but a repo
with concurrent agents should expect this class of entry to keep accumulating.

### The gate

`check:migration-prefixes` was a registered script that **nothing ran** — it was
not in `run-all-checks.sh`, not in `.replit`, and not in any other workflow. It
had been passing by not running while this real collision sat in the tree. It is
now wired into `run-all-checks.sh`, so it runs under `pnpm run check:all` and the
`api-checks` workflow.

Verified 2026-08-09 against the aggregate command, not the script in isolation:

| Scenario | `pnpm run check:all` |
|---|---|
| Documented `2059` pair only | **exit 0** — `PASSED (252 file(s), 1 documented collision(s))` |
| New duplicate prefix (`0184`) added | **exit 1** — `✘ FAILED: check:migration-prefixes`, names both `0184` files |
| Third file added at documented prefix `2059` | **exit 1** — allowlist does not mask it; names all three files |
| Fixtures removed | **exit 0** |

## What `audit:schema` can and cannot establish about privileges

Recorded here because it was got wrong once, in writing, in two migration headers — and because the wrong version prescribed a fix that would have left the real gap open while looking addressed.

**It DOES model table grants.** `auditMigrationsVsLive.ts:684-706` parses `GRANT <privs> ON <table> TO <role>` out of every migration and checks each claim against `information_schema.role_table_grants` (`isMissing` case `"grant"`, `:769`). Any statement that it "compares objects, not privileges" is false.

**What it performs is a PRESENCE check, and the claim model is ONE-DIRECTIONAL BY CONSTRUCTION.** A migration states what it *grants*. It has no way to state what a role must **not** hold. So:

| | |
|---|---|
| can establish | every grant a migration claimed is present |
| **cannot establish** | that no *other* privilege is held — **excess privilege is unrepresentable, not merely unchecked** |
| does not model | `REVOKE` at all — the word appears in that script only inside comments |

That distinction is the whole point. "Unchecked" implies a more thorough `audit:schema` would close it; **"unrepresentable" means no amount of thoroughness will**, because the input language has no sentence for the assertion.

**The case that proved it.** Migration `2092` claimed `service_role.insert` and `service_role.select`. Both were present. `audit:schema` reported nothing and **was correct to**. The live catalog nonetheless read `DELETE, INSERT, REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE`, because Supabase's default privileges on `public` grant ALL at `CREATE TABLE` and `2092` never revoked from `service_role`. The append-only guarantee D7=A rests on was held up by the UPDATE trigger alone, and nothing in the system could say so.

**Which is why `audit:shadow-append-only` is not redundant with a better `audit:schema`.** It asserts an **exact** privilege set — the only form of the question that answers *"can this table be mutated"*. Presence checks answer a different question, and answer it correctly.

The general lesson, for the next table whose correctness is a privilege claim: **a migration comment asserting a constraint is not a constraint.** Either the catalog is asserted against directly, or the claim is decoration.

## Discovery shadow table — 2092 / 2093 / 2094

All applied. This section is kept because the apply exposed two things worth not re-learning: that the CI project needs its own apply, and that a migration comment asserting a privilege constraint is not a constraint. `audit:schema` reports each one as drift, and that report is **correct** — it is the intended state until an operator applies the file. These are not allowlisted: an allowlist entry asserts *"reviewed and deliberately not applied"*, and these are the opposite.

### `2092` / `2093` / `2094` — ALL APPLIED, both projects, 2026-08-15

Applied in order `2092` → `2093` → `2094`; 2093 and 2094 both alter the table 2092 creates. Operator-applied, each returning `[]`.

| Project | Ref | Applied |
|---|---|---|
| Production | `ajrurzioarfkagpuxfnb` | `2092` (earlier), then `2093`, `2094` |
| Sanctioned CI | `hwokxgbmezheskbzskfr` | `2092`, `2093`, `2094` |

**Both projects were needed, and that is worth remembering.** CI's `schema drift · migrations vs live` job runs against the **sanctioned CI project**, never production — the read-only audit guard refuses production inside CI by design. A production-only apply leaves CI red, and the failure looks identical to "not applied anywhere". Run [31863060712](https://github.com/portava/portava.app/actions/runs/31863060712) is the recorded instance: it reported `missing table discovery_shadow_serves` *after* the production apply.

**Verified 2026-08-15, both projects, exit 0 on both commands:**

```
pnpm run audit:schema                 # 280 files, 4063 claimed objects, no missing objects
pnpm run audit:shadow-append-only     # service_role: INSERT, SELECT only; RLS on; 3/3 triggers
```

Production also verified by the operator directly: 0 rows, 27 columns (2094's cohort pair present), and `service_role` privileges reading **exactly** `[INSERT, SELECT]`. The `2092` grant discrepancy is closed — the file's claim and the live catalog now agree, and the append-only property no longer rests on the trigger alone.

### `2092_discovery_shadow_serves.sql` — applied, with a corrected header

P1 Stage 2, operator ruling **D7=A**. Creates the append-only table holding shadow-mode comparisons: what legacy served vs what PDE would have served, per request.

| | |
|---|---|
| status | **Applied, both projects**, 2026-08-15 |
| correction | ⚠ **The grants it documents did not land.** `service_role` held all seven privileges, not INSERT+SELECT — Supabase default privileges on `public` grant ALL at CREATE TABLE, and 2092 revoked from PUBLIC/anon/authenticated but never from `service_role`. anon and authenticated DID end up holding nothing. Repaired by **2093**; the file header carries a dated correction rather than a rewrite |
| missing | table `discovery_shadow_serves`; function `discovery_shadow_serves_append_only`; indexes `_key_observed_at`, `_serve_point_observed_at`, `_user_id`; policies `service_role_insert_…`, `service_role_read_…`; triggers `discovery_shadow_serves_no_update`, `discovery_shadow_serves_no_update_stmt` |
| effect of applying | **Behaviour-preserving.** It creates an empty table nothing currently reaches — `DISCOVERY_ENGINE_MODE` resolves to `legacy`, so no code path writes here |
| enabling shadow | A **separate, later, deliberate** step. Applying this migration does not enable anything |
| retention | 90-day window, no reaper. See `docs/ops/retention-policy.md` |

**Why it is staged rather than applied ahead of its writer.** The table lands in the same change as the only code that writes to it. Creating it earlier would buy nothing and would leave an empty table in production with no observable purpose; the earlier attempt was reverted for exactly this reason (`c7bd5528f`).

**Verify after applying** — expect `t` on both, then eight rows, then exit 0:

```sql
SELECT to_regclass('public.discovery_shadow_serves') IS NOT NULL AS tbl,
       (SELECT relrowsecurity FROM pg_class
         WHERE oid = to_regclass('public.discovery_shadow_serves')) AS rls;
-- expect: t, t

SELECT 'index'   AS kind, indexname AS name FROM pg_indexes
  WHERE schemaname='public' AND tablename='discovery_shadow_serves'
UNION ALL
SELECT 'policy',  policyname FROM pg_policies
  WHERE schemaname='public' AND tablename='discovery_shadow_serves'
UNION ALL
SELECT 'trigger', tgname FROM pg_trigger
  WHERE tgrelid = to_regclass('public.discovery_shadow_serves') AND NOT tgisinternal
ORDER BY 1, 2;
-- expect: 3 index + 2 policy + 3 trigger (the PK index also appears; the third trigger is 2093's TRUNCATE guard)
```

Confirmed 2026-08-15: `pnpm run audit:schema` → **exit 0** on both projects.

**Verify the append-only property directly**, since it is the entire ground of D7=A. The statement-level trigger exists so this check writes nothing — `WHERE false` matches no row, and a row-level trigger alone would let it succeed silently:

```sql
UPDATE discovery_shadow_serves SET category = 'x' WHERE false;
-- expect: ERROR ... is append-only (operator ruling D7=A): UPDATE is not permitted

TRUNCATE discovery_shadow_serves;
-- expect: ERROR ... is append-only (operator ruling D7=A): TRUNCATE is not permitted
```

Both write nothing. `WHERE false` matches no row, and `TRUNCATE` aborts before it empties anything — the statement-level triggers exist precisely so these checks cost nothing. **A property that can only be verified by writing into production is a property nobody verifies.**

### `2093_discovery_shadow_serves_grants.sql` — applied 2026-08-15

Repairs the privilege set 2092 claimed but did not establish.

| | |
|---|---|
| status | **Applied, both projects**, 2026-08-15. Verified: `service_role` now reads exactly `INSERT, SELECT` |
| what it does | `REVOKE ALL ... FROM service_role` then `GRANT INSERT, SELECT`; re-asserts the PUBLIC/anon/authenticated revokes; adds a `BEFORE TRUNCATE` trigger |
| why the TRUNCATE trigger | TRUNCATE was in the leaked privilege set and it empties the table **without firing either UPDATE trigger and without producing a DELETE**. The grants now remove it; the trigger is there because the grants already drifted once, silently, from schema defaults, on this exact table |
| not changed | `postgres` still holds ALL — it owns the table and can re-grant itself, so revoking buys the appearance of a constraint, not the constraint |
| effect | None. 0 rows, mode is `legacy`. It narrows what a role may do to a table nothing writes to |

### `2094_discovery_shadow_serves_cohort.sql` — applied 2026-08-15

Adds `cohort_reason text` and `cohort_bucket smallint` (both nullable).

| | |
|---|---|
| status | **Applied, both projects**, 2026-08-15. Verified: production column count 25 → 27 |
| why | D6=A rows (internal accounts) and D6=B rows (hashed sample) are **different populations** and must not be pooled. Without this column the only way to tell them apart is to remember what `metadata.cohort` said the day each row was written |
| why now | The table holds 0 rows. Adding it later would create a NULL population that can never be attributed — the exact gap the column closes, introduced by closing it late |
| why nullable | NULL has one meaning: written by a code path predating this column. Zero such rows exist, so NULL should never appear; a `NOT NULL` default would hide it behind a plausible value |

### `2095_discovery_place_photos.sql` — **STAGED, NOT APPLIED**

Creates `discovery_place_photos`: the persisted canonical resolved photo for a
place (`place_key` PK, `source`, `photo_url`, `photo_ref`, `resolved_at`,
`expires_at`, `invalid_at`), one index on `expires_at`, RLS on, no client grants.

| | |
|---|---|
| status | **NOT APPLIED. Staged for the operator**, ruled to be applied immediately after the republish verifies: [`discovery/place-photo-persistence-migration-staging.md`](discovery/place-photo-persistence-migration-staging.md). Four phases — before-state, apply via the **Supabase Management API** (psql is firewalled from this workspace), after-state + schema verification, and **one real end-to-end photo-resolution and persistence proof**. **Do not mark this row applied until phases 3 AND 4 both pass against the live database** — this file's own warning is that "applied" claims here are not authoritative, and a created table proves a migration ran, not that a photo was ever stored |
| why | Discovery re-resolves the FSQ → Google → artwork chain **per card, per viewer**. Nothing is stored, so the same external work is paid again for every reader of the same place. Ruled **enabling infrastructure, not a new product feature**: the chain is already approved behaviour, so storing its winner adds none |
| why a new table | `discovery_places` holds place RECORDS, and the overwhelming majority of OSM places served have no row there. Writing place rows to hang a photo off them would build a place corpus as a side effect — the explicitly forbidden outcome. This table holds a photo and nothing else. `discovery_cache` was also rejected: it is a 2-hour cache keyed by search query, and a photo is neither query-scoped nor 2-hour-lived |
| why `photo_ref` exists | Google's media URL embeds the API key. Persisting the rendered URL would store a credential and produce a dead link on the next rotation — and a dead image renders as "no photo", indistinguishable from never having resolved one. Google rows store the photo resource name; the URL is minted per read |
| effect while unapplied | **None, by design.** Every store call fails and degrades to "no stored photo", which is exactly today's behaviour. The code is merged and inert; applying this is what turns it on |
| undo | `DROP TABLE IF EXISTS public.discovery_place_photos;` — nothing depends on it, and the cost is one re-resolve per place |

**Verify all three at once** — after applying, `pnpm run audit:schema` exits 0 and:

```
pnpm run audit:shadow-append-only     # exit 0; grants, RLS and both UPDATE triggers
```

That second command is new. `audit:schema` **does** model table grants — it checks that each grant a migration *claims* is present. What it cannot do is check the privilege set is **exactly** what was claimed: it does not see excess privilege, and it does not model `REVOKE` at all. 2092 claimed `service_role.insert` and `service_role.select`, both were present, five more were too, and the audit was correct to report nothing. `audit:shadow-append-only` asserts the exact set instead.

## Automated audit

The full audit is committed as `artifacts/api-server/src/scripts/auditMigrationsVsLive.ts`. Run it on demand from `artifacts/api-server`:

```
pnpm run audit:schema                     # canonical chain (src/migrations/)
pnpm run audit:schema -- --include-legacy # also audit the legacy migrations/ dir
```

It parses every migration file for the objects it claims (tables and every column declared in their `CREATE TABLE` bodies, `ALTER TABLE … ADD COLUMN` columns, functions, indexes, policies, enums/enum values, table-scoped triggers, views) and diffs them against the live schema via the Supabase Management API (requires `SUPABASE_URL` and `SUPABASE_ACCESS_TOKEN`). Exit 0 = clean, 1 = missing objects (listed per file), 2 = env/API error. Run it after applying any migration, and whenever "column/relation does not exist" errors appear in production.

Encoded gotchas: triggers are checked via `pg_trigger` (TRUNCATE triggers are invisible to `information_schema.triggers`); tables match views too (legacy `buddy_*` compat views); `0050` and `0105` are skipped (superseded/drifted); an allowlist (in the script, with per-entry comments) covers columns where the migration files are wrong vs live — renamed (`feature_flags.key`→`flag`, `highlights.user_id`→`owner_id`, `user_location_state.latitude`→`lat`, `passport_stamps.earned_at`→`awarded_at`, …) or dropped (`tags.tagged_at`, `highlights.trip_id`, …); all verified against live 2026-07-17. The legacy `artifacts/api-server/migrations/` chain diverges heavily from live and is only audited with the opt-in flag.

## Conditional DDL blocks — a class `audit:schema` cannot see

`auditMigrationsVsLive.ts` answers one question: *does every object named by the
migration files exist live?* It parses `CREATE TABLE`/`VIEW`/`FUNCTION`/`INDEX`/
`POLICY`/`TYPE`/`TRIGGER` and `ALTER TABLE … ADD COLUMN`. Two things follow, and
both bit us:

1. **`ENABLE ROW LEVEL SECURITY` and `GRANT` are not objects.** There are 269
   `ENABLE ROW LEVEL SECURITY` and 19 `GRANT` statements in `src/migrations/`,
   and the audit models none of them. A table can be missing RLS forever and the
   audit stays green.
2. **The audit does not model conditional execution.** A statement wrapped in
   `IF <precondition> THEN …` that was skipped because the precondition was false
   is indistinguishable, to this tool, from one that ran.

Where the guard tests *the object being created* (`IF NOT EXISTS (SELECT 1 FROM
pg_policies WHERE policyname='x') THEN CREATE POLICY x`) there is no blind spot —
the object is declared and the audit checks it. Roughly 60 such blocks across 19
files are fine and need no attention.

The blind spot is guards that test a **precondition**, i.e. something other than
the statement's own target:

| Shape | Where | Count |
|---|---|---|
| `IF to_regclass('public.X') IS NOT NULL THEN` | `2070_rls_hardening.sql` (12), `2071_feature_flags_deny_anon.sql` (1) | 13 |
| `EXCEPTION WHEN undefined_table/undefined_column THEN NULL` | `2033_rls_hardening.sql` (9), `2054_backfill_stamp_country_from_city.sql` (1) | 10 |
| `IF EXISTS (SELECT … pg_tables/pg_class)` around a seed or view | `0062_notifications_schema.sql`, `0147_buddy_bookings_compat_view.sql` | 2 |
| dynamic column probe → `format()` + `EXECUTE` | `2033_rls_hardening.sql` §9 | 1 |

**A false guard is not a deferral. It is a statement that never ran and will
never retry** — creating the object later does not re-trigger the block, because
nothing re-runs the migration.

Swept against production and portava-ci, 2026-08-10. Exactly one had actually
misfired: `post_event_links`. See below.

### `post_event_links` — RLS, and why it shipped as one change set

`2070_rls_hardening.sql:89-95` enables RLS on `post_event_links` behind
`IF to_regclass('public.post_event_links') IS NOT NULL`. The table did not exist
when 2070 ran, so the block was a no-op — and creating the table on portava-ci
left it with `relrowsecurity = false`, a table with no RLS in a project whose
anon key ships in the mobile app.

Re-running that one idempotent `DO` block fixed it (`relrowsecurity = true`,
verified). It has **no policies**, which is 2070's deliberate deny-all posture
for a server-only table: the service-role client bypasses RLS, every other role
gets zero rows.

**The production change set for `post_event_links` is therefore four objects, not
three: the table, both indexes, and the RLS enable.** Applying the first three
without the fourth reproduces the same gap on production.

## Replay-fidelity breaks — migrations that CANNOT be replayed as written

Distinct from the allowlist above. An allowlist entry says "the file claims an
object that live does not have, and that is explained." An entry here says
something stronger: **the file's own SQL would fail or produce a different
object if it were run against the current schema.** These are the concrete
reason a fresh project must be restored from a production dump rather than
rebuilt by replaying `src/migrations/*.sql` — see `docs/ci/BOOTSTRAP.md` §1.

### `0026_highlights.sql` — references two columns the live table does not have

Verified against production and portava-ci, 2026-08-10. Live `highlight_replies`
is exactly:

```
id, highlight_id, replier_id, thread_id, created_at
```

The file declares and depends on `user_id` and `deleted_at`, neither of which
exists:

| File says | Live reality |
|---|---|
| `CREATE POLICY "users_view_highlight_replies" … USING (deleted_at IS NULL)` | no `deleted_at` column → statement fails |
| `CREATE POLICY "users_delete_own_reply" … USING (auth.uid() = user_id)` | no `user_id`; live policy of that name reads `auth.uid() = replier_id` |
| `CREATE POLICY "users_insert_highlight_reply" … WITH CHECK (auth.uid() = user_id)` | no `user_id` |

So 0026 **was** applied — the table and two of its three policies exist live —
but what was applied differs from what the file now says, and the file has since
drifted from it. Replaying 0026 today errors on the first policy.

**Do not "fix" 0026.** It is an applied migration; rewriting it would make the
record less accurate, not more, and there is no `schema_migrations` table here to
distinguish "applied" from "applied in an earlier form" (see *Prefix collisions*
for the same reasoning). The live objects are correct as they stand:
`users_view_highlight_replies` is superseded by `hreplies_select` (2033 creates
it and then drops it again in the same file), and the two surviving policies key
on `replier_id`. Recorded here so the next person to consider a replay knows it
will not work and why.

| Migration | Description | Applied |
|-----------|-------------|---------|
| `20260801_e2ee_devices.sql` | Creates `devices` table (id, user_id→profiles, platform, public_key nullable, key_package_count, device_fingerprint, created_at, last_seen_at); indexes on user_id and (user_id, public_key) WHERE non-null; RLS enabled. Tracks per-install cryptographic identity for E2EE (Phase E-0). Applied via Management API. | 2026-08-01 |
| `20260802_e2ee_key_packages.sql` | Creates `key_packages` table (id, device_id→devices ON DELETE CASCADE, key_package_b64, created_at); index on (device_id, created_at ASC); RLS enabled. Stores one-shot MLS KeyPackages for E2EE group formation (Phase E-1). Applied via Management API. | 2026-08-01 |
| `2035_admin_access_log.sql` | Creates `admin_access_log` table (id, admin_id→auth.users, record_type, record_id, reason nullable, action_taken default 'view', timestamp); CHECK constraints on record_type IN ('profile','event','trip','gps_event','check_in') and action_taken IN ('view','export','expand'); indexes on admin_id and timestamp; RLS enabled. Audits every admin read of private entities. Applied via Management API. | 2026-08-01 |
| `2059_content_distribution_stats.sql` | Table pre-existed live with older schema (content_id, creator_id, unique_viewers, opens, dwell_time_ms, saves, shares, comments, positive_actions, negative_actions, last_impression_at, evaluation_complete). Added missing columns via ALTER TABLE: item_id TEXT, negative_signal_count INTEGER DEFAULT 0, first_evaluated_at TIMESTAMPTZ, last_updated_at TIMESTAMPTZ DEFAULT NOW(). Also creates `increment_distribution_stats` RPC (atomically increments counters and recomputes underexposure_status). Applied via Management API. | 2026-08-01 |
| `2059_stamp_artwork_generation_source_placeholder.sql` | Shares prefix 2059 — see "Prefix collisions" above. Drops the unnamed inline check on `stamp_artwork_versions.generation_source` (which allowed only `ai_generated`, `admin_upload`) and re-adds it as the named constraint `stamp_artwork_versions_generation_source_check` allowing `ai_generated`, `admin_upload`, `placeholder`, `recomposed`. Required by the placeholder-provider detection feature, which writes `generation_source = 'placeholder'`. **Backfilled entry:** applied but never logged; confirmed live 2026-08-09 via `pg_get_constraintdef`, so the original apply date is unknown. | logged 2026-08-09 (applied earlier) |
| `2060_ranking_debug_samples.sql` | Table pre-existed live with older schema (content_id, final_score, score_components, ranking_version, surface, sampled_at). Added missing columns via ALTER TABLE: item_id TEXT, session_id TEXT, components JSONB DEFAULT '{}', explanation_key TEXT. Also creates sampled_at DESC and surface indexes. Applied via Management API. | 2026-08-01 |
| `2061_ranking_config_audit_log.sql` | Creates `ranking_config_audit_log` table (id, config_key, changed_by_user_id→auth.users ON DELETE SET NULL, old_value JSONB, new_value JSONB, changed_at); indexes on (config_key, changed_at DESC) and changed_by_user_id; RLS enabled. Audit trail for PATCH /admin/ranking/config/:key. Applied via Management API. | 2026-08-01 |
| `2062_place_votes.sql` | Creates `place_votes` table (user_id→profiles ON DELETE CASCADE, entity_type CHECK IN ('place','gem'), entity_id TEXT, vote CHECK IN ('worth_it','skip_it'), created_at); PRIMARY KEY (user_id, entity_type, entity_id); index on (entity_type, entity_id); RLS with authenticated own-row policy. Worth-It / Skip-It votes for discovery_places and hidden_gems. Applied via Management API. | 2026-08-01 |
| `0102_safe_return_single_session.sql` | Partial unique index `safe_return_sessions_one_open_per_user` on `safe_return_sessions(user_id) WHERE status IN ('pending','active')`. **Audit 2026-07-17:** was never applied and never documented here — the `safe_return_sessions` table itself was missing live; applied legacy `0040_safe_return.sql` (with `feature_flags.flag` column fix) and then this index via Management API. | 2026-07-17 |
| `0107_compass_analytics_onboarding.sql` | Creates `compass_analytics_events` and adds onboarding columns to `compass_settings`. **Audit 2026-07-17:** was never applied and never documented here; applied via Management API. | 2026-07-17 |
| `0107_portava_featured.sql` | Creates `portava_featured_category` enum, `portava_featured` table (post_id, category, status, approved_by, featured_at, permission timestamps), 4 indexes, and `profiles.featured_count` column. Applied via Management API. Seeded 5 top @Portava posts (status=live) across best_photo/best_adventure/best_hidden_gem/best_restaurant categories so the Discovery carousel is populated for all users. **Verify:** `SELECT COUNT(*) FROM portava_featured WHERE status = 'live';` — should return 5. | 2026-07-28 |
| `0115_max_members_atomic_guard.sql` | Creates `enforce_trip_max_members` trigger function (concurrency-safe cap on trip member inserts). **Audit 2026-07-17:** was never applied and never documented here; applied via Management API. | 2026-07-17 |
| `0116_post_hides.sql` | Creates `post_hides` table (user_id+post_id, RLS, indexes) for per-user feed hiding. **Audit 2026-07-17:** was never applied and never documented here; applied via Management API. | 2026-07-17 |
| `0148_memories_location.sql` | Adds nullable location columns to `memories` for Memory location tagging: `location_city` text, `location_country` text, `location_lat` numeric, `location_lng` numeric, `canonical_location_id` uuid (mirrors the `posts` table shape; `memories.place_id` text already existed). Applied via Supabase Management API. | 2026-07-17 |
| `0147_buddy_bookings_compat_view.sql` | Creates `buddy_bookings` compatibility view over `rent_buddy_bookings` (id, buddy_id, traveler_id, package_id, trip_id, booking_date, start_time, duration_h, group_size, city, category, notes, payment_mode, totals, status, lifecycle timestamps). The rent_buddy rebuild created compat views for every legacy `buddy_*` relation except `buddy_bookings`, which `src/routes/pulse.ts` still queries — this closes that gap. Applied via Supabase Management API. | 2026-07-17 |
| `0140_trips_reminder_retry_count.sql` | Adds `reminder_retry_count INTEGER NOT NULL DEFAULT 0` to `trips`. Tracks how many times the crash-recovery sweep has attempted to re-send a reminder that was claimed but never delivered. Applied via Supabase Management API. | 2026-07-17 |
| `0139_trips_reminder_delivered_at.sql` | Adds `reminder_delivered_at TIMESTAMPTZ` (nullable) to `trips`. Completion marker for the 24h trip-tomorrow reminder; set after the push is sent, used with `reminder_sent_at` to detect crash-between-claim-and-send and re-queue. Applied via Supabase Management API. | 2026-07-17 |
| `0102_universal_stamp_catalog.sql` (supabase/migrations) | Creates five tables: `universal_stamp_catalog` (canonical stamp entries, unique on (canonical_location_key, stamp_type), status pending_artwork/approved/rejected/archived), `stamp_artwork_versions` (AI/admin artwork candidates, FK to catalog, deferred FK back via `fk_catalog_active_version`), `stamp_generation_queue` (AI artwork job queue with locking; one active job per catalog entry via partial unique index), `stamp_admin_audit_log` (append-only admin actions), `stamp_reconciliation_log` (links existing stamps to catalog entries). Adds nullable `catalog_id uuid → universal_stamp_catalog(id)` to both `passport_stamps` and `user_stamps` with indexes. RLS on all five tables: service_role full access; authenticated users can SELECT approved catalog entries and approved artwork versions. Also creates `stamp-artwork` storage bucket (public) with public-read and service-role-write policies. Applied via Supabase Management API; reconciliation script (`artifacts/api-server/src/scripts/reconcileStampCatalog.ts`) run twice (idempotent): 3 catalog entries created, 9/10 user_stamps linked (1 row has no country/city — unresolvable), 3 generation jobs queued. | 2026-07-16 |
| `0120_passport_section_order.sql` | Adds `passport_section_order TEXT[]` (nullable) to `profiles` — owner's preferred passport section order (`identity`, `stamps`, `highlights`, `tabs`, `dossier`); NULL = canonical order. Read/written by GET/PATCH `/api/me/profile`; visitor/public views never read it. | 2026-07-15 |
| `0119_toggle_flag_atomic.sql` | Creates `toggle_feature_flag_with_audit(p_flag, p_new_enabled, p_changed_by_id)` SECURITY DEFINER plpgsql function; performs UPDATE feature_flags + INSERT feature_flag_audit_log in one transaction with row-level locking (FOR UPDATE); raises P0002 if the flag doesn't exist; called by PATCH /admin/feature-flags/:flag so the toggle and audit row are always atomic. | 2026-07-07 |
| `0118_feature_flag_audit_log.sql` | Creates `feature_flag_audit_log` table (`id BIGSERIAL PK`, `flag TEXT → feature_flags(flag) ON DELETE CASCADE`, `changed_by_user_id UUID → auth.users(id) ON DELETE SET NULL`, `old_enabled BOOLEAN`, `new_enabled BOOLEAN`, `changed_at TIMESTAMPTZ DEFAULT NOW()`); indexes on `(flag, changed_at DESC)` and `changed_by_user_id`; RLS enabled with no public policies (service-role only). | 2026-07-06 |
| `0114_add_max_members_to_trips.sql` | Adds nullable `max_members INTEGER` column to `trips`. NULL means no member cap. When set, the invite-link accept handler returns 410 gone before claiming a slot if the accepted-member count has reached the limit. | 2026-07-05 |
| `0010_trip_plan.sql` | Creates `trip_plan_items` table with RLS policies (select/insert/update for trip members/owners) and a partial unique index on `(trip_id, source_type, source_id)` | 2026-06-21 |
| `0011_message_type.sql` | Adds `msg_type` (default `'text'`) and `subtype` columns to `messages`; partial index on `subtype` | 2026-06-21 |
| `0012_daily_briefs.sql` | Creates `daily_briefs` table with per-user/per-day uniqueness constraint and RLS (users read own rows only); service role handles all writes | 2026-06-21 |
| `0013_daily_briefs_cleanup.sql` | `daily_briefs_brief_date_idx` index on `daily_briefs(brief_date)` — keeps the nightly purge DELETE fast as the table grows | 2026-06-21 |
| `0014_profile_about_me.sql` | Adds `spoken_languages`, `default_language`, `travel_styles`, `travel_pace`, `budget_style`, `travel_group_style`, `looking_for`, `comfort_level`, `availability_tags`, `planning_style`, `public_social_links` to `profiles` | 2026-06-22 |
| `0015_blocks.sql` | Creates `blocks` table (blocker_id, blocked_id, unique constraint, RLS) and `is_blocked(a, b)` SECURITY DEFINER helper function | 2026-06-22 |
| `0016_thread_reads.sql` | Adds `last_read_at timestamptz` to `message_thread_members`; index for unread-count queries | 2026-06-22 |
| `0017_job_health.sql` | Creates `job_health` table (primary key: `job text`) to persist background-job last-run timestamps; used by `GET /api/healthz/cleanup` to report `cleanupHealthy` across server restarts | 2026-06-22 |
| `0018_preferred_language.sql` | Adds `preferred_language` (nullable text, BCP-47) to `profiles`; overrides `preferred_message_language` in the translation pipeline when set | 2026-06-22 |
| `0019_proposed_time.sql` | Adds `proposed_time TIME` (nullable) to `meetup_time_options` — stores exact HH:MM per time-poll slot alongside the coarse time_block | 2026-06-22 |
| `0020_notifications_inbox_viewed.sql` | Adds `notifications_inbox_viewed_at timestamptz` to `profiles`; used by `GET /api/me/unread-counts` to compute unread notification badge count | 2026-06-21 |
| `0021_plan_edit_permission.sql` | Adds `plan_edit_permission` column to `trips` (enum: `owner_only \| all_members \| specific_members`, default `all_members`) and creates `plan_editors` join table with RLS; used by plan permission system | 2026-06-22 |
| `0022_availability_nudges.sql` | Creates `availability_nudges` table (sender_id, recipient_id, trip_id, nudge_date, sent_on); UNIQUE(recipient_id,trip_id,sent_on) rate-limits to one nudge per recipient per trip per day; RLS: recipients read own rows | 2026-06-22 |
| `0023_push_tokens.sql` | Adds `expo_push_token TEXT` to `profiles`; stores Expo push token per device so the API server can send push notifications | 2026-06-22 |
| `0024_post_engagement.sql` | Creates `posts_likes` (post_id+user_id unique, RLS) and `posts_comments` (body, soft-delete, RLS) tables; adds `like_count`, `comment_count`, `share_count` integer columns (DEFAULT 0) to `posts` | 2026-06-22 |
| `0025_location_system.sql` | Creates `user_location_state` (per-user GPS/manual-city upsert, unique on user_id, RLS) and `passport_stamps_gps` (GPS-earned stamp events, unique on user+type+country+city, RLS) tables; adds `location_source`, `stamp_city/country/label/unlocked_at` columns to `postcards` if it exists | 2026-06-22 |
| `0026_highlights.sql` | Creates `highlights`, `highlight_views`, `highlight_likes`, `highlight_replies`, `highlight_reports` tables with RLS; visibility enum (public/travelers_nearby/circle_only/trip_only/private); 24h default expiry, soft-delete, view/like/reply/report engagement **Correction 2026-07-17:** RLS policies + owner index were missing live; applied via Management API adapted to the live schema (`highlights.owner_id` not `user_id`, `highlight_replies.replier_id`, no `deleted_at` on replies). | 2026-06-22 |
| `0028_highlights_last_viewed.sql` | Adds `highlights_last_viewed_at timestamptz` to `profiles`; updated by `POST /api/me/highlights/mark-viewed`; used by `GET /api/me/unread-counts` to compute `newHighlights` badge count | 2026-06-26 |
| `0029_discovery_places.sql` | Creates `discovery_places` table (id, city, name, place_type, category, neighborhood, blurb, image_url, submitted_by→profiles, saved_count, tag, note, rating, source, status, verified, created_at); RLS (public read, auth insert/update/delete own); indexes on city, place_type, created_at; used by `GET /api/discovery/community` | 2026-06-26 |
| `0032_location_preferences.sql` | Creates `location_preferences` table (user_id PK→profiles, location_mode enum, sharing_paused, pulse/discovery visibility enums, safe_return_enabled, trusted_circle_share, hotel_blur_enabled, updated_at); RLS (users manage own row) | 2026-06-26 |
| `0033_location_sessions.sql` | Creates `location_sessions` table (id, user_id, session_type enum, started_at, ended_at, resolved_city/country, trip_id, plan_item_id, metadata); RLS (users manage own rows, service-role all) | 2026-06-26 |
| `0034_geo_zones.sql` | Creates `geo_zones` table (id, name, zone_type enum, center_lat/lng, radius_meters, polygon_geojson, country_code, city, created_by→profiles, is_system, metadata); RLS (public read, auth create own, service-role all) | 2026-06-26 |
| `0035_plan_geofences.sql` | Creates `plan_geofences` table (id, trip_id→trips, plan_item_id→trip_plan_items, zone_id→geo_zones, trigger_type enum, notify_members bool, message_template, last_triggered_at); RLS (trip members read/manage) | 2026-06-26 |
| `0036_pulse_geo_tags.sql` | Creates `pulse_geo_tags` table (id, post_id→posts, geo_zone_id→geo_zones, tag_type enum, display_label, confidence_score, source enum, created_at); RLS (public read); used by Pulse location context display **Correction 2026-07-17:** entire table `pulse_geo_tags` (and its enum/index/policy) was missing live despite the applied date; applied via Management API during the full-schema audit. | 2026-06-26 |
| `0037_feature_flags.sql` | Creates `feature_flags` table (flag text PK, enabled bool, description, updated_at); seed rows for location intelligence phases 1–6; service-role manages flags | 2026-06-26 |
| `0039_plan_geofence_full.sql` | Expands `plan_geofences` (public_preview_level, exact_visibility, check_in_required, window start/end, arrival_status_visible, no_show_affects_reliability, location_name, city, neighborhood, venue_name, host_revealed, UNIQUE trip_id); adds `plan_checkins` (per-member attendance status with upsert), `plan_attendance_events` (audit log: suspicious/late/override events), `geofence_admin_settings` (single-row id=1 default/min/max radius config) | 2026-06-26 |
| `0041_trip_crew_location.sql` | Creates `trip_crew_location_preferences` (per-user/per-trip ghost mode + visibility defaults, RLS), `trip_crew_location_sessions` (timed live-share sessions with allowed_member_ids, expires_at, RLS), `trip_crew_location_events` (audit log for ghost mode on/off and live share events, RLS); feature flag seeds for `trip_crew_map_enabled`, `trip_crew_live_share_enabled`, `trip_crew_ghost_mode_enabled` | 2026-06-26 |
| `0042_passport_stamps.sql` | Creates `passport_stamps` (dedup unique index on user/stamp_type/country/city, NO lat/lng stored), `passport_memories` (suggested→active→dismissed lifecycle), `passport_contribution_events` (append-only, no Trust Score modification), `passport_visibility_preferences`; feature flag seeds for passport_stamps/memories/map/contribution | 2026-06-26 |
| `0043_tags_hashtags.sql` | Creates `tags` (per-@mention dedup on source_type+source_id+tagged_user_id), `hashtags` (canonical slug registry with block/trending flags), `hashtag_usage` (dedup per hashtag+source), `user_hashtag_follows`; adds `tag_permission` enum (`anyone\|interacted\|friends_only\|nobody`, default `anyone`) to `profiles`; `increment_hashtag_usage_count(uuid)` SECURITY DEFINER helper | 2026-06-26 |
| `0044_hashtag_reports.sql` | Creates `hashtag_reports` table (hashtag_id, reporter_id, reason enum: spam/misleading/abusive, created_at); RLS: auth users insert own rows, service role reads all; indexes on hashtag_id and reporter_id | 2026-06-26 |
| `0046_tag_suppression.sql` | Adds `suppressed BOOLEAN NOT NULL DEFAULT FALSE` + `suppressed_at TIMESTAMPTZ NULL` to `tags`; partial indexes for active-by-source and suppressed-by-user lookups; used by soft-delete self-tag removal flow | 2026-06-26 |
| `0047_rent_buddy.sql` | Creates all `rent_buddy_*` tables: profiles, applications, availability, packages, addons, saved, waitlist, bookings, booking_extensions, route_stops, route_change_requests, safety_checkins, safety_events, user_limits, emergency_contacts_snapshot, reviews, disputes, policy_flags, admin_actions; seeds `rent_buddy_enabled` feature flag. **Correction 2026-07-16:** despite the applied date, the live DB kept a pre-0047 legacy shape (14-column profiles, `date_from`/`date_to` bookings); actually applied as part of `0134_rent_buddy_schema_rebuild.sql`. | 2026-06-26 (not actually applied — see 0134) |
| `0048_booking_stay_connected.sql` | Adds `stay_connected_traveler BOOLEAN NOT NULL DEFAULT FALSE` and `stay_connected_buddy BOOLEAN NOT NULL DEFAULT FALSE` to `rent_buddy_bookings`; replaces ephemeral in-memory Map so stay-connected opt-ins survive server restarts and work across multiple API instances | 2026-06-26 |
| `0049_delayed_geotag_posts.sql` | Adds `location_privacy_mode` enum, `delayed_post_status` enum, `location_sensitivity_level` enum; new columns to `posts` (`post_status`, `location_privacy_mode`, `original_lat/lng`, `public_lat/lng`, `public_location_label`, `venue_name/id`, `geofence_radius_meters`, `publish_after_exit`, `publish_after_time`, `publish_eligible_at`, `exited_geofence_at`, `published_at`, `geotag_credit_awarded`, `location_sensitivity_level`, `user_gps_lat/lng`); creates `delayed_post_location_events` table (audit log: exit_detected/manually_released/geotag_credit_awarded/privacy_changed/publish_skipped/safe_return_hold/canceled) with RLS | 2026-06-26 |
| `0050_suggestion_seen.sql` | Creates `user_suggestion_seen` table (user_id PK→profiles, seen_ids text[], expires_at, updated_at); RLS enabled (service role only — no user-facing policies); `user_suggestion_seen_expires_idx` on expires_at for fast nightly purge; backs the hybrid L1/L2 suggestion-seen cache so the 7-day freshness window survives server restarts | 2026-06-26 |
| `0051_trip_members_user_role_idx.sql` | `trip_members_user_role_idx` composite index on `trip_members(user_id, role)` — lets Postgres seek directly to a user's trip memberships instead of seq-scanning the full table; hot path for the shared-destination caller query in GET /users/search and GET /users/suggestions | 2026-06-26 |
| `0090_rent_buddy_rollout_tables.sql` | Captures rollout tables previously applied inline. Creates `rent_buddy_city_rollouts` (UUID id PK, city UNIQUE, `rent_buddy_city_status` enum `disabled/waitlist_only/buddy_applications_open/internal_testing/beta_testing/public_mvp/paused/suspended`, RLS with `rb_rollout_public_read` + `rb_rollout_svc` policies); `rent_buddy_beta_access` (user+city UNIQUE, `rb_beta_own_read` + `rb_beta_svc`); `rent_buddy_launch_checklists` (9 QA booleans, UNIQUE city_rollout_id); `rent_buddy_launch_audit_logs` (service-role append-only); `rent_buddy_global_controls` (INT PK=1, `all_bookings_paused`, `applications_paused`, `cash_balance_paused`, `nightlife_paused`, `force_full_in_app`, `force_public_meetup`, `force_delayed_posting`, `updated_by_admin_id`, `rb_global_svc` RLS); forces `rent_buddy_enabled = TRUE` (ON CONFLICT DO UPDATE) and inserts 9 MVP feature flags (ON CONFLICT DO NOTHING); all statements idempotent | applied |
| `0062_notifications_schema.sql` | Creates full push notification pipeline schema: `notifications` (in-app Activity Centre rows), `notification_devices` (Expo push tokens per device, UNIQUE user+token), `notification_preferences` (global per-user delivery toggles), `notification_category_preferences` (per-category overrides), `notification_delivery_attempts` (audit log for every dispatch, service-role only), `push_retry_queue` (transient-failure retry with exponential backoff, service-role only), `activity_events` (raw event log); seeds feature flags `notifications_enabled`, `push_notifications_enabled`, `realtime_activity_enabled`, `safety_notifications_enabled` (all enabled), `notification_digests_enabled` (disabled). RLS on all 7 tables — service role writes, users read own rows. All `user_id` columns reference `public.profiles(id)`. All `CREATE POLICY` statements are idempotent (`DO $$` guards). | 2026-06-28 |
| `0063_push_retry_queue.sql` | Supplemental indexes for `push_retry_queue`: `prq_status_next_retry_idx` (partial, `status='queued'` only — tighter than the 0062 index for worker tick queries) and `prq_user_idx` (per-user lookups). No table redefinition — purely additive. Applied as part of combined 0062+0063 production run. | 2026-06-28 |
| `0043_hidden_gems.sql` | Creates Hidden Gems & Local Guide system: `hidden_gems` (name, category, city, exact/approx coords, sensitivity enum, verification enum, status enum, save/visit/report counts, merged_into self-ref; RLS: public read for active+public gems, owner read/insert/update), `hidden_gem_verifications` (gps_proximity/community/guide/admin methods, unique per gem+user+method; owner read/insert), `hidden_gem_reports` (reason enum, unique per gem+reporter; owner read/insert), `hidden_gem_saves` (gem+user PK; owner CRUD), `hidden_gem_visits` (GPS distance, trust_level, is_suspicious; owner read/insert), `local_guide_profiles` (guide_level 0–5, city_expertise, accuracy_score, status; public read for active guides), `local_guide_contributions` (contribution_type enum, helpful_votes; owner read/insert); indexes on city/category/status/layover_safe; seeds 7 feature flags (`hidden_gems_enabled`, `hidden_gem_verification_enabled`, `local_guides_enabled`, `hidden_gems_compass_enabled`, `hidden_gems_passport_enabled`, `hidden_gems_layover_enabled`, `hidden_gems_pulse_enabled`) **Correction 2026-07-17:** all 7 hidden-gem/local-guide tables were missing live; migration also had a bug (`feature_flags(key,...)` — the column is `flag`), fixed in the file and applied via Management API. | 2026-06-29 |
| `0044_tags_hashtags.sql` | Creates tags/hashtag system (supersedes the inline 0043 reference): `tags` (source_type+source_id+tagged_user_id dedup unique index, tagger/tagged indexes; RLS: own rows only), `hashtags` (canonical slug registry, normalized_name, usage_count, is_blocked/is_hidden_from_trending; public read, service-role write), `hashtag_usage` (dedup per hashtag+source_type+source_id, author_id, city/country; public read, service-role write), `user_hashtag_follows` (user+hashtag PK; own CRUD + service-role all); adds `tag_permission` enum (`anyone\|interacted\|friends_only\|nobody`, default `anyone`) to `profiles`; `increment_hashtag_usage_count(uuid)` SECURITY DEFINER helper; `upsert_hashtag_usage_and_increment(...)` atomic upsert helper that prevents double-counting under concurrent writes **Correction 2026-07-17:** `upsert_hashtag_usage_and_increment` was missing live; applied via Management API (the two `tagged_at` indexes were skipped — live `tags` has no `tagged_at` column). | 2026-06-29 |
| `0045_dob_profiles.sql` | Adds `date_of_birth date` (nullable) and `dob_verified boolean NOT NULL DEFAULT false` to `profiles`; columns are excluded from public SELECT queries so other users never see DOB | 2026-06-29 |
| `0046_meetup_age_limits.sql` | Adds `age_limit_enabled boolean NOT NULL DEFAULT false`, `min_age integer`, `max_age integer` to `meetups`; existing rows default to no age limit with unchanged behaviour **Correction 2026-07-17:** the three age columns were missing live; applied via Management API. | 2026-06-29 |
| `0047_circle_age_settings.sql` | Creates `circle_age_settings` (owner_id PK→profiles, age_limit_enabled, min_age, max_age, updated_at); RLS: owner reads and writes own row only **Correction 2026-07-17:** table `circle_age_settings` was missing live; applied via Management API. | 2026-06-29 |
| `0048_age_audit_log.sql` | Creates `age_limit_audit_log` append-only table (actor_user_id, target_type enum `meetup\|circle\|user`, target_id, action, old/new min/max age, reason, metadata jsonb); service role writes only; actors can read own rows via policy **Correction 2026-07-17:** table `age_limit_audit_log` was missing live; applied via Management API. | 2026-06-29 |
| `0049_discovery_places_age.sql` | Adds `min_age integer` and `max_age integer` (both nullable, 0–120 check) to `discovery_places`; enables server-side age filtering on `GET /api/discovery/community` **Correction 2026-07-17:** `discovery_places.min_age/max_age` were missing live; applied via Management API. | 2026-06-29 |
| `0050_rent_a_buddy.sql` | Creates core Rent a Buddy marketplace tables: `buddy_profiles` (display_name, tagline, bio, languages, city, categories, hourly_rate_usd, status enum, verified, average_rating, review_count, response_time_h, gallery_urls; unique user_id; RLS); plus booking, availability, review, and supporting tables; seeds `rent_buddy_enabled` feature flag **Correction 2026-07-17:** never applied and intentionally NOT applied — superseded by the rent_buddy schema rebuild (`0134_rent_buddy_schema_rebuild.sql`): the legacy `buddy_*` tables live as `rent_buddy_*` tables plus `buddy_*` compatibility views. See `0147_buddy_bookings_compat_view.sql` for the one missing view. | 2026-06-29 |
| `0051_compass_foundation.sql` | Creates Compass AI intelligence foundation: `compass_user_profiles` (per-user snapshot: preferred_cities/languages, budget_style, travel_styles, social_style, safety/visibility preferences, block counts, trust_score/level, active_user_score, trip/booking/48h flags, current_city; service-role writes), `compass_user_preferences` (per-user settings: compass_enabled, intent_mode_override, show_explanations, budget_filter, min_trust_level, exclude_budget_styles; user reads/writes own), `compass_user_context_snapshots` (point-in-time context snapshots for pipeline audits), `compass_intent_modes` (user-defined intent sessions); seeds 9 Compass feature flags **Correction 2026-07-17:** all four compass foundation tables were missing live; applied via Management API. | 2026-06-29 |
| `0052_compass_pipeline_logs.sql` | Creates Compass pipeline audit/log tables: `compass_safety_filter_logs` (items blocked by Safety Filter per viewer, item_type, block_reason; service-role write, viewer reads own), `compass_eligibility_logs` (eligibility decisions per viewer+item+stage), `compass_privacy_guard_logs` (privacy suppression events), `compass_recommendation_scores` (per-user/per-item score snapshots with decay_factor, boost_factor, final_score); indexes on viewer+created_at and item_id for each table **Correction 2026-07-17:** all four pipeline-log tables were missing live; applied via Management API. | 2026-06-29 |
| `0053_compass_feed_intelligence.sql` | Creates Compass Phase 3 Feed Intelligence tables: `compass_active_user_scores` (per-user 24h/7d/30d/90d rolling scores + tier; PK on user_id), `compass_active_user_events` (append-only activity events used in scoring), `compass_active_user_badges` (badge eligibility per user), `compass_city_reputation` (city-scoped reputation per user), `compass_category_reputation` (category-scoped reputation per user), `compass_visibility_boosts` (fair-exposure appearance tracking), `compass_visibility_cooldowns` (cooldown records when appearance cap is hit); all tables with RLS, service-role writes **Correction 2026-07-17:** the feed-intelligence tables were missing live; applied via Management API. | 2026-06-29 |
| `0054_compass_cache.sql` | Creates Compass Phase 4 Cache & Front Load tables: `compass_feed_cache` (cached feed payloads per user with per-type TTLs and expires_at), `compass_preload_queue` (items queued for background preloading), `compass_preload_events` (client navigation events), `compass_cache_invalidations` (audit log for every cache invalidation), `compass_user_navigation_patterns` (aggregated nav patterns for manifest ranking), `compass_content_freshness` (per-item staleness tracking), `compass_media_preload_manifest` (media URLs for client prefetch), `compass_frontload_rules` (configurable tier-assignment rules); indexes on expires_at, user_id, entry_type **Correction 2026-07-17:** missing objects found in full audit; applied via Management API. | 2026-06-29 |
| `0055_compass_ux.sql` | Creates Compass Phase 5 Intelligence UX tables: `compass_feedback_events` (append-only user feedback actions on recommendations), `compass_explanation_reasons` (admin-editable override explanations), `compass_notification_decisions` (audit log for notification send/suppress decisions), `compass_abuse_flags` (patterns detected by CompassAbuseDefenseEngine), `compass_served_recommendations` (per-user recommendation records used by the /why endpoint); all tables with RLS, service-role writes, users read own rows **Correction 2026-07-17:** missing objects found in full audit; applied via Management API. | 2026-06-29 |
| `0056_compass_user_prefs_v2.sql` | Extends `compass_user_preferences` (created in 0051) with Phase 5 feedback-driven personalisation columns: `category_weights jsonb NOT NULL DEFAULT '{}'`, `ignored_item_ids text[] NOT NULL DEFAULT '{}'`, `muted_hashtags text[] NOT NULL DEFAULT '{}'`, `muted_topics text[] NOT NULL DEFAULT '{}'`, `public_meetups_only boolean NOT NULL DEFAULT false`; required by `processFeedback` upserts and notification quiet-hours parsing **Correction 2026-07-17:** missing objects found in full audit; applied via Management API. | 2026-06-29 |
| `0057_reply_to_messages.sql` | Adds `reply_to_id uuid REFERENCES messages(id) ON DELETE SET NULL` to `messages` (partial index on non-null rows); creates `saved_messages` table (user_id+message_id unique PK, saved_at; RLS: users read/insert/delete own rows; index on user_id+saved_at DESC) | 2026-06-29 |
| `0058_trip_flow.sql` | Creates route planning system: `route_style` enum (`nightlife\|scenic\|foodie\|low_walking\|custom`), `route_plan_status` enum (`draft\|active\|completed\|cancelled`); `route_plans` (owner_user_id, trip_id, circle_id, title, start/end_location jsonb, route_style, status, compass_explanation, is_approximated; RLS: owner read/write, trip members read); `route_stops` (route_plan_id, sequence_order, place details, checkpoint_status, ETA/ATA; trip members read, owner write); `route_legs` (stop-to-stop transit metadata: mode, distance_m, duration_s, geometry_geojson; RLS follows route stops) | 2026-06-29 |
| `0059_route_plan_members.sql` | Creates `route_plan_members` (route_plan_id, user_id, joined_at; UNIQUE per plan+user); RLS: own membership read/insert/delete; trip members can see all memberships on a trip-linked plan; index on route_plan_id for fast member lookups | 2026-06-29 |
| `0060_discovery_places_coords.sql` | Adds `lat DOUBLE PRECISION` and `lng DOUBLE PRECISION` (both nullable) to `discovery_places`; partial index `discovery_places_has_coords_idx` on `(city, place_type) WHERE lat IS NOT NULL AND lng IS NOT NULL` for fast map-view queries; enables community-submitted places to appear on DiscoveryMapView alongside OSM results | 2026-06-29 |
| `0061_discovery_place_reports.sql` | Creates `discovery_place_reports` (place_id→discovery_places, reporter_id→profiles, reason, notes; UNIQUE per place+reporter); RLS: auth users insert own reports and read own reports; indexes on place_id (for most-reported aggregation) and reporter_id | 2026-06-29 |
| `0063_interaction_foundation.sql` | Creates 8 Phase 4 User Interaction System tables: `user_interaction_cooldowns` (anti-retaliation gate, UNIQUE user+target+type, RLS users read own), `user_mutes` (global user mutes with mute_types[], UNIQUE muter+muted, RLS users manage own), `user_restrictions` (restrictor hides read-receipts/online status from restricted user, UNIQUE restrictor+restricted, RLS users manage own), `reports` (unified cross-domain report table with target_type/reason_code/severity/status, indexes on reporter/target/status, RLS reporters read own), `user_saves` (private profile bookmark, UNIQUE saver+saved, RLS users manage own), `moderation_actions` (admin/moderator action log, RLS enabled), `user_account_states` (suspended/banned/limited state, UNIQUE user+state, RLS enabled), `user_privacy_settings` (per-user privacy config: profile_visibility, who_can_tag, age_restriction, location_sharing, online_status; UNIQUE user, RLS users manage own). All statements use IF NOT EXISTS; policies use DROP IF EXISTS + CREATE. Required by POST /api/users/:id/mute, POST /api/users/:id/save, POST /api/reports. | 2026-06-30 |
| `0064_tags_approval.sql` | Adds `status text NOT NULL DEFAULT 'approved'` to `tags` table; creates partial index `tags_status_idx` on `tags(status) WHERE status <> 'approved'` for efficient pending/rejected tag lookups. Supports the `approval_required` tag_permission setting added in Phase 4. | 2026-06-30 |
| `0065_events.sql` | Creates full Events system: 4 enum types (`event_state`, `event_rsvp_status`, `event_role_type`, `event_visibility`); 8 tables: `events` (host_id, title, location, dates, cover_url, capacity/age/trust gates, visibility, state, chat/waitlist flags, price, rsvp_options, going_count, waitlist_count, city/country; 6 indexes; RLS), `event_rsvps` (event+user PK, status enum; 2 indexes; RLS), `event_waitlist` (event+user PK, position, offer_expires_at, UNIQUE event+position; 2 indexes; RLS), `event_roles` (event+user PK, role enum; 2 indexes; RLS), `event_attendee_states` (checked_in_at, confirmed_at, no_show_at, confirmed_by, no_show_by; 2 indexes; RLS), `event_join_requests` (status pending/approved/denied, message, reviewed_by/at, UNIQUE event+user; 2 indexes; RLS), `event_updates` (author_id, body, pinned; 1 index; RLS), `event_reviews` (reviewer_id, rating 1–5, body, anonymous, UNIQUE event+reviewer; 2 indexes; RLS). Adds `avg_rating NUMERIC(3,1)` and `review_count INTEGER DEFAULT 0` to `events`. Seeds 4 feature flags: `events_enabled`, `events_waitlist_enabled`, `events_chat_enabled`, `events_trust_gates_enabled` (all enabled). | 2026-06-30 |
| `0065_phase7_safety.sql` | Creates `report_evidence` (id, report_id→reports ON DELETE RESTRICT, evidence_type, content_ref, metadata jsonb, created_at; `idx_report_evidence_report` index; RLS enabled; policy "Reporters can read their own evidence" via reports.reporter_id = auth.uid()); adds `metadata jsonb` column to `feature_flags` (for parameterized emergency flags storing target IDs); seeds 11 emergency kill-switch flags (all `enabled: false`, ON CONFLICT DO NOTHING): `disable_unknown_message_requests`, `disable_new_event_creation`, `disable_rab_bookings`, `disable_tagging`, `disable_location_sharing`, `disable_profile_search`, `disable_media_uploads`, `freeze_city`, `freeze_event`, `freeze_circle`, `freeze_booking`. | 2026-06-30 |
| `0066_post_interaction_layer.sql` | Creates `post_reactions` (post_id+user_id UNIQUE, emoji, CASCADE from posts+auth.users; index on post_id), `comment_likes` (comment_id+user_id UNIQUE, CASCADE from posts_comments+auth.users; index on comment_id), `post_shares` (post_id+user_id+target UNIQUE, CASCADE from posts+auth.users; index on post_id), `post_edits` (edit history: post_id, user_id, old_content, new_content, edited_at; index on post_id). Alters `posts`: adds `comments_setting TEXT NOT NULL DEFAULT 'everyone'` + CHECK constraint (`everyone\|friends\|circle\|trip_crew\|verified\|disabled`), `likes_hidden BOOLEAN NOT NULL DEFAULT FALSE`, `sharing_disabled BOOLEAN NOT NULL DEFAULT FALSE`, `reposting_disabled BOOLEAN NOT NULL DEFAULT FALSE`. Alters `posts_comments`: adds `parent_comment_id UUID` self-referential FK (CASCADE) + partial index `idx_posts_comments_parent` for non-null rows (threaded replies). All statements use IF NOT EXISTS / DROP CONSTRAINT IF EXISTS (idempotent). | 2026-06-30 |
| `0067_memories.sql` | Creates the full Memory System: `memories` (id, owner_id→auth.users, title, caption, visibility enum 6 values, allowed/hidden_user_ids uuid[], trip_id→trips, event_id→events, place_id, starts_at/ends_at, state enum 5 values; RLS: owner all, public read for published+public; indexes on owner, trip, event, state+visibility); `memory_items` (media_url, media_type, caption, position; RLS via memory ownership; index on memory_id+position); `memory_tags` (memory_id+tagged_user_id PK, status enum pending/approved/removed; RLS: owner/tagged read, tagged update own, owner insert/delete); `memory_likes` (memory_id+user_id PK; RLS: own all, public read); `memory_saves` (memory_id+user_id PK; RLS: own all). All tables use IF NOT EXISTS; all policies on separate DROP/CREATE statements. | 2026-06-30 |
| `0068_stories.sql` | Creates Stories + Close Friends system. Enums: `story_state` (active/expired/saved/deleted/removed), `story_visibility` (public/friends_only/close_friends/trip_crew/circle_only/custom). Tables: `stories` (id, owner_id→auth.users, media_url/type, caption, visibility, allowed/hidden_user_ids uuid[], close_friends_only bool, trip_id→trips, event_id, place_id, expires_at=+24h, saved_to_highlight_id, state, hide_viewer_list; RLS: owner all, public read for active+non-expired; indexes on state+expires_at and owner+state+created_at); `story_views` (story_id+viewer_id PK, viewed_at; RLS: owner reads own story's viewers, viewer inserts own; index on story_id+viewed_at); `story_reactions` (story_id+user_id UNIQUE, emoji; RLS: owner/user read, user write); `story_replies` (story_id, user_id, message; RLS: owner+sender read, sender insert); `close_friends` (owner_id+friend_user_id PK; strictly private — owner-only RLS; index on owner_id). Seeds `stories_enabled` feature flag. Applied via Supabase Management API (no SQL file existed on disk; file saved to artifacts/api-server/src/migrations/0068_stories.sql). | 2026-06-30 |
| `0069_collections.sql` | Creates unified Collections & Saves system. Enum: `collection_entity_type` (post/event/trip/memory/highlight/place/profile/hashtag). Tables: `collections` (id, owner_id→auth.users CASCADE, name 1–120 chars, cover_url nullable, position int, is_default bool, UNIQUE partial index on owner_id WHERE is_default=TRUE; owner-only RLS), `collection_items` (id, collection_id→collections CASCADE, entity_type enum, entity_id uuid, saved_at; UNIQUE on collection_id+entity_type+entity_id; owner-only RLS via collections join). Backfills existing `user_saves` (profile saves) into the new tables — `discovery_place_saves` was not present in production and was skipped. Supports 9 API routes: GET/POST/PATCH/DELETE collections, GET collection items, POST/DELETE unified saves, GET save-status, GET saved-hashtags. | 2026-06-30 |
| `0067_reviews.sql` | Creates the cross-domain review system. Enums: `review_entity_type` (trip/rent_buddy_booking), `review_visibility` (public/anonymous), `review_state` (published/hidden/removed). Table: `reviews` (id, reviewer_id→auth.users CASCADE, entity_type, entity_id uuid, rating 1–5 CHECK, body text, tags text[], visibility, state, reported_by uuid[], created_at/updated_at; UNIQUE entity_type+entity_id+reviewer_id for dedup; indexes on entity (type+id), reviewer, created_at DESC). RLS: authors read/insert/update own rows; public read published rows; service role bypasses via service key. Feeds POST /api/reviews, GET /api/trips/:id/reviews, GET /api/users/:id/reviews, DELETE /api/reviews/:id, POST /api/reviews/:id/report routes. **Note:** File was originally committed as `0069_reviews.sql` (conflicting with `0069_collections.sql`); renamed to `0067_reviews.sql` by the 2026-07-03 DB audit to resolve the duplicate migration number. | 2026-06-30 |
| `0070_appeals.sql` | Creates the moderation appeals system. Enums: `appeal_state` (submitted/under_review/approved/denied), `appeal_target_type` (post/memory/highlight/account_warning/trust_score_event/no_show/event/event_membership/trip/trip_membership/review). Table: `appeals` (id, appellant_id→auth.users CASCADE, target_type, target_id uuid, reason text NOT NULL, evidence_url text, state DEFAULT submitted, moderator_id→auth.users nullable, resolution_note text, created_at/updated_at; UNIQUE appellant_id+target_type+target_id for one-active-appeal-per-target enforcement; indexes on appellant, state+created_at, moderator). RLS: appellants read/insert own rows; admin access enforced at application layer via service role client (bypasses RLS). Feeds POST /api/appeals, GET /api/appeals/me, GET /api/appeals (admin), PATCH /api/appeals/:id (admin with resolveAppeal reversal + notification). | 2026-06-30 |
| `0071_protect_default_collection.sql` | Adds a BEFORE DELETE trigger (`enforce_default_collection_no_delete`) on `collections` that raises an integrity constraint violation (SQLSTATE 23000) when any row with `is_default = TRUE` is deleted. Prevents direct-DB deletions (Supabase dashboard, psql, rogue migrations) from silently orphaning all of a user's saved items via the `ON DELETE CASCADE` on `collection_items`. The API DELETE /users/me/collections/:id handler already returns 403 at the application layer; this trigger is the belt-and-suspenders DB-level guard. Trigger fires BEFORE DELETE so the cascade never propagates. Function: `prevent_default_collection_delete()` (SECURITY INVOKER, LANGUAGE plpgsql). | 2026-07-01 |
| `0072_block_collections_truncate.sql` | Adds a BEFORE TRUNCATE statement-level trigger (`block_collections_truncate`) on `collections` that unconditionally raises SQLSTATE 23000. Row-level BEFORE DELETE triggers (0071) are never invoked by `TRUNCATE TABLE` in PostgreSQL, so a plain `TRUNCATE collections` from the Supabase dashboard or psql would bypass the `is_default` guard and cascade-delete every user's saved items via `collection_items ON DELETE CASCADE`. The statement-level trigger is the only PostgreSQL mechanism that intercepts TRUNCATE. Legitimate bulk-deletes should use `DELETE … WHERE` instead, which does fire the per-row guard. Function: `prevent_collections_truncate()` (LANGUAGE plpgsql). | 2026-07-01 |
| `0073_block_collection_items_truncate.sql` | Adds a BEFORE TRUNCATE statement-level trigger (`block_collection_items_truncate`) on `collection_items` that unconditionally raises SQLSTATE 23000. Migrations 0071 and 0072 protect the `collections` table (row-level DELETE guard + statement-level TRUNCATE guard), but `collection_items` had no equivalent protection. An admin running `TRUNCATE collection_items` or `TRUNCATE collection_items CASCADE` from the Supabase dashboard or psql would silently wipe every user's saved items without touching `collections` at all, bypassing both earlier triggers. The statement-level trigger is the only PostgreSQL mechanism that intercepts TRUNCATE. Legitimate bulk-deletes should use `DELETE … WHERE` instead. Function: `prevent_collection_items_truncate()` (LANGUAGE plpgsql). | 2026-07-01 |
| `0074_protect_saved_places.sql` | Creates `saved_places` table (user wishlist for Discovery places: user_id→auth.users CASCADE, place_id→discovery_places CASCADE, saved_at, UNIQUE user+place; owner-only RLS; index on user_id+saved_at DESC) and adds a BEFORE TRUNCATE statement-level trigger (`block_saved_places_truncate`) that unconditionally raises SQLSTATE 23000. A plain `TRUNCATE saved_places` from the Supabase dashboard or psql would silently wipe every user's wishlist in one statement. The trigger is the only PostgreSQL mechanism that intercepts TRUNCATE. Legitimate bulk-deletes must use `DELETE … WHERE` instead. Also extends `scripts/src/verify-db-triggers.mjs` REQUIRED array and `scripts/check-db-triggers.sh` SQL query to include `block_saved_places_truncate` on `saved_places`, so the pre-release db-triggers check catches a dropped trigger before it can ship. | 2026-07-01 |
| `0075_seed_discovery_places.sql` | Seed data: 46 curated `discovery_places` rows across 5 cities — Cebu (10), Manila (8), Bali (10), Bangkok (8), Singapore (10). Each row has name, place_type, category, neighborhood, blurb, image_url (Unsplash), rating, lat, lng, tag, source=`curated`, status=`active`, verified=`true`. Uses `ON CONFLICT (id) DO NOTHING` so it is safe to re-run. Ensures the DB-merge path in `GET /api/discovery/community` surfaces results for these destinations and the Pulse Wall shows place cards when posts are thin. Applied directly via Supabase JS service-role client (table now has 183 total rows). | 2026-07-01 |
| `0076_profile_emergency_contacts.sql` | Creates `profile_emergency_contacts` table (id, user_id→auth.users CASCADE, label, name, phone, email, notify_method enum `in_app\|sms\|email`, sort_order, created_at, updated_at; `pec_user_idx` on user_id; RLS enabled; `pec_own` policy — users read/write own rows; `pec_svc` policy — service role all). Backing store for Settings → Emergency Contacts screen; reusable across Safe Return sessions. Applied via Supabase Management API. | 2026-07-02 |
| `0077_trips_expansion.sql` | Extends `trip_status` enum with `draft` (before `planning`) and `archived` (after `cancelled`). Adds 14 new columns to `trips`: `trip_type` (default `leisure`), `timezone`, `destination_lat/lng DOUBLE PRECISION`, `destination_place_id`, `trip_notes`, and 9 privacy boolean columns: `show_on_profile` (default true), `show_in_discovery` (default false), `allow_friend_suggestions` (true), `allow_trip_crew_invites` (true), `allow_join_requests` (false), `show_exact_dates` (true), `show_destination_city` (true), `delayed_posting_default` (false), `precise_location_visible` (false). All new columns have safe defaults so existing rows are unaffected. | 2026-07-03 |
| `0078_trip_members_expansion.sql` | Extends `member_role` enum with `co_host` (after `owner`) and `viewer` (after `member`). Adds 3 new columns to `trip_members`: `status TEXT NOT NULL DEFAULT 'accepted'` with CHECK constraint (invited/accepted/declined/removed/left), `permissions JSONB` (nullable per-member permission overrides), `joined_at TIMESTAMPTZ`. Backfills existing invited-role rows to `status='invited'` and owner/member rows to `joined_at=created_at`. | 2026-07-03 |
| `0079_trip_sub_tables.sql` | Creates 11 new trip sub-resource tables: `trip_budget` (one-row-per-trip upsert: currency, total_budget, spent, breakdown jsonb; RLS owner-only), `trip_documents` (title, content, document_type enum 6 values, is_private; RLS members read non-private/own, owner sees all), `trip_join_requests` (user_id, status enum 4 values, message, reviewed_by/at; UNIQUE trip+user; RLS requester+owner read, requester insert), `trip_invite_links` (token UNIQUE, created_by, max_uses, use_count, expires_at, revoked_at; RLS owner manages, public reads valid links), `trip_saved_places` (place_id, place_name/type, lat/lng, notes; UNIQUE trip+user+place_id; RLS members read, member insert/own-delete), `trip_notes` (title, content, is_private, author_id; RLS members read non-private/own, owner sees all), `trip_checklists` (title, created_by; RLS members read+insert), `trip_checklist_items` (checklist_id, trip_id, label, is_done, assigned_to, due_date, sort_order; RLS members), `trip_activity_log` (actor_id, event_type, metadata jsonb; append-only; RLS owner+co_host read), `trip_reminders` (user_id, title, remind_at, is_sent; RLS users own rows), `trip_destinations` (city, country, lat/lng, place_id, arrival/departure dates, position; RLS members read, owner manages). All tables use IF NOT EXISTS, enable RLS, and have appropriate indexes. | 2026-07-03 |
| `0080_events_extension.sql` | Extends the Events system: adds `show_exact_location` (bool, default false), `rsvp_closed` (bool, default false), `safety_notes` (text nullable), `tags` (text[], default '{}') columns to `events`. Creates 10 new tables: `event_saves` (user-event PK bookmark table, owner RLS), `event_invites` (UNIQUE event+invitee, status enum pending/accepted/declined/revoked, inviter/invitee read+update RLS), `event_cohosts` (UNIQUE event+user, permissions JSONB, added_by; public read), `event_posts` (event-specific posts with media_urls/pinned; author write), `event_media` (image/video uploads per event; uploader write), `event_reports` (event-type or user-type reports, UNIQUE event+reporter+type, reporter RLS), `event_activity_log` (append-only audit log; host/mod read via subquery RLS), `event_share_links` (token UNIQUE via gen_random_bytes, max_uses/use_count/expires_at; creator manage + public read), `event_reminders` (UNIQUE event+user+remind_at, sent flag; owner RLS), `event_drafts` (host_id, data JSONB, last_saved_at autosave blobs; owner RLS). Adds 5 feature flag seeds: events_invites_enabled, events_cohosts_enabled, events_reports_enabled, events_reminders_enabled, events_share_links_enabled. All statements are idempotent (IF NOT EXISTS / DO $$). | 2026-07-03 |
| `0081_stamp_system_v2.sql` | Applied 2026-07-03. Creates the full Passport Stamps v2 data model: `stamp_definitions` (slug UNIQUE, name, description, stamp_type, category, rarity, is_active, is_repeatable, max_awards_per_user, level_config jsonb, criteria_type, criteria jsonb, visibility_default, source_system, city/country geo-lock, starts_at/ends_at; public read RLS for active rows; admin all), `user_stamps` (user_id→profiles, stamp_definition_id→stamp_definitions, source_type/id, earned_at, city/country, lat/lng PRIVATE, title_override, metadata, visibility, display_on_passport, is_revoked; owner read + public non-revoked-public read + friends-only read via user_friendships; service_role write), `stamp_award_events` (idempotency_key UNIQUE sha of user:def:sourceType:sourceId, status awarded/revoked/restored; owner + admin read; service_role write), `stamp_progress` (user+definition PK, progress_count, progress_target; owner read; service_role write), `stamp_collections` (slug UNIQUE, name, description; public read when is_active), `stamp_collection_items` (collection+definition PK, sort_order; public read), `stamp_campaigns` (slug UNIQUE, stamp_definition_id, starts_at/ends_at; public read when is_active; admin all). Includes 8 indexes. Seeds feature flags `stamp_system_v2_enabled` and `stamp_admin_award_enabled` (both false). Inserts ~40 starter stamp definitions across location/trip/social/safety/rent_buddy/special categories — all with `is_active=false` pending trigger wiring. All CREATE POLICY statements use DROP POLICY IF EXISTS guards (idempotent). Column-level `REVOKE SELECT (lat, lng) ON user_stamps FROM authenticated, anon` enforces DB-level privacy so PostgREST never exposes precise location to non-service callers regardless of row-level policies. | 2026-07-03 |
| `0082_stamp_definitions_v2.sql` | Applied 2026-07-03. Activates and adds the stamp definitions whose triggers were wired in Task #1048. **New rows (12):** `first_trip_created` (trip/common), `first_trip_completed` (trip/common), `solo_traveler` (trip/common), `group_tripper` (trip/uncommon), `weekend_wanderer` (trip/common), `first_postcard` (social/community/common), `safe_return_ready` (safety/safety/common/private), `safe_return_completed` (safety/safety/uncommon/private), `first_buddy_booking` (rent_buddy/common), `first_buddy_hosted` (rent_buddy/common), `hidden_gem_explorer` (special/uncommon), `verified_traveler` (safety/trust/uncommon). **Activated rows (4):** sets `is_active=true` on existing `road_warrior`, `frequent_flyer`, `long_haul`, `international_voyager` definitions (triggers now live). All INSERTs use ON CONFLICT (slug) DO NOTHING; UPDATE uses WHERE is_active = false — fully idempotent. | 2026-07-03 |
| `0094_profile_account_status_and_privacy.sql` | Adds `profiles.account_status TEXT NOT NULL DEFAULT 'active'` (backfills deactivated users from `user_account_states`); creates `profile_privacy_settings` table (user_id PK→auth.users, 18 privacy columns with defaults, RLS: `pps_own` all-ops for owner, `pps_svc` for service role); creates `user_deletion_requests` table (user_id PK→auth.users, requested_at, scheduled_at, status enum pending/cancelled/executed, RLS: `udr_own` SELECT for owner, `udr_svc` all for service role; partial index on pending requests). Required by `GET /api/me/privacy`, `PATCH /api/me/privacy`, `POST /api/me/delete-request`, `POST /api/me/deactivate`, `POST /api/me/reactivate`. Applied 2026-07-03 via Supabase management API; schema verified and all three routes smoke-tested against production (all returned HTTP 200). | 2026-07-03 |
| `20260702_crew_location_flags_reseed.sql` | Re-seeds the three trip crew location feature flags that migration `0041_trip_crew_location.sql` failed to insert. The original migration used `INSERT INTO feature_flags (key, ...)` but the `feature_flags` PK column is named `flag`, so all three rows were silently skipped. This migration inserts `trip_crew_map_enabled` (true), `trip_crew_live_share_enabled` (true), and `trip_crew_ghost_mode_enabled` (true) using `ON CONFLICT (flag) DO NOTHING` so it is safe to re-run. Also fixes the companion bug: the local `isFlagEnabled` in `routes/tripCrewLocation.ts` was using `.eq("key", ...)` instead of the shared `lib/featureFlags.ts` helper (`.eq("flag", ...)`), causing `GET /api/trips/:tripId/crew/map` to always return `featureEnabled: false` regardless of DB state. Fix: removed the local duplicate and imported `isFlagEnabled` from `../lib/featureFlags.js`. | 2026-07-02 |
| `0083_place_category_columns.sql` | Adds `primary_category TEXT NOT NULL DEFAULT 'places'` and `secondary_categories text[] NOT NULL DEFAULT '{}'` to `discovery_places`. Backfills `primary_category` from existing `category`/`place_type` values using the canonical Discovery tab mapping (food/beaches/nightlife/activities/events/places/transport — default `'places'`). Adds `discovery_places_primary_category_idx` on `(primary_category)` and `discovery_places_city_category_idx` on `(city, primary_category)` for fast category-filtered queries. Safe to re-run (IF NOT EXISTS / WHERE primary_category IS NULL guards). Backfill distributed 183 rows: places 67, food 58, nightlife 27, beaches 17, activities 12, events 2. | 2026-07-02 |
| `0084_reviews_place_entity.sql` | Extends `review_entity_type` enum with `'place'` value via idempotent `DO $$` check (`ALTER TYPE ... ADD VALUE` applied in a separate transaction from the policy to satisfy PostgreSQL commit ordering). Creates `"Authors can delete own place reviews"` DELETE policy on `reviews` (`reviewer_id = auth.uid() AND entity_type = 'place'::review_entity_type`). Enables `POST /api/reviews` with `entityType='place'` and place-rating deletes for the Discovery place cards feature. | 2026-07-02 |
| `0085_enable_passport_flags.sql` | Enables four feature flags that were seeded as `false` in earlier migrations: `passport_stamps_enabled` (0037/0042), `passport_memories_enabled` (0037/0042), `stamp_system_v2_enabled` (0081), `stamp_admin_award_enabled` (0081). Uses `ON CONFLICT (flag) DO UPDATE SET enabled = true` — safe to re-run. Without this migration stamps and memories are blocked for all users even though the routes and tables are fully implemented. Apply via Supabase dashboard SQL editor. | 2026-07-03 |
| `0086_discovery_places_osm_id.sql` | Adds `osm_id TEXT` column to `discovery_places` with a partial unique index `discovery_places_osm_id_idx ON discovery_places (osm_id) WHERE osm_id IS NOT NULL`. Sets `DEFAULT ''` on the `city` column so that OSM-sourced rows can be inserted without a destination city (they exist only for `saved_count` tracking and won't appear in city-filtered discovery queries). Enables the wishlist handler to upsert lightweight `source='osm'` rows keyed by Overpass element ID (e.g. `"node/12345678"`) and the `GET /api/discovery` handler to enrich OSM results with real save counts before caching. | 2026-07-03 |

| `0087_profiles_cover_photo_url.sql` | Adds `cover_photo_url TEXT` to `profiles` via `ADD COLUMN IF NOT EXISTS`. Multiple API routes (GET/PATCH /me/profile, passport, admin) SELECT or UPDATE this column; its absence causes PostgREST PGRST204 errors that surfaced as an error banner on the Edit Profile screen and prevented profile saves. Also fixes both PGRST204 fallback guards in the profile route (GET line 200, PATCH line 384) — they previously only checked PostgreSQL code `42703` and never triggered on the PostgREST PGRST204 variant. Safe to re-run. Applied via Supabase Management API. | 2026-07-03 |
| `0088_wishlist_places.sql` | Creates `wishlist_places` table (id, user_id→auth.users CASCADE, place_id text, place_data jsonb, list_id text DEFAULT 'global', saved_at; UNIQUE user+place+list; owner-only RLS; `wishlist_places_user_list_idx` on user_id+list_id+saved_at DESC). Stores full place data as JSONB so any bookmarked place (DB-sourced or OSM-sourced) can be persisted. Separate from `saved_places` (0074) which carries a FK to discovery_places and is reserved for DB-only places. Includes TRUNCATE guard trigger. API server writes via service role key. **Renumbered from 0076:** the original file was created as `0076_wishlist_places.sql` but migration `0076_profile_emergency_contacts.sql` had already been applied to production under that number, causing a conflict. File renamed to 0088 to avoid collision. | 2026-07-03 |
| `0089_decrement_discovery_place_saved_count.sql` | Creates `public.decrement_discovery_place_saved_count(p_id UUID) RETURNS integer` SECURITY DEFINER Postgres function with privilege hardening. Executes `UPDATE discovery_places SET saved_count = GREATEST(0, saved_count - 1) WHERE id = p_id RETURNING saved_count` atomically and returns the new value. Fixes the stale-snapshot race in `trackOsmPlaceUnsave` where two concurrent different-user unsaves both read the same `saved_count` snapshot in Node.js, both compute `snapshot - 1`, and both overwrite the DB with the same value — leaving the count one step too high. Privilege hardening: `SET search_path = public` (prevents search_path injection); `REVOKE ALL FROM PUBLIC, anon, authenticated` (removes the default PUBLIC execute grant that would otherwise allow any PostgREST caller to invoke it directly and tamper with counts); `GRANT EXECUTE TO service_role` (restricts execution to the backend service account only). Called via `svc.rpc('decrement_discovery_place_saved_count', { p_id })` from `artifacts/api-server/src/routes/wishlist.ts`. | 2026-07-03 |
| `0091_activate_stamp_definitions.sql` | Activates the beta-launch subset of the ~40 stamp definitions seeded as `is_active=false` by migration 0081. **Activated (4 rows):** `early_adopter`, `founding_member`, `ambassador`, `beta_tester` — all have `criteria_type='admin_only'`, so no automatic trigger is required; they are awarded exclusively via `POST /api/admin/stamps/award`. These are high-value engagement stamps for the beta period and are safe to enable immediately. **Not activated (kept dormant):** (a) 0081 definitions superseded by cleaner v2 slugs added in 0082 (`first_trip`, `solo_adventurer`, `crew_captain`, `weekend_warrior`, `safe_traveler`, `safety_advocate`, `verified_identity`, `first_session`); (b) automatic definitions whose route-level triggers are not yet wired: location stamps (`city_explorer`, `globe_trotter`, `world_citizen`, `continent_hopper`, `neighborhood_local`, `hidden_gem_hunter`, `night_owl`, `sunrise_chaser`), social count stamps (`first_post`, `storyteller`, `photographer`, `community_connector`, `popular_traveler`, `travel_influencer`), trip-outcome stamps (`trip_planner`, `good_host`), trust-level stamps (`trusted_member`, `city_trusted_member`), rent-a-buddy progression stamps (`buddy_veteran`, `top_rated_buddy`, `nightlife_guide`, `food_guide`), and event stamps (`event_participant`, `event_host`). Uses `WHERE is_active = false` guard — idempotent. | 2026-07-03 |
| `0092_seed_rent_buddy_launch_cities.sql` | Seeds the three initial Philippines launch cities into `rent_buddy_city_rollouts` at `public_mvp` status: Cebu, Manila, Davao City. Without at least one row at `public_mvp` or `beta_testing` status every call to `checkRentBuddyAccess` returns `city_not_available` and Rent a Buddy is invisible to all users even though migration 0090 fully deployed the tables and flags. Uses `ON CONFLICT (city) DO NOTHING` — safe to re-run. Also extends `scripts/src/verify-db-rent-buddy-rollout.mjs` and `scripts/check-db-triggers.sh` to verify at least one live city exists in production as part of the `db-triggers` pre-release check; a release will not pass until this seed is applied. Fixes the incorrect `status = 'live'` example in `docs/production-migration-runbook.md §9.7` (`'live'` is not a valid `rent_buddy_city_status` enum value; correct value is `'public_mvp'`). **Note:** Production table predated the 0090 schema file and lacks the `country` column and `UNIQUE(city)` constraint. Cebu pre-existed at `status='live'` (not recognized by `checkRentBuddyAccess`); updated to `public_mvp`. Manila and Davao City inserted fresh. | 2026-07-04 |

## Stamp award durability (no migration required)

**Implemented 2026-07-03** — No new tables or columns. Pure engine change in `artifacts/api-server/src/services/passport/StampAwardEngine.ts`.

### Problem
`awardTripCompletionStamps` calls `awardStamp` fire-and-forget. Inside `awardStamp`, step 6 inserts a `stamp_award_events` row (idempotency key committed) and step 7 inserts the `user_stamps` row. If the DB drops between steps 6 and 7, the event row exists with `status='awarded'` but no passport row is created. The user's passport stays empty until `recalculate/me` is triggered manually.

### Fix — two layers of defence

**Layer 1 — exponential-backoff retry (step 7).**  
`user_stamps` insert is retried up to 3 times on transient errors (delays: 100 ms, 200 ms). Unique-constraint violations (`23505`) are never retried — they are not transient. This closes the window for the vast majority of transient DB glitches during a live request.

**Layer 2 — self-healing idempotency check (step 3).**  
When the idempotency check at step 3 finds an existing `awarded` event but no corresponding `user_stamps` row, the engine skips steps 4–6 (dedup guards + event insert) and jumps directly to the stamp insert. This means any subsequent call to `awardStamp` with the same `(userId, definitionSlug, sourceType, sourceId)` tuple will automatically heal a partial failure — the passport row is created without needing a manual recalculate. The `recalculateForUser` engine path remains available as a manual repair tool for any rows that slipped through before this fix was deployed.

| `0093_activate_stamp_definitions.sql` | Activates 17 previously-inactive stamp definitions whose route-level triggers are now wired: `first_post`, `storyteller`, `photographer` (post counts in posts.ts); `city_explorer`, `globe_trotter`, `world_citizen` (GPS location milestones in posts.ts); `community_connector`, `popular_traveler`, `travel_influencer` (follow counts in follows.ts); `trip_planner`, `good_host` (trip lifecycle in trips.ts); `buddy_veteran`, `nightlife_guide`, `food_guide`, `top_rated_buddy` (rent-a-buddy in rentABuddy.ts); `event_host`, `event_participant` (event completion in events.ts). All 17 rows confirmed `is_active = true` in production via spot-check. Applied via Supabase JS service-role client (UPDATE … WHERE is_active = false — idempotent). | 2026-07-04 |
| `0094_profile_account_status_and_privacy.sql` | Three items used by live backend routes that had no SQL file committed: **(1)** `profiles.account_status TEXT NOT NULL DEFAULT 'active'` — read by `GET /admin/users` (explicit SELECT column, no guard) and written by `POST /me/deactivate` + `POST /me/reactivate` + `POST /me/delete-request`; backfills deactivated rows from `user_account_states` to stay consistent. **(2)** `profile_privacy_settings` table — 17-column per-user privacy config (profile_visibility, show_* toggles, allow_* toggles, delayed_posting_default, precise_location_visible; user_id PK → auth.users CASCADE; RLS: users manage own row, service role all); used by `GET /me/privacy` (table-missing guard exists) and `PATCH /me/privacy` (no guard — hard-fails if table absent). **(3)** `user_deletion_requests` table — (user_id PK, requested_at, scheduled_at, status pending/cancelled/executed, cancelled_at, executed_at; RLS: users SELECT own, service role all; partial index on status='pending'); used by `POST /me/delete-request` (awaited upsert — hard-fails if table absent). All statements idempotent (ADD COLUMN IF NOT EXISTS / CREATE TABLE IF NOT EXISTS). **Also fixed:** `hiddenGems.ts` local `requireAdmin()` was reading `profiles.is_admin` (column never created) instead of `profiles.role`; changed to `.select("role")` with `.role === "admin"` check — matching the pattern used everywhere else in the codebase. | 2026-07-03 |
| `0095_post_category.sql` | Adds nullable `category TEXT` column to `posts` (`ADD COLUMN IF NOT EXISTS` — idempotent). Values match the `PostCategory` enum on the client (e.g. `food`, `nightlife`, `beach`, `adventure`). Required for the category stamp overlay on the post detail screen and for the category picker in the post composer. `POST /api/posts` accepts and persists category; all feed and post-detail routes select and return it. | 2026-07-04 |
| `0097_post_saves.sql` | Creates `post_saves` table (`user_id`, `post_id`, `created_at`; `PRIMARY KEY (user_id, post_id)` enforces uniqueness; index on `post_id`; RLS: users manage own rows via `post_saves_own` policy). Adds `save_count INTEGER NOT NULL DEFAULT 0` to `posts` (maintained by the API server on `POST /api/posts/:id/save` and `DELETE /api/posts/:id/save`). Duplicate-save is always idempotent via `ON CONFLICT DO NOTHING`; unsave of a non-existent row is handled gracefully. Feed and post-detail responses include `savedByMe` and `save_count`. Applied manually to Supabase. | 2026-07-04 |
| `0098_profile_translation_prefs.sql` | Adds four Telegraph translation-preference columns to `profiles` (all `ADD COLUMN IF NOT EXISTS` — idempotent): `preferred_message_language TEXT`, `auto_translate_messages BOOLEAN NOT NULL DEFAULT FALSE`, `show_original_messages BOOLEAN NOT NULL DEFAULT FALSE`, `translation_updated_at TIMESTAMPTZ`. Read/written by `GET /api/me/translation-settings` and `PATCH /api/me/translation-settings` in `messaging.ts`. Note: `preferred_language` (BCP-47 fallback) was added in migration 0018; `tag_permission` was added in migration 0043. Applied manually to Supabase. | 2026-07-04 |
| `0100_backfill_display_name.sql` | One-time data backfill: copies `name → display_name` for every `profiles` row where `display_name IS NULL AND name IS NOT NULL`. Makes historical rows consistent with the dual-write pattern introduced in `PATCH /api/me/profile`. Idempotent — rows that already have `display_name` set are untouched. Verified post-apply: 0 rows remain with `display_name IS NULL AND name IS NOT NULL`. | 2026-07-04 |

## Schema verification notes (2026-07-03)

### Verification query

Run the following in the Supabase dashboard SQL editor to confirm both columns exist in production:

```sql
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name   = 'profiles'
  AND column_name  IN ('role', 'cover_photo_url')
ORDER BY column_name;
```

Expected output (2 rows):

| column_name     | data_type         | is_nullable | column_default |
|-----------------|-------------------|-------------|----------------|
| cover_photo_url | text              | YES         | NULL           |
| role            | text              | NO          | 'user'::text   |

If either row is missing, apply the relevant migration below before deploying.

---

### `profiles.role` column

The `profiles` table carries a `role TEXT NOT NULL DEFAULT 'user'` column. This column was part of the initial schema and has no standalone numbered migration file — it is present in the baseline `profiles` table DDL. Verified values: `'user'` (default for all users) and `'admin'`.

**Admin access guard** — `requireAdmin` in `artifacts/api-server/src/routes/admin.ts` reads role exclusively from `profiles.role`:

```ts
const { data, error } = await sc.from("profiles").select("role").eq("id", user.id).maybeSingle();
if (error || !data || (data as any).role !== "admin") {
  res.status(403).json({ error: "forbidden", message: "Admin role required" });
}
```

Role is **not** read from `auth.app_metadata` or `auth.users.raw_app_meta_data`. To grant admin access to a user: `UPDATE profiles SET role = 'admin' WHERE id = '<uuid>';`.

### Migration 0101 (supabase/migrations) — `search_history` table

`supabase/migrations/0101_search_history.sql` creates the `search_history` table used by the global search feature (Phases 11–18, 26).

| Detail | Value |
|--------|-------|
| File | `supabase/migrations/0101_search_history.sql` |
| Table | `search_history` |
| Columns | `id UUID PK`, `user_id UUID→auth.users`, `query TEXT NOT NULL`, `search_type TEXT NOT NULL DEFAULT 'all'`, `searched_at TIMESTAMPTZ DEFAULT now()` |
| Index | `search_history_user_idx ON search_history (user_id, searched_at DESC)` |
| RLS | Enabled; users read/write only their own rows (`auth.uid() = user_id`) |
| Note | The API server enforces a 50-row per-user cap (prunes oldest on POST). Apply before deploying the search intelligence update. |

**Verify:**
```sql
SELECT to_regclass('public.search_history'), indexname
FROM pg_indexes WHERE indexname = 'search_history_user_idx';
```

---

### `profiles.cover_photo_url` column

Added by migration `0087_profiles_cover_photo_url.sql` (`ADD COLUMN IF NOT EXISTS cover_photo_url TEXT`). Used by `GET /api/me/profile`, `PATCH /api/me/profile`, passport loader, and admin media routes. Absence causes PostgREST PGRST204 errors on profile saves — apply 0087 if the Edit Profile screen shows an error banner.

### Migrations 0095, 0097, 0098, 0099 — applied manually 2026-07-04

These migrations were applied directly via the Supabase SQL editor and have no automated runner entry. The SQL source files are committed to `artifacts/api-server/src/migrations/` for reference and future idempotent re-runs.

| Migration | Key SQL | Verify |
|-----------|---------|--------|
| `0095_post_category.sql` | `ALTER TABLE posts ADD COLUMN IF NOT EXISTS category TEXT` | `SELECT column_name FROM information_schema.columns WHERE table_name='posts' AND column_name='category'` |
| `0097_post_saves.sql` | `CREATE TABLE IF NOT EXISTS post_saves (user_id UUID, post_id UUID, PRIMARY KEY (user_id, post_id))` + `ALTER TABLE posts ADD COLUMN IF NOT EXISTS save_count INTEGER NOT NULL DEFAULT 0` | `SELECT to_regclass('public.post_saves')` |
| `0098_profile_translation_prefs.sql` | Four `ADD COLUMN IF NOT EXISTS` on `profiles`: `preferred_message_language TEXT`, `auto_translate_messages BOOLEAN`, `show_original_messages BOOLEAN`, `translation_updated_at TIMESTAMPTZ` | `SELECT column_name FROM information_schema.columns WHERE table_name='profiles' AND column_name='auto_translate_messages'` |
| `0099_missing_indexes.sql` | `CREATE INDEX IF NOT EXISTS posts_author_status_idx ON posts (author_id, post_status)` + `CREATE INDEX IF NOT EXISTS post_saves_post_created_idx ON post_saves (post_id, created_at DESC)` | `SELECT indexname FROM pg_indexes WHERE indexname IN ('posts_author_status_idx','post_saves_post_created_idx')` |
| `0101_feed_save_indexes.sql` | `CREATE INDEX IF NOT EXISTS posts_status_published_at_idx ON posts (post_status, published_at DESC)` + `CREATE INDEX IF NOT EXISTS post_saves_created_at_idx ON post_saves (created_at DESC)` | `SELECT indexname FROM pg_indexes WHERE indexname IN ('posts_status_published_at_idx','post_saves_created_at_idx')` |

All statements are idempotent — safe to re-run.

### Migration 0100 — applied 2026-07-04

`artifacts/api-server/migrations/0100_backfill_display_name.sql` was applied to production on 2026-07-04 via the Supabase Management API. The backfill copied `name → display_name` for every `profiles` row where `display_name IS NULL AND name IS NOT NULL`. Post-apply verification confirmed 0 remaining rows with `display_name IS NULL AND name IS NOT NULL`.

| Effect | Detail |
|--------|--------|
| Rows updated | All `profiles` rows where `display_name IS NULL AND name IS NOT NULL` (at time of apply) |
| Rows unchanged | Rows that already had `display_name` set, or where `name` was also NULL |
| Safe to re-run | Yes — `WHERE display_name IS NULL` makes subsequent runs a no-op |

### Migration 0101 — pending production apply

`artifacts/api-server/src/migrations/0101_feed_save_indexes.sql` adds two indexes to keep the feed and saves queries fast as data grows:

| Index | Table | Columns | Query served |
|-------|-------|---------|--------------|
| `posts_status_published_at_idx` | `posts` | `(post_status, published_at DESC)` | Feed: `WHERE post_status = 'published' ORDER BY published_at DESC LIMIT 20` |
| `post_saves_created_at_idx` | `post_saves` | `(created_at DESC)` | Saves listing: `ORDER BY created_at DESC` |

**Apply:** paste the file contents into the Supabase SQL editor and run. Both `CREATE INDEX IF NOT EXISTS` statements are idempotent.

**Verify:**
```sql
SELECT indexname FROM pg_indexes
WHERE indexname IN ('posts_status_published_at_idx', 'post_saves_created_at_idx');
```

Both index names should appear in the result before marking this migration applied.

**Why these two indexes?**

- `posts_author_status_idx` (0099) covers `(author_id, post_status)` — good for per-author filtered feeds — but the public feed query only filters on `post_status` and sorts by `published_at`. Without `posts_status_published_at_idx`, Postgres cannot use that index and falls back to a sequential scan + sort.
- `post_saves_post_created_idx` (0099) covers `(post_id, created_at DESC)` — efficient for "who saved this post" lookups — but a full saves listing sorted only by `created_at` still requires a seq-scan + sort. `post_saves_created_at_idx` on `(created_at DESC)` lets Postgres walk the index in order and stop at the LIMIT.

| `0103_post_media.sql` | Creates `post_media` table (id UUID PK, post_id→posts CASCADE, user_id→auth.users CASCADE, media_type text CHECK image\|video, storage_bucket/path, public_url, thumbnail_url, thumbnail_storage_path, mime_type, file_size_bytes bigint, duration_seconds float, width/height int, processing_status text CHECK pending\|ready\|failed DEFAULT pending, moderation_status text CHECK pending\|approved\|flagged\|rejected DEFAULT pending, sort_order int DEFAULT 0, created_at/updated_at timestamptz); indexes: `post_media_post_id_idx`, `post_media_user_id_idx`, `post_media_post_status_idx`; RLS enabled; `post_media_owner_write` policy (owner write + parent post ownership check); `post_media_public_select` policy (owner sees all, others see ready+approved+unblocked media on public or trip_only posts); storage bucket policies on `storage.objects` for `post-media` bucket (owner insert/delete, public read); `ALTER TABLE posts ADD COLUMN IF NOT EXISTS primary_media_type TEXT, media_count INTEGER NOT NULL DEFAULT 0, has_video BOOLEAN NOT NULL DEFAULT false`; `ALTER TABLE passport_postcards` same three columns; back-fills `primary_media_type/media_count/has_video` on existing rows of both tables. **RLS fix applied at deploy time**: migration file had `p.visibility = 'followers'` which is not a valid `post_visibility` enum value (production enum: `public\|trip_only\|private`); corrected to remove the friends-only branch before applying. | 2026-07-05 |
| `0104_compass_new_tables.sql` | Creates `compass_feedback` (per-recommendation in-feed feedback actions: not_now/hide/report/etc., RLS owner-only), `compass_recent_context` (persisted context session with 4h TTL, unique per user, RLS owner-only), `compass_settings` (user-level Compass privacy/data-use toggle settings, RLS owner-only); all tables idempotent (IF NOT EXISTS) **Correction 2026-07-17:** `compass_feedback` and `compass_settings` were missing live; applied via Management API. | 2026-07-05 |
| `0105_compass_performance_indexes.sql` | Adds covering indexes for Compass item hydrator queries: `events_compass_city_starts_idx` (city, starts_at, status, visibility), `events_compass_category_idx`, `trips_compass_destination_idx`, `discovery_places_compass_city_idx` + `discovery_places_compass_category_idx`, `places_compass_city_category_idx` (partial, excludes rejected), `profiles_compass_city_idx` (partial, active-only), `profiles_compass_city_role_idx`; all idempotent (IF NOT EXISTS) **Correction 2026-07-17:** original file referenced columns/tables that do not exist live (`events.status` (live: `state`), `trips.destination/starts_at`, `places`, `discovery_places.moderation_status/is_active`, `profiles.is_active`). **Rewritten 2026-07-17** against the real schema — now creates `events_compass_city_starts_idx` (city, starts_at, state, visibility), `events_compass_category_idx` (category, state, visibility, starts_at), `trips_compass_destination_idx` (destination_city, start_date, visibility, status), `discovery_places_compass_city_idx` (city, category, status), `discovery_places_compass_category_idx` (category, status), `profiles_compass_city_idx` (current_city, verification_status); `places` and role/is_active indexes removed. Applied via Management API; verified all six index names in `pg_indexes`. | 2026-07-05, rewritten+applied 2026-07-17 |
| `0106_engagement_indexes.sql` | Adds five composite indexes for cursor-based pagination in `GET /api/engagement/likes`: `idx_posts_likes_post_created` on `posts_likes (post_id, created_at DESC)`, `idx_post_reactions_post_emoji_created` on `post_reactions (post_id, emoji, created_at DESC)`, `idx_comment_likes_comment_created` on `comment_likes (comment_id, created_at DESC)`, `idx_highlight_likes_highlight_created` on `highlight_likes (highlight_id, created_at DESC)`, `idx_memory_likes_memory_created` on `memory_likes (memory_id, created_at DESC)`; all idempotent (IF NOT EXISTS). **Apply:** paste `artifacts/api-server/src/migrations/0106_engagement_indexes.sql` into the Supabase SQL editor and run. **Verify:** `SELECT indexname FROM pg_indexes WHERE indexname IN ('idx_posts_likes_post_created','idx_post_reactions_post_emoji_created','idx_comment_likes_comment_created','idx_highlight_likes_highlight_created','idx_memory_likes_memory_created');` — all five names must appear. | 2026-07-05 |
| `0108_circle_schema_tracked.sql` | **Canonical tracking for all Find Your Circle tables** (previously applied to production via non-canonical migrations 0115–0122). Creates seven tables — (1) `circle_visibility_settings` (PK user_id→profiles, global_enabled BOOLEAN DEFAULT false, visibility_mode CHECK status_only\|approximate_area\|venue_checkin\|precise_live, trip_sharing_default TEXT CHECK off\|status_only\|approximate_area\|venue_checkin DEFAULT status_only, event_sharing_default same, is_paused BOOLEAN DEFAULT false, paused_until TIMESTAMPTZ, consent_version TEXT, consented_at TIMESTAMPTZ, updated_at; RLS: owner all + service_role all; seeds find_your_circle_enabled and find_your_circle_disabled feature flags); (2) `circle_context_settings` (id UUID PK, user_id→profiles, context_type CHECK trip\|event, context_id UUID, enabled BOOLEAN DEFAULT true, visibility_mode_override TEXT nullable, paused BOOLEAN DEFAULT false, paused_until TIMESTAMPTZ, UNIQUE user_id+context_type+context_id; index: ccs_context_idx; RLS: owner all + service_role all); (3) `circle_presence` (id UUID PK, user_id→profiles, context_type, context_id, status CHECK active\|arrived\|with_group\|leaving\|safe\|needs_help DEFAULT active, status_label/approximate_label/venue_label TEXT, checked_in BOOLEAN, stale_after_secs INTEGER DEFAULT 1800, last_seen_at TIMESTAMPTZ, expires_at TIMESTAMPTZ, is_stale BOOLEAN, needs_help BOOLEAN, UNIQUE user_id+context_type+context_id; indexes: cp_context_idx + cp_expires_at_idx + cp_last_seen_idx; RLS: owner read own row + owner write + service_role all); (4) `circle_checkins` (immutable log: id, user_id, context_type, context_id, checkin_type CHECK arrived\|with_group\|leaving\|safe\|needs_help, note/venue_label/approximate_label TEXT, created_at; indexes: ccin_user_context_idx + ccin_context_idx; RLS: owner read + owner insert + service_role all); (5) `circle_member_visibility_overrides` (id, user_id→profiles, target_user_id→profiles, context_type, context_id, direction CHECK hide_from_me\|hide_me_from, hidden BOOLEAN DEFAULT true, UNIQUE user_id+target_user_id+context_type+context_id+direction; indexes: cmvo_user_context_idx + cmvo_target_context_idx; RLS: owner all + service_role all); (6) `circle_meeting_points` (id, context_type, context_id, host_user_id→profiles, venue_label/approximate_label/description TEXT, is_active BOOLEAN DEFAULT true; index: cmp_context_active_idx; RLS: service_role all only — membership gate enforced at API layer); (7) `circle_audit_events` (immutable log: id, actor_user_id→profiles nullable, target_user_id→profiles nullable, context_type, context_id, event_type CHECK 11 values, metadata JSONB; indexes: cae_actor_idx + cae_target_idx + cae_context_idx + cae_event_type_idx; RLS: actor\|target read own rows + service_role all). All statements idempotent (IF NOT EXISTS, ADD COLUMN IF NOT EXISTS, DROP POLICY IF EXISTS). **Apply:** paste `artifacts/api-server/src/migrations/0108_circle_schema_tracked.sql` into the Supabase SQL editor and run. This is safe on a database that already has these tables — all statements are guards. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name IN ('circle_visibility_settings','circle_context_settings','circle_presence','circle_checkins','circle_member_visibility_overrides','circle_meeting_points','circle_audit_events') ORDER BY table_name;` — all seven names must appear. `SELECT policyname, tablename FROM pg_policies WHERE tablename LIKE 'circle_%' ORDER BY tablename, policyname;` — every table must show at least a service_role policy. | 2026-07-05 |
| `0107_rent_buddy_admin_actions.sql` | Creates `rent_buddy_admin_actions` audit log table. This table was referenced by 20+ INSERT statements in `rentABuddy.ts` and `rentABuddyMarketplace.ts` (admin suspend/approve/feature actions) and declared in `database.types.ts`, but was absent from all prior migrations. Schema: `id UUID PK, admin_id UUID FK→profiles (nullable), target_type TEXT NOT NULL, target_id TEXT NOT NULL, action TEXT NOT NULL, notes TEXT, details JSONB, created_at TIMESTAMPTZ`; RLS enabled with service-role-only policy; indexes on `(admin_id, created_at DESC)` and `(target_type, target_id)`. **Apply:** paste `artifacts/api-server/migrations/0107_rent_buddy_admin_actions.sql` into the Supabase SQL editor and run. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name = 'rent_buddy_admin_actions';` — the table name must appear. Also run `SELECT policyname FROM pg_policies WHERE tablename = 'rent_buddy_admin_actions';` — `rb_admin_actions_svc` must appear. | 2026-07-05 |
| `0109_claim_invite_link_slot.sql` | Creates two SECURITY DEFINER functions used by `POST /api/trips/invite-link/:token/accept`: `claim_invite_link_slot(link_id uuid) RETURNS boolean` — increments `use_count` atomically gated on `max_uses`, returns FALSE when the cap is hit; `release_invite_link_slot(link_id uuid)` — decrements `use_count` (floors at 0) as a compensation step when the subsequent `trip_members` INSERT fails. Both functions revoke PUBLIC/anon/authenticated execute rights and grant only to `service_role`. **Apply:** paste `artifacts/api-server/src/migrations/0109_claim_invite_link_slot.sql` into the Supabase SQL editor and run (both `CREATE OR REPLACE FUNCTION` statements are idempotent). Must be applied before 0110. **Verify:** `SELECT proname FROM pg_proc WHERE proname IN ('claim_invite_link_slot', 'release_invite_link_slot');` — both names must appear. The pre-release `db-triggers` check confirms these are live before any EAS build. | 2026-07-05 |
| `0110_invite_link_idempotency.sql` | Creates `trip_invite_link_attempts` table (PK `link_id`+`user_id`, `claimed_at`; service-role-only access) and replaces the two-step claim with `claim_invite_link_slot_for_user(link_id, user_id) RETURNS text`. The combined function atomically: inserts the attempt row + increments `use_count` (returns `'claimed'`), or returns `'already_attempted'` if a prior request already claimed a slot that never completed (so the handler can skip re-claiming and retry the INSERT), or returns `'limit_reached'` when `max_uses` is exhausted. Both table and function are SECURITY DEFINER / service-role-only. **Apply:** paste `artifacts/api-server/src/migrations/0110_invite_link_idempotency.sql` into the Supabase SQL editor and run — must be applied after 0109. **Verify:** `SELECT relname FROM pg_class WHERE relname = 'trip_invite_link_attempts' AND relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'public');` — the table name must appear. Also `SELECT proname FROM pg_proc WHERE proname = 'claim_invite_link_slot_for_user';` — the function name must appear. | 2026-07-05 |
| `0113_cleanup_stale_invite_attempts.sql` | Creates `cleanup_stale_invite_link_attempts() RETURNS TABLE (link_id, user_id, claimed_at, trip_id)` SECURITY DEFINER function. Removes `trip_invite_link_attempts` rows where a matching `trip_members` row already exists for that trip+user — these arise when `clearAttempt()` fails silently after a successful join. Unlike `reconcile_invite_link_slots` (0111), no min-age gate is needed: if the member row exists the attempt row is definitively stale. No use_count adjustment is made (the slot was legitimately used). Concurrent calls are safe (plain DELETE is idempotent per PK). Called by `POST /api/admin/trips/reconcile-invite-slots` alongside `reconcile_invite_link_slots`, adding `staleAttempts` + `stale` fields to the response. **Status: Applied to production** — verified 2026-07-05 via Supabase Management API: `SELECT proname FROM pg_proc WHERE proname = 'cleanup_stale_invite_link_attempts'` returns `[{"proname":"cleanup_stale_invite_link_attempts"}]`. **Apply (if missing):** paste `artifacts/api-server/src/migrations/0113_cleanup_stale_invite_attempts.sql` into the Supabase SQL editor and run — safe to run after 0110. **Verify:** `SELECT proname FROM pg_proc WHERE proname = 'cleanup_stale_invite_link_attempts';` — function name must appear. | 2026-07-05 |
| `0111_reconcile_invite_slots.sql` | Creates `reconcile_invite_link_slots(min_age_minutes integer DEFAULT 5) RETURNS TABLE (link_id, user_id, claimed_at, trip_id)` SECURITY DEFINER function. Finds stranded invite-link slots — `trip_invite_link_attempts` rows older than `min_age_minutes` with no corresponding `trip_members` row for that trip+user — and for each: decrements `use_count` (floors at 0) in `trip_invite_links` and deletes the orphaned attempt row. Uses `FOR UPDATE OF ... SKIP LOCKED` so concurrent reconciliation runs never double-fix. Called by `POST /api/admin/trips/reconcile-invite-slots` (admin-only) and by the automated `InviteSlotSweeper` background job (see below). **Status: Applied to production** — verified 2026-07-05 via Supabase Management API: `SELECT proname, pronargs FROM pg_proc WHERE proname = 'reconcile_invite_link_slots'` returns `[{"proname":"reconcile_invite_link_slots","pronargs":1}]`. **Apply (if missing):** paste `artifacts/api-server/src/migrations/0111_reconcile_invite_slots.sql` into the Supabase SQL editor and run — must be applied after 0110. **Verify:** `SELECT proname FROM pg_proc WHERE proname = 'reconcile_invite_link_slots';` — function name must appear. The pre-release `db-triggers` check confirms this function is live before any EAS build. | 2026-07-05 |
| `0112_trip_members_invite_link_id.sql` | Adds `invite_link_id UUID REFERENCES trip_invite_links(id) ON DELETE SET NULL` (nullable) to `trip_members`. Enables per-link audit queries ("how many current members joined via link X?"). All pre-existing rows keep `NULL`; new members who join via an invite link have the column set by the `POST /api/trips/invite-link/:token/accept` handler. Also creates partial index `trip_members_invite_link_id_idx` on `(invite_link_id) WHERE invite_link_id IS NOT NULL`. `ON DELETE SET NULL` ensures deleting an invite link does not remove its members. **Apply:** paste `artifacts/api-server/src/migrations/0112_trip_members_invite_link_id.sql` into the Supabase SQL editor and run — must be applied after 0110 (requires `trip_invite_links` table). **Verify:** `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name='trip_members' AND column_name='invite_link_id';` — one row must appear with `data_type='uuid'` and `is_nullable='YES'`. Also: `SELECT indexname FROM pg_indexes WHERE tablename='trip_members' AND indexname='trip_members_invite_link_id_idx';` — index name must appear. | 2026-07-05 |

### Invite-link stranded-slot cleanup — InviteSlotSweeper background job

The Postgres function added in migration 0111 (`reconcile_invite_link_slots`) is
called automatically by the `InviteSlotSweeper` background job in
`artifacts/api-server/src/lib/inviteSlotSweeper.ts`.  No additional migration is
required — the cleanup runs entirely in Node.js process space using the existing
service-role client.

**What it does**

On server startup (after a 60-second delay to let the service fully initialise)
and then on every subsequent interval, the sweeper calls:

```sql
SELECT * FROM reconcile_invite_link_slots(min_age_minutes => <TTL_MINUTES>);
```

For each row returned (a stranded attempt with no matching `trip_members` row)
the Postgres function has already:
1. Decremented `use_count` (floored at 0) in `trip_invite_links`.
2. Deleted the orphaned `trip_invite_link_attempts` row.

The sweeper logs the number of recovered slots at `INFO` level when any are
found, and at `DEBUG` level when the run finds nothing.  Failures are logged at
`ERROR` and tracked in `consecutiveFailures` on `getSweeperStatus()`.

**Configuration (env vars in `artifacts/api-server/.env`)**

| Env var | Default | Meaning |
|---|---|---|
| `INVITE_SLOT_SWEEP_TTL_HOURS` | `24` | Attempt rows older than this (with no member row) are cleaned up |
| `INVITE_SLOT_SWEEP_INTERVAL_HOURS` | `1` | How often the sweeper runs |

Lowering `INVITE_SLOT_SWEEP_TTL_HOURS` below the expected maximum request
latency (a few seconds) is unsafe — it would age out in-flight legitimate
requests.  The 24-hour default is deliberately generous.

**Manual trigger**

An on-demand reconcile is also available via:

```
POST /api/admin/trips/reconcile-invite-slots
{ "minAgeMinutes": 5 }   // optional; defaults to 5
```

This is the admin-only HTTP endpoint wired to the same Postgres function.

| `0108_rent_buddy_spec_tables.sql` | Adds three functional tables and four compatibility VIEW aliases. New tables: (1) `buddy_services` — typed service catalog per buddy (category, title, hourly/half-day/full-day rates, min/max hours, group size, approval flag); RLS: public read, owner write, service-role all. (2) `buddy_availability_exceptions` — structured per-date availability overrides with `buddy_exception_type` enum (`blocked`, `time_blocked`, `vacation`, `available_only`); RLS: public read, owner write, service-role all. (3) `buddy_booking_events` — immutable audit log of booking state transitions written by route handlers on accept/decline/start/complete/cancel; fields: `booking_id`, `actor_user_id`, `event`, `from_status`, `to_status`, `metadata JSONB`; RLS: parties to booking can read, service-role all. VIEW aliases: `buddy_booking_checkins → rent_buddy_safety_checkins`, `buddy_change_requests → rent_buddy_route_change_requests`, `buddy_favorites → rent_buddy_saved`, `buddy_booking_requests → rent_buddy_bookings`. **Apply:** paste `artifacts/api-server/migrations/0108_rent_buddy_spec_tables.sql` into the Supabase SQL editor and run. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name IN ('buddy_services','buddy_availability_exceptions','buddy_booking_events') ORDER BY table_name;` — all three names must appear. `SELECT table_name FROM information_schema.views WHERE table_name IN ('buddy_booking_checkins','buddy_change_requests','buddy_favorites','buddy_booking_requests');` — all four view names must appear. **Correction 2026-07-16:** this migration was never actually applied — its `ALTER TABLE ... ADD CONSTRAINT IF NOT EXISTS` is invalid PostgreSQL syntax and would have failed. A fixed version (guarded DO block) was applied as part of `0134_rent_buddy_schema_rebuild.sql`. | recorded 2026-07-05 but never applied — see 0134 |

| `0115_circle_visibility_settings.sql` | Non-canonical origin file for `circle_visibility_settings`. **Now tracked in canonical** `0108_circle_schema_tracked.sql` — apply that file instead. | tracked in 0108 |
| `0116_circle_context_settings.sql` | Non-canonical origin file for `circle_context_settings`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0117_beta_feature_flags.sql` | Seeds all beta-phase feature-gate and kill-switch flags into `feature_flags` using `ON CONFLICT (flag) DO NOTHING` so re-runs are safe and existing rows are never overwritten. Feature gates (enabled = true): `rent_buddy_enabled`, `safe_return_live_share_enabled`, `find_your_circle_enabled`, `compass_ai_enabled`, `hidden_gems_enabled`, `push_notifications_enabled`. Kill switches (enabled = false): `disable_signups`, `disable_posting`, `disable_messaging`, `disable_rent_buddy_booking` (blocks `POST /api/rent-a-buddy/bookings`), `city_launch_mode`, `invite_only_beta`. Kill-switch enforcement: `disable_posting` → `POST /posts` (posts.ts); `disable_messaging` → `POST /threads/:id/messages` (messaging.ts); `disable_rent_buddy_booking` → `POST /api/rent-a-buddy/bookings` (rentABuddy.ts); `disable_signups` + `invite_only_beta` → `GET /api/auth/signup-status` (auth.ts — mobile app must call this before Supabase Auth sign-up). All gates are fail-open. Ban enforcement: `requireUser` (lib/http.ts) now reads `profiles.account_status` and returns 403 for `banned` or `suspended` accounts; fail-open on DB error. Hide-content audit: `logModerationAction` now resolves the content owner's profile UUID first (falls back to admin UUID) so the FK to `profiles(id)` is always satisfied. Admin lookup: `GET /admin/users` now returns `onboardingStatus` from `compass_analytics`. Full kill-switch audit in `docs/beta-db-audit.md`. **Apply:** paste `artifacts/api-server/src/migrations/0117_beta_feature_flags.sql` into the Supabase SQL editor and run. **Verify:** `SELECT flag, enabled FROM feature_flags WHERE flag IN ('rent_buddy_enabled','disable_signups','disable_posting','disable_messaging','disable_rent_buddy_booking','invite_only_beta') ORDER BY flag;` — all 6 rows must appear. Applied via Supabase Management API; verify query returned all 6 flags (kill switches false, `rent_buddy_enabled` true). | 2026-07-16 |
| `0117_circle_presence.sql` | Non-canonical origin file for `circle_presence`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0118_circle_checkins.sql` | Non-canonical origin file for `circle_checkins`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0119_circle_member_visibility_overrides.sql` | Non-canonical origin file for `circle_member_visibility_overrides`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0120_circle_meeting_points.sql` | Non-canonical origin file for `circle_meeting_points`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0121_circle_audit_events.sql` | Non-canonical origin file for `circle_audit_events`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0123_engagement_user_indexes.sql` | Adds five user-perspective indexes for engagement like/reaction tables — complements the post-perspective indexes from migration 0106. `idx_posts_likes_user_created` on `posts_likes (user_id, created_at DESC)`, `idx_post_reactions_user_created` on `post_reactions (user_id, created_at DESC)`, `idx_comment_likes_user_created` on `comment_likes (user_id, created_at DESC)`, `idx_highlight_likes_user_created` on `highlight_likes (user_id, created_at DESC)`, `idx_memory_likes_user_created` on `memory_likes (user_id, created_at DESC)`. All idempotent (IF NOT EXISTS). Covers the reverse lookup "which posts/comments/highlights/memories has a given user liked?" — used by profile pages and the 'liked by me' feed indicator; without these indexes Postgres must sequentially scan each like table filtered by user_id. **Apply:** applied via the Supabase Management API query endpoint. **Verify:** `SELECT indexname FROM pg_indexes WHERE indexname IN ('idx_posts_likes_user_created','idx_post_reactions_user_created','idx_comment_likes_user_created','idx_highlight_likes_user_created','idx_memory_likes_user_created');` — all five names must appear (verified 2026-07-16). | 2026-07-16 |
| `0124_trip_crew_location_sessions_drift.sql` | Fixes schema drift on `trip_crew_location_sessions` that made the 5-minute live-share sweeper fail with PGRST204 in production. Adds `status` (TEXT NOT NULL DEFAULT 'active', CHECK active\|stopped\|expired), `started_at` (TIMESTAMPTZ NOT NULL DEFAULT now()), `stopped_at` (TIMESTAMPTZ), `visibility_level` (TEXT NOT NULL DEFAULT 'neighborhood', CHECK city_only\|neighborhood\|nearby). Backfills status from `ended_at`/`expires_at`, adds `crew_loc_sessions_status_idx (status, expires_at)`, and widens the `trip_crew_location_events.event_type` CHECK to include `live_share_started`, `live_share_stopped`, `live_share_expired`, `access_revoked` (the event names the service actually writes). All idempotent. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='trip_crew_location_sessions';` — status/started_at/stopped_at/visibility_level must appear. | applied 2026-07-16 |
| `0130_rent_buddy_place_coords.sql` | Adds `lat DOUBLE PRECISION` and `lng DOUBLE PRECISION` to `rent_buddy_waitlist`, and to `rent_buddy_requests` when it exists (`ALTER TABLE IF EXISTS` — that table comes from `0048_rent_buddy_marketplace.sql`, whose `CREATE TABLE` now also includes lat/lng). Lets open buddy requests and waitlist entries created from a canonical picked place keep coordinates for proximity-aware matching/notifications. Idempotent. **Verify:** `SELECT table_name, column_name FROM information_schema.columns WHERE table_name IN ('rent_buddy_waitlist','rent_buddy_requests') AND column_name IN ('lat','lng');` | applied 2026-07-16 (as part of the 0134 rebuild — the same lat/lng ALTERs are included in `0134_rent_buddy_schema_rebuild.sql`; verified 4 rows via information_schema) |

### Stale migration file — `artifacts/api-server/migrations/0041_notifications.sql`

This file is **not applied to production** and is superseded by `0062_notifications_schema.sql` (applied 2026-06-28). The 0062 file is the authoritative notification schema: it adds `push_retry_queue`, uses `public.profiles(id)` foreign keys, and has the correct RLS policy names. Do not apply `0041_notifications.sql`.

| `0125_canonical_locations.sql` | Creates `canonical_locations` — the universal location registry backing the app-wide location picker. One row per real-world location; provider variants ("Cebu" / "Cebu City" / Foursquare venue ids) merge via `normalized_name` + kind-class + proximity rules in `src/lib/canonicalLocations.ts`. Columns: `id uuid PK`, `name`, `normalized_name`, `kind`, `country`, `country_code`, `region`, `lat/lng`, `aliases text[]`, `provider_ids jsonb`, timestamps. Indexes on `normalized_name` and `(kind, country_code)`. RLS: public read, service-role write. Server degrades gracefully if missing (resolve returns null canonicalId). **Applied 2026-07-15 via Supabase Management API** (`POST /v1/projects/{ref}/database/query` — note: direct psql and pooler connections are unreachable from this workspace; the Management API with `SUPABASE_ACCESS_TOKEN` is the working path). **Verify:** `SELECT tablename, rowsecurity FROM pg_tables WHERE tablename='canonical_locations';` — one row, rowsecurity true. | applied 2026-07-15 |
| `0126_canonical_locations_unique.sql` | Unique expression index `uq_canonical_locations_identity` on `canonical_locations (normalized_name, kind-class CASE, COALESCE(country_code,''))` — closes the concurrent find-then-insert race that could create two canonical rows for one real place. The resolver catches `23505` and re-matches once. The CASE must stay in sync with `kindClass()` in `src/lib/canonicalLocations.ts`. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT indexname FROM pg_indexes WHERE indexname='uq_canonical_locations_identity';` — one row. | applied 2026-07-15 |
| `0124_stamp_definitions_universal_artwork.sql` | Adds nullable `universal_artwork_url text` to `stamp_definitions` — the AI-generated universal stamp artwork image URL rendered by the mobile passport stamp collection. Server code degrades gracefully: stamp routes probe for the column and omit it from selects until this migration is applied, and the `/me/stamps` artwork enrichment is best-effort. **Apply:** paste `artifacts/api-server/src/migrations/0124_stamp_definitions_universal_artwork.sql` into the Supabase SQL editor and run. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='stamp_definitions' AND column_name='universal_artwork_url';` — one row must appear. | applied 2026-07-16 |
| `0127_layover_system.sql` | Recreates the full layover backend that was never applied (original 0044 sat in the stale migrations dir): `airport_profiles` (+`canonical_location_id`), `layover_sessions` (+`canonical_city_id`, `share_city_status`, `return_reminder_at`), `layover_recommendations`, `layover_events` (extended event CHECK list), NEW `layover_plan_stops` (mini-itinerary, owner-only RLS via session join), share partial index, and feature-flag seeds using the correct `flag` column (all 5 airport/layover flags enabled). **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name LIKE $$layover| `0127_layover_system.sql` | Recreates the full layover backend that was never applied (original 0044 sat in the stale migrations dir): `airport_profiles` (+`canonical_location_id`), `layover_sessions` (+`canonical_city_id`, `share_city_status`, `return_reminder_at`), `layover_recommendations`, `layover_events` (extended event CHECK list), NEW `layover_plan_stops` (mini-itinerary, owner-only RLS via session join), share partial index, and feature-flag seeds using the correct `flag` column (all 5 airport/layover flags enabled). **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name LIKE 'layover%';` — 4 rows incl. `layover_plan_stops`. | applied 2026-07-15 |

| `0128_posts_canonical_location.sql` | Adds nullable `canonical_location_id uuid` (FK → `canonical_locations.id`, ON DELETE SET NULL) + partial index `idx_posts_canonical_location` on `posts` — the universal-registry reference written by the postcard composer's location picker. The postcards create route degrades gracefully (retries without the column on PGRST204) so unmigrated environments keep posting. Root-cause context: POST /api/postcards previously wrote nonexistent `event_id`/`place_id` columns → PGRST204 → every composer post failed with a raw db_error banner; provider place refs now map to `location_place_id`. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='posts' AND column_name='canonical_location_id';` — one row. | applied 2026-07-15 |
| `0129_post_media_stamp_overlay.sql` | Adds nullable `stamp_overlay` jsonb to `post_media` — optional decorative passport-stamp overlay metadata written at upload-complete (stamp definition id, artwork URL pinned server-side at publish, render style, normalized position/scale/rotation/opacity). Overlay is non-destructive (original photo untouched); complete never fails because of overlay problems (ineligible/unavailable stamps are skipped with a warning flag), and feed projections probe for the column so unmigrated environments degrade gracefully. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='post_media' AND column_name='stamp_overlay';` — one row. | applied 2026-07-15 |
| `0130_user_account_states_updated_at.sql` | Adds `updated_at timestamptz NOT NULL DEFAULT now()` to `user_account_states` — POST /api/me/deactivate and /me/reactivate upsert this column but migration 0063 never created it, so every deactivate 500'd (PGRST204). **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='user_account_states' AND column_name='updated_at';` — one row. | applied 2026-07-15 |
| `0131_location_mode_check_update.sql` | Widens `location_preferences_location_mode_check` from the legacy set ('precise','city','off') to the API vocabulary ('off','city_only','nearby','live_during_activity','trusted_circle_live') plus legacy values — every PATCH /api/me/location-preferences mode change violated the old CHECK and 500'd. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='location_preferences_location_mode_check';` — must list city_only/nearby/etc. | applied 2026-07-15 |
| `0132_passport_visibility_prefs_columns.sql` | Adds the six columns the visibility-preferences routes actually use (`default_stamp_visibility`, `default_memory_visibility`, `show_city_map`, `show_neighborhoods`, `show_plan_stamps`, `show_safe_return_stamps`) to `passport_visibility_preferences`, which migration 0042 created with only legacy columns (stamps_visible/memories_visible/map_visible) — every PATCH /api/me/passport/visibility-preferences 500'd with PGRST204. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='passport_visibility_preferences' AND column_name IN ('default_stamp_visibility','show_city_map');` — two rows. | applied 2026-07-15 |
| `0133_rent_buddy_availability_alignment.sql` | Fixes buddy vacation/blocked-date persistence end-to-end. (1) Recreates `rent_buddy_availability` in the per-date-slots shape the dashboard routes use (live table kept a pre-0047 legacy shape — `date_from`/`date_to`/`is_blocked` — so 0047's CREATE TABLE IF NOT EXISTS silently no-oped; legacy table verified empty before drop, guarded with a RAISE EXCEPTION otherwise). (2) Creates `buddy_availability_exceptions` (+`buddy_exception_type` enum, RLS, UNIQUE(buddy_id,exception_date), range index) — the table `POST /api/rent-a-buddy/bookings` consults to reject bookings on blocked/vacation dates; carried over from unapplied 0108_rent_buddy_spec_tables.sql minus its invalid `ADD CONSTRAINT IF NOT EXISTS` statement. (3) Adds `available_now`, `available_now_until`, `min_notice_hours`, `buffer_minutes`, `max_bookings_per_day` to `rent_buddy_profiles`, backing the new `PATCH /api/rent-a-buddy/dashboard/availability/settings` route. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_availability' AND column_name='date';` — one row; `SELECT 1 FROM information_schema.tables WHERE table_name='buddy_availability_exceptions';` — one row. | applied 2026-07-15 |
| `0134_rent_buddy_schema_rebuild.sql` | Full rent_buddy schema rebuild fixing the drifted live DB. The live `rent_buddy_*` tables kept a pre-0047 legacy shape (`rent_buddy_profiles` had 14 old columns — no `status`/`admin_status`/`display_name`/`city`/`categories`/`hourly_rate_usd`; `rent_buddy_bookings` used `date_from`/`date_to`), so buddy sign-up, search, and bookings all failed against production. All drifted tables verified empty (a run-time RAISE EXCEPTION guard re-verifies), then dropped with CASCADE and rebuilt from the canonical chain: 0047 → 0048_rollout → 0048_marketplace → 0051 → 0107 → 0108 (with its invalid `ADD CONSTRAINT IF NOT EXISTS` replaced by a guarded DO block) → 0109 → 0110 → 0111 → 0112 → 0113 → 0133. Kept (had data): `rent_buddy_city_rollouts` (+ added missing `country`, `status_changed_at`, `status_changed_by`, `target_launch_date`, `buddy_cap`), `rent_buddy_fee_rules`, `rent_buddy_global_controls` (+ added `updated_by_admin_id`); their RLS policies are dropped/recreated. Trailing route-alignment ALTERs add columns routes select but no prior migration created: `rent_buddy_profiles.completed_count/cancel_count/no_show_count/favorites_count/expo_push_token`, and realign `rent_buddy_admin_actions` to the 0107 shape (nullable `admin_id`, TEXT `target_id`, `details JSONB`). **Applied 2026-07-16 via Supabase Management API, one PART per request (0112 split so `ALTER TYPE ... ADD VALUE` commits before the new value is used).** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_profiles' AND column_name IN ('status','admin_status','display_name','city','categories','hourly_rate_usd','favorites_count');` — 7 rows; `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_bookings' AND column_name IN ('booking_date','start_time','duration_h');` — 3 rows. E2E verified: buddy profile insert, `BUDDY_PUBLIC_COLUMNS` select, and booking insert all succeed via PostgREST. | applied 2026-07-16 |
| `0135_rent_buddy_meetup_base_coords.sql` | Adds nullable `meetup_base_lat`/`meetup_base_lng` (DOUBLE PRECISION, range CHECKs) to `rent_buddy_profiles` — a buddy-chosen, neighbourhood-level meetup-base pin used by `POST /api/rent-a-buddy/search` for per-buddy distance chips (falls back to city-centre geocoding when unset). Set via `PATCH /api/rent-a-buddy/me/profile` (`meetupBaseLat`/`meetupBaseLng`, both-or-neither, null clears). **Note:** the column is in the buddy search select list, so environments missing it fail search entirely (probe added to schemaDriftCheck). **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_profiles' AND column_name LIKE 'meetup_base%';` — two rows. | applied 2026-07-16 |
| `0136_stamp_queue_requeue_cap.sql` | Adds `requeue_count int NOT NULL DEFAULT 0` to `stamp_generation_queue`, extends the status CHECK with terminal `permanently_failed`, and recreates `uix_queue_catalog_active` so permanently_failed rows don't count as "active" (otherwise admin regenerate could never enqueue a new job). Worker auto-requeue (`requeueStaleFailedJobs`) increments `requeue_count` each round and moves jobs at the cap (`STAMP_FAILED_REQUEUE_MAX_ROUNDS`, default 3) to `permanently_failed`; manual admin re-queue resets both `attempts` and `requeue_count` to 0. **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='stamp_generation_queue' AND column_name='requeue_count';` — one row. | applied 2026-07-16 |
| `0138_city_country_geocode_cache.sql` | Creates `city_country_geocode_cache` (city_key TEXT PK — normalised city name, country, country_code with `^[A-Z]{2}# Applied Migrations

All migrations have been applied to production Supabase unless noted otherwise.

> **Warning — "applied" claims in this file are not authoritative.** Always verify against the live database (`information_schema.tables` / `information_schema.columns`) before relying on a row here. Example: `0108_rent_buddy_spec_tables.sql` was recorded as applied on 2026-07-05 but was never actually run (it contains invalid `ADD CONSTRAINT IF NOT EXISTS` syntax and would have failed), and the whole `0047`–`0113` rent_buddy chain was found unapplied on 2026-07-16 despite being listed here — the live tables still had a pre-0047 legacy shape. See `0134_rent_buddy_schema_rebuild.sql`.

> **Contributing reminder:** When you apply a new migration to Supabase, immediately append a row to this table before merging. Include the filename, a plain-English description of every table/column/index/policy created or altered, and the date applied. Skipping this step is the primary cause of log drift.

| Migration | Description | Applied |
|-----------|-------------|---------|
| `0102_universal_stamp_catalog.sql` (supabase/migrations) | Creates five tables: `universal_stamp_catalog` (canonical stamp entries, unique on (canonical_location_key, stamp_type), status pending_artwork/approved/rejected/archived), `stamp_artwork_versions` (AI/admin artwork candidates, FK to catalog, deferred FK back via `fk_catalog_active_version`), `stamp_generation_queue` (AI artwork job queue with locking; one active job per catalog entry via partial unique index), `stamp_admin_audit_log` (append-only admin actions), `stamp_reconciliation_log` (links existing stamps to catalog entries). Adds nullable `catalog_id uuid → universal_stamp_catalog(id)` to both `passport_stamps` and `user_stamps` with indexes. RLS on all five tables: service_role full access; authenticated users can SELECT approved catalog entries and approved artwork versions. Also creates `stamp-artwork` storage bucket (public) with public-read and service-role-write policies. Applied via Supabase Management API; reconciliation script (`artifacts/api-server/src/scripts/reconcileStampCatalog.ts`) run twice (idempotent): 3 catalog entries created, 9/10 user_stamps linked (1 row has no country/city — unresolvable), 3 generation jobs queued. | 2026-07-16 |
| `0120_passport_section_order.sql` | Adds `passport_section_order TEXT[]` (nullable) to `profiles` — owner's preferred passport section order (`identity`, `stamps`, `highlights`, `tabs`, `dossier`); NULL = canonical order. Read/written by GET/PATCH `/api/me/profile`; visitor/public views never read it. | 2026-07-15 |
| `0119_toggle_flag_atomic.sql` | Creates `toggle_feature_flag_with_audit(p_flag, p_new_enabled, p_changed_by_id)` SECURITY DEFINER plpgsql function; performs UPDATE feature_flags + INSERT feature_flag_audit_log in one transaction with row-level locking (FOR UPDATE); raises P0002 if the flag doesn't exist; called by PATCH /admin/feature-flags/:flag so the toggle and audit row are always atomic. | 2026-07-07 |
| `0118_feature_flag_audit_log.sql` | Creates `feature_flag_audit_log` table (`id BIGSERIAL PK`, `flag TEXT → feature_flags(flag) ON DELETE CASCADE`, `changed_by_user_id UUID → auth.users(id) ON DELETE SET NULL`, `old_enabled BOOLEAN`, `new_enabled BOOLEAN`, `changed_at TIMESTAMPTZ DEFAULT NOW()`); indexes on `(flag, changed_at DESC)` and `changed_by_user_id`; RLS enabled with no public policies (service-role only). | 2026-07-06 |
| `0114_add_max_members_to_trips.sql` | Adds nullable `max_members INTEGER` column to `trips`. NULL means no member cap. When set, the invite-link accept handler returns 410 gone before claiming a slot if the accepted-member count has reached the limit. | 2026-07-05 |
| `0010_trip_plan.sql` | Creates `trip_plan_items` table with RLS policies (select/insert/update for trip members/owners) and a partial unique index on `(trip_id, source_type, source_id)` | 2026-06-21 |
| `0011_message_type.sql` | Adds `msg_type` (default `'text'`) and `subtype` columns to `messages`; partial index on `subtype` | 2026-06-21 |
| `0012_daily_briefs.sql` | Creates `daily_briefs` table with per-user/per-day uniqueness constraint and RLS (users read own rows only); service role handles all writes | 2026-06-21 |
| `0013_daily_briefs_cleanup.sql` | `daily_briefs_brief_date_idx` index on `daily_briefs(brief_date)` — keeps the nightly purge DELETE fast as the table grows | 2026-06-21 |
| `0014_profile_about_me.sql` | Adds `spoken_languages`, `default_language`, `travel_styles`, `travel_pace`, `budget_style`, `travel_group_style`, `looking_for`, `comfort_level`, `availability_tags`, `planning_style`, `public_social_links` to `profiles` | 2026-06-22 |
| `0015_blocks.sql` | Creates `blocks` table (blocker_id, blocked_id, unique constraint, RLS) and `is_blocked(a, b)` SECURITY DEFINER helper function | 2026-06-22 |
| `0016_thread_reads.sql` | Adds `last_read_at timestamptz` to `message_thread_members`; index for unread-count queries | 2026-06-22 |
| `0017_job_health.sql` | Creates `job_health` table (primary key: `job text`) to persist background-job last-run timestamps; used by `GET /api/healthz/cleanup` to report `cleanupHealthy` across server restarts | 2026-06-22 |
| `0018_preferred_language.sql` | Adds `preferred_language` (nullable text, BCP-47) to `profiles`; overrides `preferred_message_language` in the translation pipeline when set | 2026-06-22 |
| `0019_proposed_time.sql` | Adds `proposed_time TIME` (nullable) to `meetup_time_options` — stores exact HH:MM per time-poll slot alongside the coarse time_block | 2026-06-22 |
| `0020_notifications_inbox_viewed.sql` | Adds `notifications_inbox_viewed_at timestamptz` to `profiles`; used by `GET /api/me/unread-counts` to compute unread notification badge count | 2026-06-21 |
| `0021_plan_edit_permission.sql` | Adds `plan_edit_permission` column to `trips` (enum: `owner_only \| all_members \| specific_members`, default `all_members`) and creates `plan_editors` join table with RLS; used by plan permission system | 2026-06-22 |
| `0022_availability_nudges.sql` | Creates `availability_nudges` table (sender_id, recipient_id, trip_id, nudge_date, sent_on); UNIQUE(recipient_id,trip_id,sent_on) rate-limits to one nudge per recipient per trip per day; RLS: recipients read own rows | 2026-06-22 |
| `0023_push_tokens.sql` | Adds `expo_push_token TEXT` to `profiles`; stores Expo push token per device so the API server can send push notifications | 2026-06-22 |
| `0024_post_engagement.sql` | Creates `posts_likes` (post_id+user_id unique, RLS) and `posts_comments` (body, soft-delete, RLS) tables; adds `like_count`, `comment_count`, `share_count` integer columns (DEFAULT 0) to `posts` | 2026-06-22 |
| `0025_location_system.sql` | Creates `user_location_state` (per-user GPS/manual-city upsert, unique on user_id, RLS) and `passport_stamps_gps` (GPS-earned stamp events, unique on user+type+country+city, RLS) tables; adds `location_source`, `stamp_city/country/label/unlocked_at` columns to `postcards` if it exists | 2026-06-22 |
| `0026_highlights.sql` | Creates `highlights`, `highlight_views`, `highlight_likes`, `highlight_replies`, `highlight_reports` tables with RLS; visibility enum (public/travelers_nearby/circle_only/trip_only/private); 24h default expiry, soft-delete, view/like/reply/report engagement | 2026-06-22 |
| `0028_highlights_last_viewed.sql` | Adds `highlights_last_viewed_at timestamptz` to `profiles`; updated by `POST /api/me/highlights/mark-viewed`; used by `GET /api/me/unread-counts` to compute `newHighlights` badge count | 2026-06-26 |
| `0029_discovery_places.sql` | Creates `discovery_places` table (id, city, name, place_type, category, neighborhood, blurb, image_url, submitted_by→profiles, saved_count, tag, note, rating, source, status, verified, created_at); RLS (public read, auth insert/update/delete own); indexes on city, place_type, created_at; used by `GET /api/discovery/community` | 2026-06-26 |
| `0032_location_preferences.sql` | Creates `location_preferences` table (user_id PK→profiles, location_mode enum, sharing_paused, pulse/discovery visibility enums, safe_return_enabled, trusted_circle_share, hotel_blur_enabled, updated_at); RLS (users manage own row) | 2026-06-26 |
| `0033_location_sessions.sql` | Creates `location_sessions` table (id, user_id, session_type enum, started_at, ended_at, resolved_city/country, trip_id, plan_item_id, metadata); RLS (users manage own rows, service-role all) | 2026-06-26 |
| `0034_geo_zones.sql` | Creates `geo_zones` table (id, name, zone_type enum, center_lat/lng, radius_meters, polygon_geojson, country_code, city, created_by→profiles, is_system, metadata); RLS (public read, auth create own, service-role all) | 2026-06-26 |
| `0035_plan_geofences.sql` | Creates `plan_geofences` table (id, trip_id→trips, plan_item_id→trip_plan_items, zone_id→geo_zones, trigger_type enum, notify_members bool, message_template, last_triggered_at); RLS (trip members read/manage) | 2026-06-26 |
| `0036_pulse_geo_tags.sql` | Creates `pulse_geo_tags` table (id, post_id→posts, geo_zone_id→geo_zones, tag_type enum, display_label, confidence_score, source enum, created_at); RLS (public read); used by Pulse location context display | 2026-06-26 |
| `0037_feature_flags.sql` | Creates `feature_flags` table (flag text PK, enabled bool, description, updated_at); seed rows for location intelligence phases 1–6; service-role manages flags | 2026-06-26 |
| `0039_plan_geofence_full.sql` | Expands `plan_geofences` (public_preview_level, exact_visibility, check_in_required, window start/end, arrival_status_visible, no_show_affects_reliability, location_name, city, neighborhood, venue_name, host_revealed, UNIQUE trip_id); adds `plan_checkins` (per-member attendance status with upsert), `plan_attendance_events` (audit log: suspicious/late/override events), `geofence_admin_settings` (single-row id=1 default/min/max radius config) | 2026-06-26 |
| `0041_trip_crew_location.sql` | Creates `trip_crew_location_preferences` (per-user/per-trip ghost mode + visibility defaults, RLS), `trip_crew_location_sessions` (timed live-share sessions with allowed_member_ids, expires_at, RLS), `trip_crew_location_events` (audit log for ghost mode on/off and live share events, RLS); feature flag seeds for `trip_crew_map_enabled`, `trip_crew_live_share_enabled`, `trip_crew_ghost_mode_enabled` | 2026-06-26 |
| `0042_passport_stamps.sql` | Creates `passport_stamps` (dedup unique index on user/stamp_type/country/city, NO lat/lng stored), `passport_memories` (suggested→active→dismissed lifecycle), `passport_contribution_events` (append-only, no Trust Score modification), `passport_visibility_preferences`; feature flag seeds for passport_stamps/memories/map/contribution | 2026-06-26 |
| `0043_tags_hashtags.sql` | Creates `tags` (per-@mention dedup on source_type+source_id+tagged_user_id), `hashtags` (canonical slug registry with block/trending flags), `hashtag_usage` (dedup per hashtag+source), `user_hashtag_follows`; adds `tag_permission` enum (`anyone\|interacted\|friends_only\|nobody`, default `anyone`) to `profiles`; `increment_hashtag_usage_count(uuid)` SECURITY DEFINER helper | 2026-06-26 |
| `0044_hashtag_reports.sql` | Creates `hashtag_reports` table (hashtag_id, reporter_id, reason enum: spam/misleading/abusive, created_at); RLS: auth users insert own rows, service role reads all; indexes on hashtag_id and reporter_id | 2026-06-26 |
| `0046_tag_suppression.sql` | Adds `suppressed BOOLEAN NOT NULL DEFAULT FALSE` + `suppressed_at TIMESTAMPTZ NULL` to `tags`; partial indexes for active-by-source and suppressed-by-user lookups; used by soft-delete self-tag removal flow | 2026-06-26 |
| `0047_rent_buddy.sql` | Creates all `rent_buddy_*` tables: profiles, applications, availability, packages, addons, saved, waitlist, bookings, booking_extensions, route_stops, route_change_requests, safety_checkins, safety_events, user_limits, emergency_contacts_snapshot, reviews, disputes, policy_flags, admin_actions; seeds `rent_buddy_enabled` feature flag. **Correction 2026-07-16:** despite the applied date, the live DB kept a pre-0047 legacy shape (14-column profiles, `date_from`/`date_to` bookings); actually applied as part of `0134_rent_buddy_schema_rebuild.sql`. | 2026-06-26 (not actually applied — see 0134) |
| `0048_booking_stay_connected.sql` | Adds `stay_connected_traveler BOOLEAN NOT NULL DEFAULT FALSE` and `stay_connected_buddy BOOLEAN NOT NULL DEFAULT FALSE` to `rent_buddy_bookings`; replaces ephemeral in-memory Map so stay-connected opt-ins survive server restarts and work across multiple API instances | 2026-06-26 |
| `0049_delayed_geotag_posts.sql` | Adds `location_privacy_mode` enum, `delayed_post_status` enum, `location_sensitivity_level` enum; new columns to `posts` (`post_status`, `location_privacy_mode`, `original_lat/lng`, `public_lat/lng`, `public_location_label`, `venue_name/id`, `geofence_radius_meters`, `publish_after_exit`, `publish_after_time`, `publish_eligible_at`, `exited_geofence_at`, `published_at`, `geotag_credit_awarded`, `location_sensitivity_level`, `user_gps_lat/lng`); creates `delayed_post_location_events` table (audit log: exit_detected/manually_released/geotag_credit_awarded/privacy_changed/publish_skipped/safe_return_hold/canceled) with RLS | 2026-06-26 |
| `0050_suggestion_seen.sql` | Creates `user_suggestion_seen` table (user_id PK→profiles, seen_ids text[], expires_at, updated_at); RLS enabled (service role only — no user-facing policies); `user_suggestion_seen_expires_idx` on expires_at for fast nightly purge; backs the hybrid L1/L2 suggestion-seen cache so the 7-day freshness window survives server restarts | 2026-06-26 |
| `0051_trip_members_user_role_idx.sql` | `trip_members_user_role_idx` composite index on `trip_members(user_id, role)` — lets Postgres seek directly to a user's trip memberships instead of seq-scanning the full table; hot path for the shared-destination caller query in GET /users/search and GET /users/suggestions | 2026-06-26 |
| `0090_rent_buddy_rollout_tables.sql` | Captures rollout tables previously applied inline. Creates `rent_buddy_city_rollouts` (UUID id PK, city UNIQUE, `rent_buddy_city_status` enum `disabled/waitlist_only/buddy_applications_open/internal_testing/beta_testing/public_mvp/paused/suspended`, RLS with `rb_rollout_public_read` + `rb_rollout_svc` policies); `rent_buddy_beta_access` (user+city UNIQUE, `rb_beta_own_read` + `rb_beta_svc`); `rent_buddy_launch_checklists` (9 QA booleans, UNIQUE city_rollout_id); `rent_buddy_launch_audit_logs` (service-role append-only); `rent_buddy_global_controls` (INT PK=1, `all_bookings_paused`, `applications_paused`, `cash_balance_paused`, `nightlife_paused`, `force_full_in_app`, `force_public_meetup`, `force_delayed_posting`, `updated_by_admin_id`, `rb_global_svc` RLS); forces `rent_buddy_enabled = TRUE` (ON CONFLICT DO UPDATE) and inserts 9 MVP feature flags (ON CONFLICT DO NOTHING); all statements idempotent | applied |
| `0062_notifications_schema.sql` | Creates full push notification pipeline schema: `notifications` (in-app Activity Centre rows), `notification_devices` (Expo push tokens per device, UNIQUE user+token), `notification_preferences` (global per-user delivery toggles), `notification_category_preferences` (per-category overrides), `notification_delivery_attempts` (audit log for every dispatch, service-role only), `push_retry_queue` (transient-failure retry with exponential backoff, service-role only), `activity_events` (raw event log); seeds feature flags `notifications_enabled`, `push_notifications_enabled`, `realtime_activity_enabled`, `safety_notifications_enabled` (all enabled), `notification_digests_enabled` (disabled). RLS on all 7 tables — service role writes, users read own rows. All `user_id` columns reference `public.profiles(id)`. All `CREATE POLICY` statements are idempotent (`DO $$` guards). | 2026-06-28 |
| `0063_push_retry_queue.sql` | Supplemental indexes for `push_retry_queue`: `prq_status_next_retry_idx` (partial, `status='queued'` only — tighter than the 0062 index for worker tick queries) and `prq_user_idx` (per-user lookups). No table redefinition — purely additive. Applied as part of combined 0062+0063 production run. | 2026-06-28 |
| `0043_hidden_gems.sql` | Creates Hidden Gems & Local Guide system: `hidden_gems` (name, category, city, exact/approx coords, sensitivity enum, verification enum, status enum, save/visit/report counts, merged_into self-ref; RLS: public read for active+public gems, owner read/insert/update), `hidden_gem_verifications` (gps_proximity/community/guide/admin methods, unique per gem+user+method; owner read/insert), `hidden_gem_reports` (reason enum, unique per gem+reporter; owner read/insert), `hidden_gem_saves` (gem+user PK; owner CRUD), `hidden_gem_visits` (GPS distance, trust_level, is_suspicious; owner read/insert), `local_guide_profiles` (guide_level 0–5, city_expertise, accuracy_score, status; public read for active guides), `local_guide_contributions` (contribution_type enum, helpful_votes; owner read/insert); indexes on city/category/status/layover_safe; seeds 7 feature flags (`hidden_gems_enabled`, `hidden_gem_verification_enabled`, `local_guides_enabled`, `hidden_gems_compass_enabled`, `hidden_gems_passport_enabled`, `hidden_gems_layover_enabled`, `hidden_gems_pulse_enabled`) | 2026-06-29 |
| `0044_tags_hashtags.sql` | Creates tags/hashtag system (supersedes the inline 0043 reference): `tags` (source_type+source_id+tagged_user_id dedup unique index, tagger/tagged indexes; RLS: own rows only), `hashtags` (canonical slug registry, normalized_name, usage_count, is_blocked/is_hidden_from_trending; public read, service-role write), `hashtag_usage` (dedup per hashtag+source_type+source_id, author_id, city/country; public read, service-role write), `user_hashtag_follows` (user+hashtag PK; own CRUD + service-role all); adds `tag_permission` enum (`anyone\|interacted\|friends_only\|nobody`, default `anyone`) to `profiles`; `increment_hashtag_usage_count(uuid)` SECURITY DEFINER helper; `upsert_hashtag_usage_and_increment(...)` atomic upsert helper that prevents double-counting under concurrent writes | 2026-06-29 |
| `0045_dob_profiles.sql` | Adds `date_of_birth date` (nullable) and `dob_verified boolean NOT NULL DEFAULT false` to `profiles`; columns are excluded from public SELECT queries so other users never see DOB | 2026-06-29 |
| `0046_meetup_age_limits.sql` | Adds `age_limit_enabled boolean NOT NULL DEFAULT false`, `min_age integer`, `max_age integer` to `meetups`; existing rows default to no age limit with unchanged behaviour | 2026-06-29 |
| `0047_circle_age_settings.sql` | Creates `circle_age_settings` (owner_id PK→profiles, age_limit_enabled, min_age, max_age, updated_at); RLS: owner reads and writes own row only | 2026-06-29 |
| `0048_age_audit_log.sql` | Creates `age_limit_audit_log` append-only table (actor_user_id, target_type enum `meetup\|circle\|user`, target_id, action, old/new min/max age, reason, metadata jsonb); service role writes only; actors can read own rows via policy | 2026-06-29 |
| `0049_discovery_places_age.sql` | Adds `min_age integer` and `max_age integer` (both nullable, 0–120 check) to `discovery_places`; enables server-side age filtering on `GET /api/discovery/community` | 2026-06-29 |
| `0050_rent_a_buddy.sql` | Creates core Rent a Buddy marketplace tables: `buddy_profiles` (display_name, tagline, bio, languages, city, categories, hourly_rate_usd, status enum, verified, average_rating, review_count, response_time_h, gallery_urls; unique user_id; RLS); plus booking, availability, review, and supporting tables; seeds `rent_buddy_enabled` feature flag | 2026-06-29 |
| `0051_compass_foundation.sql` | Creates Compass AI intelligence foundation: `compass_user_profiles` (per-user snapshot: preferred_cities/languages, budget_style, travel_styles, social_style, safety/visibility preferences, block counts, trust_score/level, active_user_score, trip/booking/48h flags, current_city; service-role writes), `compass_user_preferences` (per-user settings: compass_enabled, intent_mode_override, show_explanations, budget_filter, min_trust_level, exclude_budget_styles; user reads/writes own), `compass_user_context_snapshots` (point-in-time context snapshots for pipeline audits), `compass_intent_modes` (user-defined intent sessions); seeds 9 Compass feature flags | 2026-06-29 |
| `0052_compass_pipeline_logs.sql` | Creates Compass pipeline audit/log tables: `compass_safety_filter_logs` (items blocked by Safety Filter per viewer, item_type, block_reason; service-role write, viewer reads own), `compass_eligibility_logs` (eligibility decisions per viewer+item+stage), `compass_privacy_guard_logs` (privacy suppression events), `compass_recommendation_scores` (per-user/per-item score snapshots with decay_factor, boost_factor, final_score); indexes on viewer+created_at and item_id for each table | 2026-06-29 |
| `0053_compass_feed_intelligence.sql` | Creates Compass Phase 3 Feed Intelligence tables: `compass_active_user_scores` (per-user 24h/7d/30d/90d rolling scores + tier; PK on user_id), `compass_active_user_events` (append-only activity events used in scoring), `compass_active_user_badges` (badge eligibility per user), `compass_city_reputation` (city-scoped reputation per user), `compass_category_reputation` (category-scoped reputation per user), `compass_visibility_boosts` (fair-exposure appearance tracking), `compass_visibility_cooldowns` (cooldown records when appearance cap is hit); all tables with RLS, service-role writes | 2026-06-29 |
| `0054_compass_cache.sql` | Creates Compass Phase 4 Cache & Front Load tables: `compass_feed_cache` (cached feed payloads per user with per-type TTLs and expires_at), `compass_preload_queue` (items queued for background preloading), `compass_preload_events` (client navigation events), `compass_cache_invalidations` (audit log for every cache invalidation), `compass_user_navigation_patterns` (aggregated nav patterns for manifest ranking), `compass_content_freshness` (per-item staleness tracking), `compass_media_preload_manifest` (media URLs for client prefetch), `compass_frontload_rules` (configurable tier-assignment rules); indexes on expires_at, user_id, entry_type | 2026-06-29 |
| `0055_compass_ux.sql` | Creates Compass Phase 5 Intelligence UX tables: `compass_feedback_events` (append-only user feedback actions on recommendations), `compass_explanation_reasons` (admin-editable override explanations), `compass_notification_decisions` (audit log for notification send/suppress decisions), `compass_abuse_flags` (patterns detected by CompassAbuseDefenseEngine), `compass_served_recommendations` (per-user recommendation records used by the /why endpoint); all tables with RLS, service-role writes, users read own rows | 2026-06-29 |
| `0056_compass_user_prefs_v2.sql` | Extends `compass_user_preferences` (created in 0051) with Phase 5 feedback-driven personalisation columns: `category_weights jsonb NOT NULL DEFAULT '{}'`, `ignored_item_ids text[] NOT NULL DEFAULT '{}'`, `muted_hashtags text[] NOT NULL DEFAULT '{}'`, `muted_topics text[] NOT NULL DEFAULT '{}'`, `public_meetups_only boolean NOT NULL DEFAULT false`; required by `processFeedback` upserts and notification quiet-hours parsing | 2026-06-29 |
| `0057_reply_to_messages.sql` | Adds `reply_to_id uuid REFERENCES messages(id) ON DELETE SET NULL` to `messages` (partial index on non-null rows); creates `saved_messages` table (user_id+message_id unique PK, saved_at; RLS: users read/insert/delete own rows; index on user_id+saved_at DESC) | 2026-06-29 |
| `0058_trip_flow.sql` | Creates route planning system: `route_style` enum (`nightlife\|scenic\|foodie\|low_walking\|custom`), `route_plan_status` enum (`draft\|active\|completed\|cancelled`); `route_plans` (owner_user_id, trip_id, circle_id, title, start/end_location jsonb, route_style, status, compass_explanation, is_approximated; RLS: owner read/write, trip members read); `route_stops` (route_plan_id, sequence_order, place details, checkpoint_status, ETA/ATA; trip members read, owner write); `route_legs` (stop-to-stop transit metadata: mode, distance_m, duration_s, geometry_geojson; RLS follows route stops) | 2026-06-29 |
| `0059_route_plan_members.sql` | Creates `route_plan_members` (route_plan_id, user_id, joined_at; UNIQUE per plan+user); RLS: own membership read/insert/delete; trip members can see all memberships on a trip-linked plan; index on route_plan_id for fast member lookups | 2026-06-29 |
| `0060_discovery_places_coords.sql` | Adds `lat DOUBLE PRECISION` and `lng DOUBLE PRECISION` (both nullable) to `discovery_places`; partial index `discovery_places_has_coords_idx` on `(city, place_type) WHERE lat IS NOT NULL AND lng IS NOT NULL` for fast map-view queries; enables community-submitted places to appear on DiscoveryMapView alongside OSM results | 2026-06-29 |
| `0061_discovery_place_reports.sql` | Creates `discovery_place_reports` (place_id→discovery_places, reporter_id→profiles, reason, notes; UNIQUE per place+reporter); RLS: auth users insert own reports and read own reports; indexes on place_id (for most-reported aggregation) and reporter_id | 2026-06-29 |
| `0063_interaction_foundation.sql` | Creates 8 Phase 4 User Interaction System tables: `user_interaction_cooldowns` (anti-retaliation gate, UNIQUE user+target+type, RLS users read own), `user_mutes` (global user mutes with mute_types[], UNIQUE muter+muted, RLS users manage own), `user_restrictions` (restrictor hides read-receipts/online status from restricted user, UNIQUE restrictor+restricted, RLS users manage own), `reports` (unified cross-domain report table with target_type/reason_code/severity/status, indexes on reporter/target/status, RLS reporters read own), `user_saves` (private profile bookmark, UNIQUE saver+saved, RLS users manage own), `moderation_actions` (admin/moderator action log, RLS enabled), `user_account_states` (suspended/banned/limited state, UNIQUE user+state, RLS enabled), `user_privacy_settings` (per-user privacy config: profile_visibility, who_can_tag, age_restriction, location_sharing, online_status; UNIQUE user, RLS users manage own). All statements use IF NOT EXISTS; policies use DROP IF EXISTS + CREATE. Required by POST /api/users/:id/mute, POST /api/users/:id/save, POST /api/reports. | 2026-06-30 |
| `0064_tags_approval.sql` | Adds `status text NOT NULL DEFAULT 'approved'` to `tags` table; creates partial index `tags_status_idx` on `tags(status) WHERE status <> 'approved'` for efficient pending/rejected tag lookups. Supports the `approval_required` tag_permission setting added in Phase 4. | 2026-06-30 |
| `0065_events.sql` | Creates full Events system: 4 enum types (`event_state`, `event_rsvp_status`, `event_role_type`, `event_visibility`); 8 tables: `events` (host_id, title, location, dates, cover_url, capacity/age/trust gates, visibility, state, chat/waitlist flags, price, rsvp_options, going_count, waitlist_count, city/country; 6 indexes; RLS), `event_rsvps` (event+user PK, status enum; 2 indexes; RLS), `event_waitlist` (event+user PK, position, offer_expires_at, UNIQUE event+position; 2 indexes; RLS), `event_roles` (event+user PK, role enum; 2 indexes; RLS), `event_attendee_states` (checked_in_at, confirmed_at, no_show_at, confirmed_by, no_show_by; 2 indexes; RLS), `event_join_requests` (status pending/approved/denied, message, reviewed_by/at, UNIQUE event+user; 2 indexes; RLS), `event_updates` (author_id, body, pinned; 1 index; RLS), `event_reviews` (reviewer_id, rating 1–5, body, anonymous, UNIQUE event+reviewer; 2 indexes; RLS). Adds `avg_rating NUMERIC(3,1)` and `review_count INTEGER DEFAULT 0` to `events`. Seeds 4 feature flags: `events_enabled`, `events_waitlist_enabled`, `events_chat_enabled`, `events_trust_gates_enabled` (all enabled). | 2026-06-30 |
| `0065_phase7_safety.sql` | Creates `report_evidence` (id, report_id→reports ON DELETE RESTRICT, evidence_type, content_ref, metadata jsonb, created_at; `idx_report_evidence_report` index; RLS enabled; policy "Reporters can read their own evidence" via reports.reporter_id = auth.uid()); adds `metadata jsonb` column to `feature_flags` (for parameterized emergency flags storing target IDs); seeds 11 emergency kill-switch flags (all `enabled: false`, ON CONFLICT DO NOTHING): `disable_unknown_message_requests`, `disable_new_event_creation`, `disable_rab_bookings`, `disable_tagging`, `disable_location_sharing`, `disable_profile_search`, `disable_media_uploads`, `freeze_city`, `freeze_event`, `freeze_circle`, `freeze_booking`. | 2026-06-30 |
| `0066_post_interaction_layer.sql` | Creates `post_reactions` (post_id+user_id UNIQUE, emoji, CASCADE from posts+auth.users; index on post_id), `comment_likes` (comment_id+user_id UNIQUE, CASCADE from posts_comments+auth.users; index on comment_id), `post_shares` (post_id+user_id+target UNIQUE, CASCADE from posts+auth.users; index on post_id), `post_edits` (edit history: post_id, user_id, old_content, new_content, edited_at; index on post_id). Alters `posts`: adds `comments_setting TEXT NOT NULL DEFAULT 'everyone'` + CHECK constraint (`everyone\|friends\|circle\|trip_crew\|verified\|disabled`), `likes_hidden BOOLEAN NOT NULL DEFAULT FALSE`, `sharing_disabled BOOLEAN NOT NULL DEFAULT FALSE`, `reposting_disabled BOOLEAN NOT NULL DEFAULT FALSE`. Alters `posts_comments`: adds `parent_comment_id UUID` self-referential FK (CASCADE) + partial index `idx_posts_comments_parent` for non-null rows (threaded replies). All statements use IF NOT EXISTS / DROP CONSTRAINT IF EXISTS (idempotent). | 2026-06-30 |
| `0067_memories.sql` | Creates the full Memory System: `memories` (id, owner_id→auth.users, title, caption, visibility enum 6 values, allowed/hidden_user_ids uuid[], trip_id→trips, event_id→events, place_id, starts_at/ends_at, state enum 5 values; RLS: owner all, public read for published+public; indexes on owner, trip, event, state+visibility); `memory_items` (media_url, media_type, caption, position; RLS via memory ownership; index on memory_id+position); `memory_tags` (memory_id+tagged_user_id PK, status enum pending/approved/removed; RLS: owner/tagged read, tagged update own, owner insert/delete); `memory_likes` (memory_id+user_id PK; RLS: own all, public read); `memory_saves` (memory_id+user_id PK; RLS: own all). All tables use IF NOT EXISTS; all policies on separate DROP/CREATE statements. | 2026-06-30 |
| `0068_stories.sql` | Creates Stories + Close Friends system. Enums: `story_state` (active/expired/saved/deleted/removed), `story_visibility` (public/friends_only/close_friends/trip_crew/circle_only/custom). Tables: `stories` (id, owner_id→auth.users, media_url/type, caption, visibility, allowed/hidden_user_ids uuid[], close_friends_only bool, trip_id→trips, event_id, place_id, expires_at=+24h, saved_to_highlight_id, state, hide_viewer_list; RLS: owner all, public read for active+non-expired; indexes on state+expires_at and owner+state+created_at); `story_views` (story_id+viewer_id PK, viewed_at; RLS: owner reads own story's viewers, viewer inserts own; index on story_id+viewed_at); `story_reactions` (story_id+user_id UNIQUE, emoji; RLS: owner/user read, user write); `story_replies` (story_id, user_id, message; RLS: owner+sender read, sender insert); `close_friends` (owner_id+friend_user_id PK; strictly private — owner-only RLS; index on owner_id). Seeds `stories_enabled` feature flag. Applied via Supabase Management API (no SQL file existed on disk; file saved to artifacts/api-server/src/migrations/0068_stories.sql). | 2026-06-30 |
| `0069_collections.sql` | Creates unified Collections & Saves system. Enum: `collection_entity_type` (post/event/trip/memory/highlight/place/profile/hashtag). Tables: `collections` (id, owner_id→auth.users CASCADE, name 1–120 chars, cover_url nullable, position int, is_default bool, UNIQUE partial index on owner_id WHERE is_default=TRUE; owner-only RLS), `collection_items` (id, collection_id→collections CASCADE, entity_type enum, entity_id uuid, saved_at; UNIQUE on collection_id+entity_type+entity_id; owner-only RLS via collections join). Backfills existing `user_saves` (profile saves) into the new tables — `discovery_place_saves` was not present in production and was skipped. Supports 9 API routes: GET/POST/PATCH/DELETE collections, GET collection items, POST/DELETE unified saves, GET save-status, GET saved-hashtags. | 2026-06-30 |
| `0067_reviews.sql` | Creates the cross-domain review system. Enums: `review_entity_type` (trip/rent_buddy_booking), `review_visibility` (public/anonymous), `review_state` (published/hidden/removed). Table: `reviews` (id, reviewer_id→auth.users CASCADE, entity_type, entity_id uuid, rating 1–5 CHECK, body text, tags text[], visibility, state, reported_by uuid[], created_at/updated_at; UNIQUE entity_type+entity_id+reviewer_id for dedup; indexes on entity (type+id), reviewer, created_at DESC). RLS: authors read/insert/update own rows; public read published rows; service role bypasses via service key. Feeds POST /api/reviews, GET /api/trips/:id/reviews, GET /api/users/:id/reviews, DELETE /api/reviews/:id, POST /api/reviews/:id/report routes. **Note:** File was originally committed as `0069_reviews.sql` (conflicting with `0069_collections.sql`); renamed to `0067_reviews.sql` by the 2026-07-03 DB audit to resolve the duplicate migration number. | 2026-06-30 |
| `0070_appeals.sql` | Creates the moderation appeals system. Enums: `appeal_state` (submitted/under_review/approved/denied), `appeal_target_type` (post/memory/highlight/account_warning/trust_score_event/no_show/event/event_membership/trip/trip_membership/review). Table: `appeals` (id, appellant_id→auth.users CASCADE, target_type, target_id uuid, reason text NOT NULL, evidence_url text, state DEFAULT submitted, moderator_id→auth.users nullable, resolution_note text, created_at/updated_at; UNIQUE appellant_id+target_type+target_id for one-active-appeal-per-target enforcement; indexes on appellant, state+created_at, moderator). RLS: appellants read/insert own rows; admin access enforced at application layer via service role client (bypasses RLS). Feeds POST /api/appeals, GET /api/appeals/me, GET /api/appeals (admin), PATCH /api/appeals/:id (admin with resolveAppeal reversal + notification). | 2026-06-30 |
| `0071_protect_default_collection.sql` | Adds a BEFORE DELETE trigger (`enforce_default_collection_no_delete`) on `collections` that raises an integrity constraint violation (SQLSTATE 23000) when any row with `is_default = TRUE` is deleted. Prevents direct-DB deletions (Supabase dashboard, psql, rogue migrations) from silently orphaning all of a user's saved items via the `ON DELETE CASCADE` on `collection_items`. The API DELETE /users/me/collections/:id handler already returns 403 at the application layer; this trigger is the belt-and-suspenders DB-level guard. Trigger fires BEFORE DELETE so the cascade never propagates. Function: `prevent_default_collection_delete()` (SECURITY INVOKER, LANGUAGE plpgsql). | 2026-07-01 |
| `0072_block_collections_truncate.sql` | Adds a BEFORE TRUNCATE statement-level trigger (`block_collections_truncate`) on `collections` that unconditionally raises SQLSTATE 23000. Row-level BEFORE DELETE triggers (0071) are never invoked by `TRUNCATE TABLE` in PostgreSQL, so a plain `TRUNCATE collections` from the Supabase dashboard or psql would bypass the `is_default` guard and cascade-delete every user's saved items via `collection_items ON DELETE CASCADE`. The statement-level trigger is the only PostgreSQL mechanism that intercepts TRUNCATE. Legitimate bulk-deletes should use `DELETE … WHERE` instead, which does fire the per-row guard. Function: `prevent_collections_truncate()` (LANGUAGE plpgsql). | 2026-07-01 |
| `0073_block_collection_items_truncate.sql` | Adds a BEFORE TRUNCATE statement-level trigger (`block_collection_items_truncate`) on `collection_items` that unconditionally raises SQLSTATE 23000. Migrations 0071 and 0072 protect the `collections` table (row-level DELETE guard + statement-level TRUNCATE guard), but `collection_items` had no equivalent protection. An admin running `TRUNCATE collection_items` or `TRUNCATE collection_items CASCADE` from the Supabase dashboard or psql would silently wipe every user's saved items without touching `collections` at all, bypassing both earlier triggers. The statement-level trigger is the only PostgreSQL mechanism that intercepts TRUNCATE. Legitimate bulk-deletes should use `DELETE … WHERE` instead. Function: `prevent_collection_items_truncate()` (LANGUAGE plpgsql). | 2026-07-01 |
| `0074_protect_saved_places.sql` | Creates `saved_places` table (user wishlist for Discovery places: user_id→auth.users CASCADE, place_id→discovery_places CASCADE, saved_at, UNIQUE user+place; owner-only RLS; index on user_id+saved_at DESC) and adds a BEFORE TRUNCATE statement-level trigger (`block_saved_places_truncate`) that unconditionally raises SQLSTATE 23000. A plain `TRUNCATE saved_places` from the Supabase dashboard or psql would silently wipe every user's wishlist in one statement. The trigger is the only PostgreSQL mechanism that intercepts TRUNCATE. Legitimate bulk-deletes must use `DELETE … WHERE` instead. Also extends `scripts/src/verify-db-triggers.mjs` REQUIRED array and `scripts/check-db-triggers.sh` SQL query to include `block_saved_places_truncate` on `saved_places`, so the pre-release db-triggers check catches a dropped trigger before it can ship. | 2026-07-01 |
| `0075_seed_discovery_places.sql` | Seed data: 46 curated `discovery_places` rows across 5 cities — Cebu (10), Manila (8), Bali (10), Bangkok (8), Singapore (10). Each row has name, place_type, category, neighborhood, blurb, image_url (Unsplash), rating, lat, lng, tag, source=`curated`, status=`active`, verified=`true`. Uses `ON CONFLICT (id) DO NOTHING` so it is safe to re-run. Ensures the DB-merge path in `GET /api/discovery/community` surfaces results for these destinations and the Pulse Wall shows place cards when posts are thin. Applied directly via Supabase JS service-role client (table now has 183 total rows). | 2026-07-01 |
| `0076_profile_emergency_contacts.sql` | Creates `profile_emergency_contacts` table (id, user_id→auth.users CASCADE, label, name, phone, email, notify_method enum `in_app\|sms\|email`, sort_order, created_at, updated_at; `pec_user_idx` on user_id; RLS enabled; `pec_own` policy — users read/write own rows; `pec_svc` policy — service role all). Backing store for Settings → Emergency Contacts screen; reusable across Safe Return sessions. Applied via Supabase Management API. | 2026-07-02 |
| `0077_trips_expansion.sql` | Extends `trip_status` enum with `draft` (before `planning`) and `archived` (after `cancelled`). Adds 14 new columns to `trips`: `trip_type` (default `leisure`), `timezone`, `destination_lat/lng DOUBLE PRECISION`, `destination_place_id`, `trip_notes`, and 9 privacy boolean columns: `show_on_profile` (default true), `show_in_discovery` (default false), `allow_friend_suggestions` (true), `allow_trip_crew_invites` (true), `allow_join_requests` (false), `show_exact_dates` (true), `show_destination_city` (true), `delayed_posting_default` (false), `precise_location_visible` (false). All new columns have safe defaults so existing rows are unaffected. | 2026-07-03 |
| `0078_trip_members_expansion.sql` | Extends `member_role` enum with `co_host` (after `owner`) and `viewer` (after `member`). Adds 3 new columns to `trip_members`: `status TEXT NOT NULL DEFAULT 'accepted'` with CHECK constraint (invited/accepted/declined/removed/left), `permissions JSONB` (nullable per-member permission overrides), `joined_at TIMESTAMPTZ`. Backfills existing invited-role rows to `status='invited'` and owner/member rows to `joined_at=created_at`. | 2026-07-03 |
| `0079_trip_sub_tables.sql` | Creates 11 new trip sub-resource tables: `trip_budget` (one-row-per-trip upsert: currency, total_budget, spent, breakdown jsonb; RLS owner-only), `trip_documents` (title, content, document_type enum 6 values, is_private; RLS members read non-private/own, owner sees all), `trip_join_requests` (user_id, status enum 4 values, message, reviewed_by/at; UNIQUE trip+user; RLS requester+owner read, requester insert), `trip_invite_links` (token UNIQUE, created_by, max_uses, use_count, expires_at, revoked_at; RLS owner manages, public reads valid links), `trip_saved_places` (place_id, place_name/type, lat/lng, notes; UNIQUE trip+user+place_id; RLS members read, member insert/own-delete), `trip_notes` (title, content, is_private, author_id; RLS members read non-private/own, owner sees all), `trip_checklists` (title, created_by; RLS members read+insert), `trip_checklist_items` (checklist_id, trip_id, label, is_done, assigned_to, due_date, sort_order; RLS members), `trip_activity_log` (actor_id, event_type, metadata jsonb; append-only; RLS owner+co_host read), `trip_reminders` (user_id, title, remind_at, is_sent; RLS users own rows), `trip_destinations` (city, country, lat/lng, place_id, arrival/departure dates, position; RLS members read, owner manages). All tables use IF NOT EXISTS, enable RLS, and have appropriate indexes. | 2026-07-03 |
| `0080_events_extension.sql` | Extends the Events system: adds `show_exact_location` (bool, default false), `rsvp_closed` (bool, default false), `safety_notes` (text nullable), `tags` (text[], default '{}') columns to `events`. Creates 10 new tables: `event_saves` (user-event PK bookmark table, owner RLS), `event_invites` (UNIQUE event+invitee, status enum pending/accepted/declined/revoked, inviter/invitee read+update RLS), `event_cohosts` (UNIQUE event+user, permissions JSONB, added_by; public read), `event_posts` (event-specific posts with media_urls/pinned; author write), `event_media` (image/video uploads per event; uploader write), `event_reports` (event-type or user-type reports, UNIQUE event+reporter+type, reporter RLS), `event_activity_log` (append-only audit log; host/mod read via subquery RLS), `event_share_links` (token UNIQUE via gen_random_bytes, max_uses/use_count/expires_at; creator manage + public read), `event_reminders` (UNIQUE event+user+remind_at, sent flag; owner RLS), `event_drafts` (host_id, data JSONB, last_saved_at autosave blobs; owner RLS). Adds 5 feature flag seeds: events_invites_enabled, events_cohosts_enabled, events_reports_enabled, events_reminders_enabled, events_share_links_enabled. All statements are idempotent (IF NOT EXISTS / DO $$). | 2026-07-03 |
| `0081_stamp_system_v2.sql` | Applied 2026-07-03. Creates the full Passport Stamps v2 data model: `stamp_definitions` (slug UNIQUE, name, description, stamp_type, category, rarity, is_active, is_repeatable, max_awards_per_user, level_config jsonb, criteria_type, criteria jsonb, visibility_default, source_system, city/country geo-lock, starts_at/ends_at; public read RLS for active rows; admin all), `user_stamps` (user_id→profiles, stamp_definition_id→stamp_definitions, source_type/id, earned_at, city/country, lat/lng PRIVATE, title_override, metadata, visibility, display_on_passport, is_revoked; owner read + public non-revoked-public read + friends-only read via user_friendships; service_role write), `stamp_award_events` (idempotency_key UNIQUE sha of user:def:sourceType:sourceId, status awarded/revoked/restored; owner + admin read; service_role write), `stamp_progress` (user+definition PK, progress_count, progress_target; owner read; service_role write), `stamp_collections` (slug UNIQUE, name, description; public read when is_active), `stamp_collection_items` (collection+definition PK, sort_order; public read), `stamp_campaigns` (slug UNIQUE, stamp_definition_id, starts_at/ends_at; public read when is_active; admin all). Includes 8 indexes. Seeds feature flags `stamp_system_v2_enabled` and `stamp_admin_award_enabled` (both false). Inserts ~40 starter stamp definitions across location/trip/social/safety/rent_buddy/special categories — all with `is_active=false` pending trigger wiring. All CREATE POLICY statements use DROP POLICY IF EXISTS guards (idempotent). Column-level `REVOKE SELECT (lat, lng) ON user_stamps FROM authenticated, anon` enforces DB-level privacy so PostgREST never exposes precise location to non-service callers regardless of row-level policies. | 2026-07-03 |
| `0082_stamp_definitions_v2.sql` | Applied 2026-07-03. Activates and adds the stamp definitions whose triggers were wired in Task #1048. **New rows (12):** `first_trip_created` (trip/common), `first_trip_completed` (trip/common), `solo_traveler` (trip/common), `group_tripper` (trip/uncommon), `weekend_wanderer` (trip/common), `first_postcard` (social/community/common), `safe_return_ready` (safety/safety/common/private), `safe_return_completed` (safety/safety/uncommon/private), `first_buddy_booking` (rent_buddy/common), `first_buddy_hosted` (rent_buddy/common), `hidden_gem_explorer` (special/uncommon), `verified_traveler` (safety/trust/uncommon). **Activated rows (4):** sets `is_active=true` on existing `road_warrior`, `frequent_flyer`, `long_haul`, `international_voyager` definitions (triggers now live). All INSERTs use ON CONFLICT (slug) DO NOTHING; UPDATE uses WHERE is_active = false — fully idempotent. | 2026-07-03 |
| `0094_profile_account_status_and_privacy.sql` | Adds `profiles.account_status TEXT NOT NULL DEFAULT 'active'` (backfills deactivated users from `user_account_states`); creates `profile_privacy_settings` table (user_id PK→auth.users, 18 privacy columns with defaults, RLS: `pps_own` all-ops for owner, `pps_svc` for service role); creates `user_deletion_requests` table (user_id PK→auth.users, requested_at, scheduled_at, status enum pending/cancelled/executed, RLS: `udr_own` SELECT for owner, `udr_svc` all for service role; partial index on pending requests). Required by `GET /api/me/privacy`, `PATCH /api/me/privacy`, `POST /api/me/delete-request`, `POST /api/me/deactivate`, `POST /api/me/reactivate`. Applied 2026-07-03 via Supabase management API; schema verified and all three routes smoke-tested against production (all returned HTTP 200). | 2026-07-03 |
| `20260702_crew_location_flags_reseed.sql` | Re-seeds the three trip crew location feature flags that migration `0041_trip_crew_location.sql` failed to insert. The original migration used `INSERT INTO feature_flags (key, ...)` but the `feature_flags` PK column is named `flag`, so all three rows were silently skipped. This migration inserts `trip_crew_map_enabled` (true), `trip_crew_live_share_enabled` (true), and `trip_crew_ghost_mode_enabled` (true) using `ON CONFLICT (flag) DO NOTHING` so it is safe to re-run. Also fixes the companion bug: the local `isFlagEnabled` in `routes/tripCrewLocation.ts` was using `.eq("key", ...)` instead of the shared `lib/featureFlags.ts` helper (`.eq("flag", ...)`), causing `GET /api/trips/:tripId/crew/map` to always return `featureEnabled: false` regardless of DB state. Fix: removed the local duplicate and imported `isFlagEnabled` from `../lib/featureFlags.js`. | 2026-07-02 |
| `0083_place_category_columns.sql` | Adds `primary_category TEXT NOT NULL DEFAULT 'places'` and `secondary_categories text[] NOT NULL DEFAULT '{}'` to `discovery_places`. Backfills `primary_category` from existing `category`/`place_type` values using the canonical Discovery tab mapping (food/beaches/nightlife/activities/events/places/transport — default `'places'`). Adds `discovery_places_primary_category_idx` on `(primary_category)` and `discovery_places_city_category_idx` on `(city, primary_category)` for fast category-filtered queries. Safe to re-run (IF NOT EXISTS / WHERE primary_category IS NULL guards). Backfill distributed 183 rows: places 67, food 58, nightlife 27, beaches 17, activities 12, events 2. | 2026-07-02 |
| `0084_reviews_place_entity.sql` | Extends `review_entity_type` enum with `'place'` value via idempotent `DO $$` check (`ALTER TYPE ... ADD VALUE` applied in a separate transaction from the policy to satisfy PostgreSQL commit ordering). Creates `"Authors can delete own place reviews"` DELETE policy on `reviews` (`reviewer_id = auth.uid() AND entity_type = 'place'::review_entity_type`). Enables `POST /api/reviews` with `entityType='place'` and place-rating deletes for the Discovery place cards feature. | 2026-07-02 |
| `0085_enable_passport_flags.sql` | Enables four feature flags that were seeded as `false` in earlier migrations: `passport_stamps_enabled` (0037/0042), `passport_memories_enabled` (0037/0042), `stamp_system_v2_enabled` (0081), `stamp_admin_award_enabled` (0081). Uses `ON CONFLICT (flag) DO UPDATE SET enabled = true` — safe to re-run. Without this migration stamps and memories are blocked for all users even though the routes and tables are fully implemented. Apply via Supabase dashboard SQL editor. | 2026-07-03 |
| `0086_discovery_places_osm_id.sql` | Adds `osm_id TEXT` column to `discovery_places` with a partial unique index `discovery_places_osm_id_idx ON discovery_places (osm_id) WHERE osm_id IS NOT NULL`. Sets `DEFAULT ''` on the `city` column so that OSM-sourced rows can be inserted without a destination city (they exist only for `saved_count` tracking and won't appear in city-filtered discovery queries). Enables the wishlist handler to upsert lightweight `source='osm'` rows keyed by Overpass element ID (e.g. `"node/12345678"`) and the `GET /api/discovery` handler to enrich OSM results with real save counts before caching. | 2026-07-03 |

| `0087_profiles_cover_photo_url.sql` | Adds `cover_photo_url TEXT` to `profiles` via `ADD COLUMN IF NOT EXISTS`. Multiple API routes (GET/PATCH /me/profile, passport, admin) SELECT or UPDATE this column; its absence causes PostgREST PGRST204 errors that surfaced as an error banner on the Edit Profile screen and prevented profile saves. Also fixes both PGRST204 fallback guards in the profile route (GET line 200, PATCH line 384) — they previously only checked PostgreSQL code `42703` and never triggered on the PostgREST PGRST204 variant. Safe to re-run. Applied via Supabase Management API. | 2026-07-03 |
| `0088_wishlist_places.sql` | Creates `wishlist_places` table (id, user_id→auth.users CASCADE, place_id text, place_data jsonb, list_id text DEFAULT 'global', saved_at; UNIQUE user+place+list; owner-only RLS; `wishlist_places_user_list_idx` on user_id+list_id+saved_at DESC). Stores full place data as JSONB so any bookmarked place (DB-sourced or OSM-sourced) can be persisted. Separate from `saved_places` (0074) which carries a FK to discovery_places and is reserved for DB-only places. Includes TRUNCATE guard trigger. API server writes via service role key. **Renumbered from 0076:** the original file was created as `0076_wishlist_places.sql` but migration `0076_profile_emergency_contacts.sql` had already been applied to production under that number, causing a conflict. File renamed to 0088 to avoid collision. | 2026-07-03 |
| `0089_decrement_discovery_place_saved_count.sql` | Creates `public.decrement_discovery_place_saved_count(p_id UUID) RETURNS integer` SECURITY DEFINER Postgres function with privilege hardening. Executes `UPDATE discovery_places SET saved_count = GREATEST(0, saved_count - 1) WHERE id = p_id RETURNING saved_count` atomically and returns the new value. Fixes the stale-snapshot race in `trackOsmPlaceUnsave` where two concurrent different-user unsaves both read the same `saved_count` snapshot in Node.js, both compute `snapshot - 1`, and both overwrite the DB with the same value — leaving the count one step too high. Privilege hardening: `SET search_path = public` (prevents search_path injection); `REVOKE ALL FROM PUBLIC, anon, authenticated` (removes the default PUBLIC execute grant that would otherwise allow any PostgREST caller to invoke it directly and tamper with counts); `GRANT EXECUTE TO service_role` (restricts execution to the backend service account only). Called via `svc.rpc('decrement_discovery_place_saved_count', { p_id })` from `artifacts/api-server/src/routes/wishlist.ts`. | 2026-07-03 |
| `0091_activate_stamp_definitions.sql` | Activates the beta-launch subset of the ~40 stamp definitions seeded as `is_active=false` by migration 0081. **Activated (4 rows):** `early_adopter`, `founding_member`, `ambassador`, `beta_tester` — all have `criteria_type='admin_only'`, so no automatic trigger is required; they are awarded exclusively via `POST /api/admin/stamps/award`. These are high-value engagement stamps for the beta period and are safe to enable immediately. **Not activated (kept dormant):** (a) 0081 definitions superseded by cleaner v2 slugs added in 0082 (`first_trip`, `solo_adventurer`, `crew_captain`, `weekend_warrior`, `safe_traveler`, `safety_advocate`, `verified_identity`, `first_session`); (b) automatic definitions whose route-level triggers are not yet wired: location stamps (`city_explorer`, `globe_trotter`, `world_citizen`, `continent_hopper`, `neighborhood_local`, `hidden_gem_hunter`, `night_owl`, `sunrise_chaser`), social count stamps (`first_post`, `storyteller`, `photographer`, `community_connector`, `popular_traveler`, `travel_influencer`), trip-outcome stamps (`trip_planner`, `good_host`), trust-level stamps (`trusted_member`, `city_trusted_member`), rent-a-buddy progression stamps (`buddy_veteran`, `top_rated_buddy`, `nightlife_guide`, `food_guide`), and event stamps (`event_participant`, `event_host`). Uses `WHERE is_active = false` guard — idempotent. | 2026-07-03 |
| `0092_seed_rent_buddy_launch_cities.sql` | Seeds the three initial Philippines launch cities into `rent_buddy_city_rollouts` at `public_mvp` status: Cebu, Manila, Davao City. Without at least one row at `public_mvp` or `beta_testing` status every call to `checkRentBuddyAccess` returns `city_not_available` and Rent a Buddy is invisible to all users even though migration 0090 fully deployed the tables and flags. Uses `ON CONFLICT (city) DO NOTHING` — safe to re-run. Also extends `scripts/src/verify-db-rent-buddy-rollout.mjs` and `scripts/check-db-triggers.sh` to verify at least one live city exists in production as part of the `db-triggers` pre-release check; a release will not pass until this seed is applied. Fixes the incorrect `status = 'live'` example in `docs/production-migration-runbook.md §9.7` (`'live'` is not a valid `rent_buddy_city_status` enum value; correct value is `'public_mvp'`). **Note:** Production table predated the 0090 schema file and lacks the `country` column and `UNIQUE(city)` constraint. Cebu pre-existed at `status='live'` (not recognized by `checkRentBuddyAccess`); updated to `public_mvp`. Manila and Davao City inserted fresh. | 2026-07-04 |

## Stamp award durability (no migration required)

**Implemented 2026-07-03** — No new tables or columns. Pure engine change in `artifacts/api-server/src/services/passport/StampAwardEngine.ts`.

### Problem
`awardTripCompletionStamps` calls `awardStamp` fire-and-forget. Inside `awardStamp`, step 6 inserts a `stamp_award_events` row (idempotency key committed) and step 7 inserts the `user_stamps` row. If the DB drops between steps 6 and 7, the event row exists with `status='awarded'` but no passport row is created. The user's passport stays empty until `recalculate/me` is triggered manually.

### Fix — two layers of defence

**Layer 1 — exponential-backoff retry (step 7).**  
`user_stamps` insert is retried up to 3 times on transient errors (delays: 100 ms, 200 ms). Unique-constraint violations (`23505`) are never retried — they are not transient. This closes the window for the vast majority of transient DB glitches during a live request.

**Layer 2 — self-healing idempotency check (step 3).**  
When the idempotency check at step 3 finds an existing `awarded` event but no corresponding `user_stamps` row, the engine skips steps 4–6 (dedup guards + event insert) and jumps directly to the stamp insert. This means any subsequent call to `awardStamp` with the same `(userId, definitionSlug, sourceType, sourceId)` tuple will automatically heal a partial failure — the passport row is created without needing a manual recalculate. The `recalculateForUser` engine path remains available as a manual repair tool for any rows that slipped through before this fix was deployed.

| `0093_activate_stamp_definitions.sql` | Activates 17 previously-inactive stamp definitions whose route-level triggers are now wired: `first_post`, `storyteller`, `photographer` (post counts in posts.ts); `city_explorer`, `globe_trotter`, `world_citizen` (GPS location milestones in posts.ts); `community_connector`, `popular_traveler`, `travel_influencer` (follow counts in follows.ts); `trip_planner`, `good_host` (trip lifecycle in trips.ts); `buddy_veteran`, `nightlife_guide`, `food_guide`, `top_rated_buddy` (rent-a-buddy in rentABuddy.ts); `event_host`, `event_participant` (event completion in events.ts). All 17 rows confirmed `is_active = true` in production via spot-check. Applied via Supabase JS service-role client (UPDATE … WHERE is_active = false — idempotent). | 2026-07-04 |
| `0094_profile_account_status_and_privacy.sql` | Three items used by live backend routes that had no SQL file committed: **(1)** `profiles.account_status TEXT NOT NULL DEFAULT 'active'` — read by `GET /admin/users` (explicit SELECT column, no guard) and written by `POST /me/deactivate` + `POST /me/reactivate` + `POST /me/delete-request`; backfills deactivated rows from `user_account_states` to stay consistent. **(2)** `profile_privacy_settings` table — 17-column per-user privacy config (profile_visibility, show_* toggles, allow_* toggles, delayed_posting_default, precise_location_visible; user_id PK → auth.users CASCADE; RLS: users manage own row, service role all); used by `GET /me/privacy` (table-missing guard exists) and `PATCH /me/privacy` (no guard — hard-fails if table absent). **(3)** `user_deletion_requests` table — (user_id PK, requested_at, scheduled_at, status pending/cancelled/executed, cancelled_at, executed_at; RLS: users SELECT own, service role all; partial index on status='pending'); used by `POST /me/delete-request` (awaited upsert — hard-fails if table absent). All statements idempotent (ADD COLUMN IF NOT EXISTS / CREATE TABLE IF NOT EXISTS). **Also fixed:** `hiddenGems.ts` local `requireAdmin()` was reading `profiles.is_admin` (column never created) instead of `profiles.role`; changed to `.select("role")` with `.role === "admin"` check — matching the pattern used everywhere else in the codebase. | 2026-07-03 |
| `0095_post_category.sql` | Adds nullable `category TEXT` column to `posts` (`ADD COLUMN IF NOT EXISTS` — idempotent). Values match the `PostCategory` enum on the client (e.g. `food`, `nightlife`, `beach`, `adventure`). Required for the category stamp overlay on the post detail screen and for the category picker in the post composer. `POST /api/posts` accepts and persists category; all feed and post-detail routes select and return it. | 2026-07-04 |
| `0097_post_saves.sql` | Creates `post_saves` table (`user_id`, `post_id`, `created_at`; `PRIMARY KEY (user_id, post_id)` enforces uniqueness; index on `post_id`; RLS: users manage own rows via `post_saves_own` policy). Adds `save_count INTEGER NOT NULL DEFAULT 0` to `posts` (maintained by the API server on `POST /api/posts/:id/save` and `DELETE /api/posts/:id/save`). Duplicate-save is always idempotent via `ON CONFLICT DO NOTHING`; unsave of a non-existent row is handled gracefully. Feed and post-detail responses include `savedByMe` and `save_count`. Applied manually to Supabase. | 2026-07-04 |
| `0098_profile_translation_prefs.sql` | Adds four Telegraph translation-preference columns to `profiles` (all `ADD COLUMN IF NOT EXISTS` — idempotent): `preferred_message_language TEXT`, `auto_translate_messages BOOLEAN NOT NULL DEFAULT FALSE`, `show_original_messages BOOLEAN NOT NULL DEFAULT FALSE`, `translation_updated_at TIMESTAMPTZ`. Read/written by `GET /api/me/translation-settings` and `PATCH /api/me/translation-settings` in `messaging.ts`. Note: `preferred_language` (BCP-47 fallback) was added in migration 0018; `tag_permission` was added in migration 0043. Applied manually to Supabase. | 2026-07-04 |
| `0100_backfill_display_name.sql` | One-time data backfill: copies `name → display_name` for every `profiles` row where `display_name IS NULL AND name IS NOT NULL`. Makes historical rows consistent with the dual-write pattern introduced in `PATCH /api/me/profile`. Idempotent — rows that already have `display_name` set are untouched. Verified post-apply: 0 rows remain with `display_name IS NULL AND name IS NOT NULL`. | 2026-07-04 |

## Schema verification notes (2026-07-03)

### Verification query

Run the following in the Supabase dashboard SQL editor to confirm both columns exist in production:

```sql
SELECT column_name, data_type, is_nullable, column_default
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name   = 'profiles'
  AND column_name  IN ('role', 'cover_photo_url')
ORDER BY column_name;
```

Expected output (2 rows):

| column_name     | data_type         | is_nullable | column_default |
|-----------------|-------------------|-------------|----------------|
| cover_photo_url | text              | YES         | NULL           |
| role            | text              | NO          | 'user'::text   |

If either row is missing, apply the relevant migration below before deploying.

---

### `profiles.role` column

The `profiles` table carries a `role TEXT NOT NULL DEFAULT 'user'` column. This column was part of the initial schema and has no standalone numbered migration file — it is present in the baseline `profiles` table DDL. Verified values: `'user'` (default for all users) and `'admin'`.

**Admin access guard** — `requireAdmin` in `artifacts/api-server/src/routes/admin.ts` reads role exclusively from `profiles.role`:

```ts
const { data, error } = await sc.from("profiles").select("role").eq("id", user.id).maybeSingle();
if (error || !data || (data as any).role !== "admin") {
  res.status(403).json({ error: "forbidden", message: "Admin role required" });
}
```

Role is **not** read from `auth.app_metadata` or `auth.users.raw_app_meta_data`. To grant admin access to a user: `UPDATE profiles SET role = 'admin' WHERE id = '<uuid>';`.

### Migration 0101 (supabase/migrations) — `search_history` table

`supabase/migrations/0101_search_history.sql` creates the `search_history` table used by the global search feature (Phases 11–18, 26).

| Detail | Value |
|--------|-------|
| File | `supabase/migrations/0101_search_history.sql` |
| Table | `search_history` |
| Columns | `id UUID PK`, `user_id UUID→auth.users`, `query TEXT NOT NULL`, `search_type TEXT NOT NULL DEFAULT 'all'`, `searched_at TIMESTAMPTZ DEFAULT now()` |
| Index | `search_history_user_idx ON search_history (user_id, searched_at DESC)` |
| RLS | Enabled; users read/write only their own rows (`auth.uid() = user_id`) |
| Note | The API server enforces a 50-row per-user cap (prunes oldest on POST). Apply before deploying the search intelligence update. |

**Verify:**
```sql
SELECT to_regclass('public.search_history'), indexname
FROM pg_indexes WHERE indexname = 'search_history_user_idx';
```

---

### `profiles.cover_photo_url` column

Added by migration `0087_profiles_cover_photo_url.sql` (`ADD COLUMN IF NOT EXISTS cover_photo_url TEXT`). Used by `GET /api/me/profile`, `PATCH /api/me/profile`, passport loader, and admin media routes. Absence causes PostgREST PGRST204 errors on profile saves — apply 0087 if the Edit Profile screen shows an error banner.

### Migrations 0095, 0097, 0098, 0099 — applied manually 2026-07-04

These migrations were applied directly via the Supabase SQL editor and have no automated runner entry. The SQL source files are committed to `artifacts/api-server/src/migrations/` for reference and future idempotent re-runs.

| Migration | Key SQL | Verify |
|-----------|---------|--------|
| `0095_post_category.sql` | `ALTER TABLE posts ADD COLUMN IF NOT EXISTS category TEXT` | `SELECT column_name FROM information_schema.columns WHERE table_name='posts' AND column_name='category'` |
| `0097_post_saves.sql` | `CREATE TABLE IF NOT EXISTS post_saves (user_id UUID, post_id UUID, PRIMARY KEY (user_id, post_id))` + `ALTER TABLE posts ADD COLUMN IF NOT EXISTS save_count INTEGER NOT NULL DEFAULT 0` | `SELECT to_regclass('public.post_saves')` |
| `0098_profile_translation_prefs.sql` | Four `ADD COLUMN IF NOT EXISTS` on `profiles`: `preferred_message_language TEXT`, `auto_translate_messages BOOLEAN`, `show_original_messages BOOLEAN`, `translation_updated_at TIMESTAMPTZ` | `SELECT column_name FROM information_schema.columns WHERE table_name='profiles' AND column_name='auto_translate_messages'` |
| `0099_missing_indexes.sql` | `CREATE INDEX IF NOT EXISTS posts_author_status_idx ON posts (author_id, post_status)` + `CREATE INDEX IF NOT EXISTS post_saves_post_created_idx ON post_saves (post_id, created_at DESC)` | `SELECT indexname FROM pg_indexes WHERE indexname IN ('posts_author_status_idx','post_saves_post_created_idx')` |
| `0101_feed_save_indexes.sql` | `CREATE INDEX IF NOT EXISTS posts_status_published_at_idx ON posts (post_status, published_at DESC)` + `CREATE INDEX IF NOT EXISTS post_saves_created_at_idx ON post_saves (created_at DESC)` | `SELECT indexname FROM pg_indexes WHERE indexname IN ('posts_status_published_at_idx','post_saves_created_at_idx')` |

All statements are idempotent — safe to re-run.

### Migration 0100 — applied 2026-07-04

`artifacts/api-server/migrations/0100_backfill_display_name.sql` was applied to production on 2026-07-04 via the Supabase Management API. The backfill copied `name → display_name` for every `profiles` row where `display_name IS NULL AND name IS NOT NULL`. Post-apply verification confirmed 0 remaining rows with `display_name IS NULL AND name IS NOT NULL`.

| Effect | Detail |
|--------|--------|
| Rows updated | All `profiles` rows where `display_name IS NULL AND name IS NOT NULL` (at time of apply) |
| Rows unchanged | Rows that already had `display_name` set, or where `name` was also NULL |
| Safe to re-run | Yes — `WHERE display_name IS NULL` makes subsequent runs a no-op |

### Migration 0101 — pending production apply

`artifacts/api-server/src/migrations/0101_feed_save_indexes.sql` adds two indexes to keep the feed and saves queries fast as data grows:

| Index | Table | Columns | Query served |
|-------|-------|---------|--------------|
| `posts_status_published_at_idx` | `posts` | `(post_status, published_at DESC)` | Feed: `WHERE post_status = 'published' ORDER BY published_at DESC LIMIT 20` |
| `post_saves_created_at_idx` | `post_saves` | `(created_at DESC)` | Saves listing: `ORDER BY created_at DESC` |

**Apply:** paste the file contents into the Supabase SQL editor and run. Both `CREATE INDEX IF NOT EXISTS` statements are idempotent.

**Verify:**
```sql
SELECT indexname FROM pg_indexes
WHERE indexname IN ('posts_status_published_at_idx', 'post_saves_created_at_idx');
```

Both index names should appear in the result before marking this migration applied.

**Why these two indexes?**

- `posts_author_status_idx` (0099) covers `(author_id, post_status)` — good for per-author filtered feeds — but the public feed query only filters on `post_status` and sorts by `published_at`. Without `posts_status_published_at_idx`, Postgres cannot use that index and falls back to a sequential scan + sort.
- `post_saves_post_created_idx` (0099) covers `(post_id, created_at DESC)` — efficient for "who saved this post" lookups — but a full saves listing sorted only by `created_at` still requires a seq-scan + sort. `post_saves_created_at_idx` on `(created_at DESC)` lets Postgres walk the index in order and stop at the LIMIT.

| `0103_post_media.sql` | Creates `post_media` table (id UUID PK, post_id→posts CASCADE, user_id→auth.users CASCADE, media_type text CHECK image\|video, storage_bucket/path, public_url, thumbnail_url, thumbnail_storage_path, mime_type, file_size_bytes bigint, duration_seconds float, width/height int, processing_status text CHECK pending\|ready\|failed DEFAULT pending, moderation_status text CHECK pending\|approved\|flagged\|rejected DEFAULT pending, sort_order int DEFAULT 0, created_at/updated_at timestamptz); indexes: `post_media_post_id_idx`, `post_media_user_id_idx`, `post_media_post_status_idx`; RLS enabled; `post_media_owner_write` policy (owner write + parent post ownership check); `post_media_public_select` policy (owner sees all, others see ready+approved+unblocked media on public or trip_only posts); storage bucket policies on `storage.objects` for `post-media` bucket (owner insert/delete, public read); `ALTER TABLE posts ADD COLUMN IF NOT EXISTS primary_media_type TEXT, media_count INTEGER NOT NULL DEFAULT 0, has_video BOOLEAN NOT NULL DEFAULT false`; `ALTER TABLE passport_postcards` same three columns; back-fills `primary_media_type/media_count/has_video` on existing rows of both tables. **RLS fix applied at deploy time**: migration file had `p.visibility = 'followers'` which is not a valid `post_visibility` enum value (production enum: `public\|trip_only\|private`); corrected to remove the friends-only branch before applying. | 2026-07-05 |
| `0104_compass_new_tables.sql` | Creates `compass_feedback` (per-recommendation in-feed feedback actions: not_now/hide/report/etc., RLS owner-only), `compass_recent_context` (persisted context session with 4h TTL, unique per user, RLS owner-only), `compass_settings` (user-level Compass privacy/data-use toggle settings, RLS owner-only); all tables idempotent (IF NOT EXISTS) | 2026-07-05 |
| `0105_compass_performance_indexes.sql` | Adds covering indexes for Compass item hydrator queries: `events_compass_city_starts_idx` (city, starts_at, status, visibility), `events_compass_category_idx`, `trips_compass_destination_idx`, `discovery_places_compass_city_idx` + `discovery_places_compass_category_idx`, `places_compass_city_category_idx` (partial, excludes rejected), `profiles_compass_city_idx` (partial, active-only), `profiles_compass_city_role_idx`; all idempotent (IF NOT EXISTS). Rewritten 2026-07-17 against the live schema (events.state, trips.destination_city/start_date, discovery_places.status, profiles.current_city) and applied — see the corrected entry above. | 2026-07-05, rewritten+applied 2026-07-17 |
| `0106_engagement_indexes.sql` | Adds five composite indexes for cursor-based pagination in `GET /api/engagement/likes`: `idx_posts_likes_post_created` on `posts_likes (post_id, created_at DESC)`, `idx_post_reactions_post_emoji_created` on `post_reactions (post_id, emoji, created_at DESC)`, `idx_comment_likes_comment_created` on `comment_likes (comment_id, created_at DESC)`, `idx_highlight_likes_highlight_created` on `highlight_likes (highlight_id, created_at DESC)`, `idx_memory_likes_memory_created` on `memory_likes (memory_id, created_at DESC)`; all idempotent (IF NOT EXISTS). **Apply:** paste `artifacts/api-server/src/migrations/0106_engagement_indexes.sql` into the Supabase SQL editor and run. **Verify:** `SELECT indexname FROM pg_indexes WHERE indexname IN ('idx_posts_likes_post_created','idx_post_reactions_post_emoji_created','idx_comment_likes_comment_created','idx_highlight_likes_highlight_created','idx_memory_likes_memory_created');` — all five names must appear. | 2026-07-05 |
| `0108_circle_schema_tracked.sql` | **Canonical tracking for all Find Your Circle tables** (previously applied to production via non-canonical migrations 0115–0122). Creates seven tables — (1) `circle_visibility_settings` (PK user_id→profiles, global_enabled BOOLEAN DEFAULT false, visibility_mode CHECK status_only\|approximate_area\|venue_checkin\|precise_live, trip_sharing_default TEXT CHECK off\|status_only\|approximate_area\|venue_checkin DEFAULT status_only, event_sharing_default same, is_paused BOOLEAN DEFAULT false, paused_until TIMESTAMPTZ, consent_version TEXT, consented_at TIMESTAMPTZ, updated_at; RLS: owner all + service_role all; seeds find_your_circle_enabled and find_your_circle_disabled feature flags); (2) `circle_context_settings` (id UUID PK, user_id→profiles, context_type CHECK trip\|event, context_id UUID, enabled BOOLEAN DEFAULT true, visibility_mode_override TEXT nullable, paused BOOLEAN DEFAULT false, paused_until TIMESTAMPTZ, UNIQUE user_id+context_type+context_id; index: ccs_context_idx; RLS: owner all + service_role all); (3) `circle_presence` (id UUID PK, user_id→profiles, context_type, context_id, status CHECK active\|arrived\|with_group\|leaving\|safe\|needs_help DEFAULT active, status_label/approximate_label/venue_label TEXT, checked_in BOOLEAN, stale_after_secs INTEGER DEFAULT 1800, last_seen_at TIMESTAMPTZ, expires_at TIMESTAMPTZ, is_stale BOOLEAN, needs_help BOOLEAN, UNIQUE user_id+context_type+context_id; indexes: cp_context_idx + cp_expires_at_idx + cp_last_seen_idx; RLS: owner read own row + owner write + service_role all); (4) `circle_checkins` (immutable log: id, user_id, context_type, context_id, checkin_type CHECK arrived\|with_group\|leaving\|safe\|needs_help, note/venue_label/approximate_label TEXT, created_at; indexes: ccin_user_context_idx + ccin_context_idx; RLS: owner read + owner insert + service_role all); (5) `circle_member_visibility_overrides` (id, user_id→profiles, target_user_id→profiles, context_type, context_id, direction CHECK hide_from_me\|hide_me_from, hidden BOOLEAN DEFAULT true, UNIQUE user_id+target_user_id+context_type+context_id+direction; indexes: cmvo_user_context_idx + cmvo_target_context_idx; RLS: owner all + service_role all); (6) `circle_meeting_points` (id, context_type, context_id, host_user_id→profiles, venue_label/approximate_label/description TEXT, is_active BOOLEAN DEFAULT true; index: cmp_context_active_idx; RLS: service_role all only — membership gate enforced at API layer); (7) `circle_audit_events` (immutable log: id, actor_user_id→profiles nullable, target_user_id→profiles nullable, context_type, context_id, event_type CHECK 11 values, metadata JSONB; indexes: cae_actor_idx + cae_target_idx + cae_context_idx + cae_event_type_idx; RLS: actor\|target read own rows + service_role all). All statements idempotent (IF NOT EXISTS, ADD COLUMN IF NOT EXISTS, DROP POLICY IF EXISTS). **Apply:** paste `artifacts/api-server/src/migrations/0108_circle_schema_tracked.sql` into the Supabase SQL editor and run. This is safe on a database that already has these tables — all statements are guards. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name IN ('circle_visibility_settings','circle_context_settings','circle_presence','circle_checkins','circle_member_visibility_overrides','circle_meeting_points','circle_audit_events') ORDER BY table_name;` — all seven names must appear. `SELECT policyname, tablename FROM pg_policies WHERE tablename LIKE 'circle_%' ORDER BY tablename, policyname;` — every table must show at least a service_role policy. | 2026-07-05 |
| `0107_rent_buddy_admin_actions.sql` | Creates `rent_buddy_admin_actions` audit log table. This table was referenced by 20+ INSERT statements in `rentABuddy.ts` and `rentABuddyMarketplace.ts` (admin suspend/approve/feature actions) and declared in `database.types.ts`, but was absent from all prior migrations. Schema: `id UUID PK, admin_id UUID FK→profiles (nullable), target_type TEXT NOT NULL, target_id TEXT NOT NULL, action TEXT NOT NULL, notes TEXT, details JSONB, created_at TIMESTAMPTZ`; RLS enabled with service-role-only policy; indexes on `(admin_id, created_at DESC)` and `(target_type, target_id)`. **Apply:** paste `artifacts/api-server/migrations/0107_rent_buddy_admin_actions.sql` into the Supabase SQL editor and run. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name = 'rent_buddy_admin_actions';` — the table name must appear. Also run `SELECT policyname FROM pg_policies WHERE tablename = 'rent_buddy_admin_actions';` — `rb_admin_actions_svc` must appear. | 2026-07-05 |
| `0109_claim_invite_link_slot.sql` | Creates two SECURITY DEFINER functions used by `POST /api/trips/invite-link/:token/accept`: `claim_invite_link_slot(link_id uuid) RETURNS boolean` — increments `use_count` atomically gated on `max_uses`, returns FALSE when the cap is hit; `release_invite_link_slot(link_id uuid)` — decrements `use_count` (floors at 0) as a compensation step when the subsequent `trip_members` INSERT fails. Both functions revoke PUBLIC/anon/authenticated execute rights and grant only to `service_role`. **Apply:** paste `artifacts/api-server/src/migrations/0109_claim_invite_link_slot.sql` into the Supabase SQL editor and run (both `CREATE OR REPLACE FUNCTION` statements are idempotent). Must be applied before 0110. **Verify:** `SELECT proname FROM pg_proc WHERE proname IN ('claim_invite_link_slot', 'release_invite_link_slot');` — both names must appear. The pre-release `db-triggers` check confirms these are live before any EAS build. | 2026-07-05 |
| `0110_invite_link_idempotency.sql` | Creates `trip_invite_link_attempts` table (PK `link_id`+`user_id`, `claimed_at`; service-role-only access) and replaces the two-step claim with `claim_invite_link_slot_for_user(link_id, user_id) RETURNS text`. The combined function atomically: inserts the attempt row + increments `use_count` (returns `'claimed'`), or returns `'already_attempted'` if a prior request already claimed a slot that never completed (so the handler can skip re-claiming and retry the INSERT), or returns `'limit_reached'` when `max_uses` is exhausted. Both table and function are SECURITY DEFINER / service-role-only. **Apply:** paste `artifacts/api-server/src/migrations/0110_invite_link_idempotency.sql` into the Supabase SQL editor and run — must be applied after 0109. **Verify:** `SELECT relname FROM pg_class WHERE relname = 'trip_invite_link_attempts' AND relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'public');` — the table name must appear. Also `SELECT proname FROM pg_proc WHERE proname = 'claim_invite_link_slot_for_user';` — the function name must appear. | 2026-07-05 |
| `0113_cleanup_stale_invite_attempts.sql` | Creates `cleanup_stale_invite_link_attempts() RETURNS TABLE (link_id, user_id, claimed_at, trip_id)` SECURITY DEFINER function. Removes `trip_invite_link_attempts` rows where a matching `trip_members` row already exists for that trip+user — these arise when `clearAttempt()` fails silently after a successful join. Unlike `reconcile_invite_link_slots` (0111), no min-age gate is needed: if the member row exists the attempt row is definitively stale. No use_count adjustment is made (the slot was legitimately used). Concurrent calls are safe (plain DELETE is idempotent per PK). Called by `POST /api/admin/trips/reconcile-invite-slots` alongside `reconcile_invite_link_slots`, adding `staleAttempts` + `stale` fields to the response. **Status: Applied to production** — verified 2026-07-05 via Supabase Management API: `SELECT proname FROM pg_proc WHERE proname = 'cleanup_stale_invite_link_attempts'` returns `[{"proname":"cleanup_stale_invite_link_attempts"}]`. **Apply (if missing):** paste `artifacts/api-server/src/migrations/0113_cleanup_stale_invite_attempts.sql` into the Supabase SQL editor and run — safe to run after 0110. **Verify:** `SELECT proname FROM pg_proc WHERE proname = 'cleanup_stale_invite_link_attempts';` — function name must appear. | 2026-07-05 |
| `0111_reconcile_invite_slots.sql` | Creates `reconcile_invite_link_slots(min_age_minutes integer DEFAULT 5) RETURNS TABLE (link_id, user_id, claimed_at, trip_id)` SECURITY DEFINER function. Finds stranded invite-link slots — `trip_invite_link_attempts` rows older than `min_age_minutes` with no corresponding `trip_members` row for that trip+user — and for each: decrements `use_count` (floors at 0) in `trip_invite_links` and deletes the orphaned attempt row. Uses `FOR UPDATE OF ... SKIP LOCKED` so concurrent reconciliation runs never double-fix. Called by `POST /api/admin/trips/reconcile-invite-slots` (admin-only) and by the automated `InviteSlotSweeper` background job (see below). **Status: Applied to production** — verified 2026-07-05 via Supabase Management API: `SELECT proname, pronargs FROM pg_proc WHERE proname = 'reconcile_invite_link_slots'` returns `[{"proname":"reconcile_invite_link_slots","pronargs":1}]`. **Apply (if missing):** paste `artifacts/api-server/src/migrations/0111_reconcile_invite_slots.sql` into the Supabase SQL editor and run — must be applied after 0110. **Verify:** `SELECT proname FROM pg_proc WHERE proname = 'reconcile_invite_link_slots';` — function name must appear. The pre-release `db-triggers` check confirms this function is live before any EAS build. | 2026-07-05 |
| `0112_trip_members_invite_link_id.sql` | Adds `invite_link_id UUID REFERENCES trip_invite_links(id) ON DELETE SET NULL` (nullable) to `trip_members`. Enables per-link audit queries ("how many current members joined via link X?"). All pre-existing rows keep `NULL`; new members who join via an invite link have the column set by the `POST /api/trips/invite-link/:token/accept` handler. Also creates partial index `trip_members_invite_link_id_idx` on `(invite_link_id) WHERE invite_link_id IS NOT NULL`. `ON DELETE SET NULL` ensures deleting an invite link does not remove its members. **Apply:** paste `artifacts/api-server/src/migrations/0112_trip_members_invite_link_id.sql` into the Supabase SQL editor and run — must be applied after 0110 (requires `trip_invite_links` table). **Verify:** `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name='trip_members' AND column_name='invite_link_id';` — one row must appear with `data_type='uuid'` and `is_nullable='YES'`. Also: `SELECT indexname FROM pg_indexes WHERE tablename='trip_members' AND indexname='trip_members_invite_link_id_idx';` — index name must appear. | 2026-07-05 |

### Invite-link stranded-slot cleanup — InviteSlotSweeper background job

The Postgres function added in migration 0111 (`reconcile_invite_link_slots`) is
called automatically by the `InviteSlotSweeper` background job in
`artifacts/api-server/src/lib/inviteSlotSweeper.ts`.  No additional migration is
required — the cleanup runs entirely in Node.js process space using the existing
service-role client.

**What it does**

On server startup (after a 60-second delay to let the service fully initialise)
and then on every subsequent interval, the sweeper calls:

```sql
SELECT * FROM reconcile_invite_link_slots(min_age_minutes => <TTL_MINUTES>);
```

For each row returned (a stranded attempt with no matching `trip_members` row)
the Postgres function has already:
1. Decremented `use_count` (floored at 0) in `trip_invite_links`.
2. Deleted the orphaned `trip_invite_link_attempts` row.

The sweeper logs the number of recovered slots at `INFO` level when any are
found, and at `DEBUG` level when the run finds nothing.  Failures are logged at
`ERROR` and tracked in `consecutiveFailures` on `getSweeperStatus()`.

**Configuration (env vars in `artifacts/api-server/.env`)**

| Env var | Default | Meaning |
|---|---|---|
| `INVITE_SLOT_SWEEP_TTL_HOURS` | `24` | Attempt rows older than this (with no member row) are cleaned up |
| `INVITE_SLOT_SWEEP_INTERVAL_HOURS` | `1` | How often the sweeper runs |

Lowering `INVITE_SLOT_SWEEP_TTL_HOURS` below the expected maximum request
latency (a few seconds) is unsafe — it would age out in-flight legitimate
requests.  The 24-hour default is deliberately generous.

**Manual trigger**

An on-demand reconcile is also available via:

```
POST /api/admin/trips/reconcile-invite-slots
{ "minAgeMinutes": 5 }   // optional; defaults to 5
```

This is the admin-only HTTP endpoint wired to the same Postgres function.

| `0108_rent_buddy_spec_tables.sql` | Adds three functional tables and four compatibility VIEW aliases. New tables: (1) `buddy_services` — typed service catalog per buddy (category, title, hourly/half-day/full-day rates, min/max hours, group size, approval flag); RLS: public read, owner write, service-role all. (2) `buddy_availability_exceptions` — structured per-date availability overrides with `buddy_exception_type` enum (`blocked`, `time_blocked`, `vacation`, `available_only`); RLS: public read, owner write, service-role all. (3) `buddy_booking_events` — immutable audit log of booking state transitions written by route handlers on accept/decline/start/complete/cancel; fields: `booking_id`, `actor_user_id`, `event`, `from_status`, `to_status`, `metadata JSONB`; RLS: parties to booking can read, service-role all. VIEW aliases: `buddy_booking_checkins → rent_buddy_safety_checkins`, `buddy_change_requests → rent_buddy_route_change_requests`, `buddy_favorites → rent_buddy_saved`, `buddy_booking_requests → rent_buddy_bookings`. **Apply:** paste `artifacts/api-server/migrations/0108_rent_buddy_spec_tables.sql` into the Supabase SQL editor and run. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name IN ('buddy_services','buddy_availability_exceptions','buddy_booking_events') ORDER BY table_name;` — all three names must appear. `SELECT table_name FROM information_schema.views WHERE table_name IN ('buddy_booking_checkins','buddy_change_requests','buddy_favorites','buddy_booking_requests');` — all four view names must appear. **Correction 2026-07-16:** this migration was never actually applied — its `ALTER TABLE ... ADD CONSTRAINT IF NOT EXISTS` is invalid PostgreSQL syntax and would have failed. A fixed version (guarded DO block) was applied as part of `0134_rent_buddy_schema_rebuild.sql`. | recorded 2026-07-05 but never applied — see 0134 |

| `0115_circle_visibility_settings.sql` | Non-canonical origin file for `circle_visibility_settings`. **Now tracked in canonical** `0108_circle_schema_tracked.sql` — apply that file instead. | tracked in 0108 |
| `0116_circle_context_settings.sql` | Non-canonical origin file for `circle_context_settings`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0117_beta_feature_flags.sql` | Seeds all beta-phase feature-gate and kill-switch flags into `feature_flags` using `ON CONFLICT (flag) DO NOTHING` so re-runs are safe and existing rows are never overwritten. Feature gates (enabled = true): `rent_buddy_enabled`, `safe_return_live_share_enabled`, `find_your_circle_enabled`, `compass_ai_enabled`, `hidden_gems_enabled`, `push_notifications_enabled`. Kill switches (enabled = false): `disable_signups`, `disable_posting`, `disable_messaging`, `disable_rent_buddy_booking` (blocks `POST /api/rent-a-buddy/bookings`), `city_launch_mode`, `invite_only_beta`. Kill-switch enforcement: `disable_posting` → `POST /posts` (posts.ts); `disable_messaging` → `POST /threads/:id/messages` (messaging.ts); `disable_rent_buddy_booking` → `POST /api/rent-a-buddy/bookings` (rentABuddy.ts); `disable_signups` + `invite_only_beta` → `GET /api/auth/signup-status` (auth.ts — mobile app must call this before Supabase Auth sign-up). All gates are fail-open. Ban enforcement: `requireUser` (lib/http.ts) now reads `profiles.account_status` and returns 403 for `banned` or `suspended` accounts; fail-open on DB error. Hide-content audit: `logModerationAction` now resolves the content owner's profile UUID first (falls back to admin UUID) so the FK to `profiles(id)` is always satisfied. Admin lookup: `GET /admin/users` now returns `onboardingStatus` from `compass_analytics`. Full kill-switch audit in `docs/beta-db-audit.md`. **Apply:** paste `artifacts/api-server/src/migrations/0117_beta_feature_flags.sql` into the Supabase SQL editor and run. **Verify:** `SELECT flag, enabled FROM feature_flags WHERE flag IN ('rent_buddy_enabled','disable_signups','disable_posting','disable_messaging','disable_rent_buddy_booking','invite_only_beta') ORDER BY flag;` — all 6 rows must appear. Applied via Supabase Management API; verify query returned all 6 flags (kill switches false, `rent_buddy_enabled` true). | 2026-07-16 |
| `0117_circle_presence.sql` | Non-canonical origin file for `circle_presence`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0118_circle_checkins.sql` | Non-canonical origin file for `circle_checkins`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0119_circle_member_visibility_overrides.sql` | Non-canonical origin file for `circle_member_visibility_overrides`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0120_circle_meeting_points.sql` | Non-canonical origin file for `circle_meeting_points`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0121_circle_audit_events.sql` | Non-canonical origin file for `circle_audit_events`. **Now tracked in canonical** `0108_circle_schema_tracked.sql`. | tracked in 0108 |
| `0123_engagement_user_indexes.sql` | Adds five user-perspective indexes for engagement like/reaction tables — complements the post-perspective indexes from migration 0106. `idx_posts_likes_user_created` on `posts_likes (user_id, created_at DESC)`, `idx_post_reactions_user_created` on `post_reactions (user_id, created_at DESC)`, `idx_comment_likes_user_created` on `comment_likes (user_id, created_at DESC)`, `idx_highlight_likes_user_created` on `highlight_likes (user_id, created_at DESC)`, `idx_memory_likes_user_created` on `memory_likes (user_id, created_at DESC)`. All idempotent (IF NOT EXISTS). Covers the reverse lookup "which posts/comments/highlights/memories has a given user liked?" — used by profile pages and the 'liked by me' feed indicator; without these indexes Postgres must sequentially scan each like table filtered by user_id. **Apply:** applied via the Supabase Management API query endpoint. **Verify:** `SELECT indexname FROM pg_indexes WHERE indexname IN ('idx_posts_likes_user_created','idx_post_reactions_user_created','idx_comment_likes_user_created','idx_highlight_likes_user_created','idx_memory_likes_user_created');` — all five names must appear (verified 2026-07-16). | 2026-07-16 |
| `0124_trip_crew_location_sessions_drift.sql` | Fixes schema drift on `trip_crew_location_sessions` that made the 5-minute live-share sweeper fail with PGRST204 in production. Adds `status` (TEXT NOT NULL DEFAULT 'active', CHECK active\|stopped\|expired), `started_at` (TIMESTAMPTZ NOT NULL DEFAULT now()), `stopped_at` (TIMESTAMPTZ), `visibility_level` (TEXT NOT NULL DEFAULT 'neighborhood', CHECK city_only\|neighborhood\|nearby). Backfills status from `ended_at`/`expires_at`, adds `crew_loc_sessions_status_idx (status, expires_at)`, and widens the `trip_crew_location_events.event_type` CHECK to include `live_share_started`, `live_share_stopped`, `live_share_expired`, `access_revoked` (the event names the service actually writes). All idempotent. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='trip_crew_location_sessions';` — status/started_at/stopped_at/visibility_level must appear. | applied 2026-07-16 |
| `0130_rent_buddy_place_coords.sql` | Adds `lat DOUBLE PRECISION` and `lng DOUBLE PRECISION` to `rent_buddy_waitlist`, and to `rent_buddy_requests` when it exists (`ALTER TABLE IF EXISTS` — that table comes from `0048_rent_buddy_marketplace.sql`, whose `CREATE TABLE` now also includes lat/lng). Lets open buddy requests and waitlist entries created from a canonical picked place keep coordinates for proximity-aware matching/notifications. Idempotent. **Verify:** `SELECT table_name, column_name FROM information_schema.columns WHERE table_name IN ('rent_buddy_waitlist','rent_buddy_requests') AND column_name IN ('lat','lng');` | applied 2026-07-16 (as part of the 0134 rebuild — the same lat/lng ALTERs are included in `0134_rent_buddy_schema_rebuild.sql`; verified 4 rows via information_schema) |

### Stale migration file — `artifacts/api-server/migrations/0041_notifications.sql`

This file is **not applied to production** and is superseded by `0062_notifications_schema.sql` (applied 2026-06-28). The 0062 file is the authoritative notification schema: it adds `push_retry_queue`, uses `public.profiles(id)` foreign keys, and has the correct RLS policy names. Do not apply `0041_notifications.sql`.

| `0125_canonical_locations.sql` | Creates `canonical_locations` — the universal location registry backing the app-wide location picker. One row per real-world location; provider variants ("Cebu" / "Cebu City" / Foursquare venue ids) merge via `normalized_name` + kind-class + proximity rules in `src/lib/canonicalLocations.ts`. Columns: `id uuid PK`, `name`, `normalized_name`, `kind`, `country`, `country_code`, `region`, `lat/lng`, `aliases text[]`, `provider_ids jsonb`, timestamps. Indexes on `normalized_name` and `(kind, country_code)`. RLS: public read, service-role write. Server degrades gracefully if missing (resolve returns null canonicalId). **Applied 2026-07-15 via Supabase Management API** (`POST /v1/projects/{ref}/database/query` — note: direct psql and pooler connections are unreachable from this workspace; the Management API with `SUPABASE_ACCESS_TOKEN` is the working path). **Verify:** `SELECT tablename, rowsecurity FROM pg_tables WHERE tablename='canonical_locations';` — one row, rowsecurity true. | applied 2026-07-15 |
| `0126_canonical_locations_unique.sql` | Unique expression index `uq_canonical_locations_identity` on `canonical_locations (normalized_name, kind-class CASE, COALESCE(country_code,''))` — closes the concurrent find-then-insert race that could create two canonical rows for one real place. The resolver catches `23505` and re-matches once. The CASE must stay in sync with `kindClass()` in `src/lib/canonicalLocations.ts`. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT indexname FROM pg_indexes WHERE indexname='uq_canonical_locations_identity';` — one row. | applied 2026-07-15 |
| `0124_stamp_definitions_universal_artwork.sql` | Adds nullable `universal_artwork_url text` to `stamp_definitions` — the AI-generated universal stamp artwork image URL rendered by the mobile passport stamp collection. Server code degrades gracefully: stamp routes probe for the column and omit it from selects until this migration is applied, and the `/me/stamps` artwork enrichment is best-effort. **Apply:** paste `artifacts/api-server/src/migrations/0124_stamp_definitions_universal_artwork.sql` into the Supabase SQL editor and run. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='stamp_definitions' AND column_name='universal_artwork_url';` — one row must appear. | applied 2026-07-16 |
| `0127_layover_system.sql` | Recreates the full layover backend that was never applied (original 0044 sat in the stale migrations dir): `airport_profiles` (+`canonical_location_id`), `layover_sessions` (+`canonical_city_id`, `share_city_status`, `return_reminder_at`), `layover_recommendations`, `layover_events` (extended event CHECK list), NEW `layover_plan_stops` (mini-itinerary, owner-only RLS via session join), share partial index, and feature-flag seeds using the correct `flag` column (all 5 airport/layover flags enabled). **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name LIKE $$layover| `0127_layover_system.sql` | Recreates the full layover backend that was never applied (original 0044 sat in the stale migrations dir): `airport_profiles` (+`canonical_location_id`), `layover_sessions` (+`canonical_city_id`, `share_city_status`, `return_reminder_at`), `layover_recommendations`, `layover_events` (extended event CHECK list), NEW `layover_plan_stops` (mini-itinerary, owner-only RLS via session join), share partial index, and feature-flag seeds using the correct `flag` column (all 5 airport/layover flags enabled). **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name LIKE 'layover%';` — 4 rows incl. `layover_plan_stops`. | applied 2026-07-15 |

| `0128_posts_canonical_location.sql` | Adds nullable `canonical_location_id uuid` (FK → `canonical_locations.id`, ON DELETE SET NULL) + partial index `idx_posts_canonical_location` on `posts` — the universal-registry reference written by the postcard composer's location picker. The postcards create route degrades gracefully (retries without the column on PGRST204) so unmigrated environments keep posting. Root-cause context: POST /api/postcards previously wrote nonexistent `event_id`/`place_id` columns → PGRST204 → every composer post failed with a raw db_error banner; provider place refs now map to `location_place_id`. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='posts' AND column_name='canonical_location_id';` — one row. | applied 2026-07-15 |
| `0129_post_media_stamp_overlay.sql` | Adds nullable `stamp_overlay` jsonb to `post_media` — optional decorative passport-stamp overlay metadata written at upload-complete (stamp definition id, artwork URL pinned server-side at publish, render style, normalized position/scale/rotation/opacity). Overlay is non-destructive (original photo untouched); complete never fails because of overlay problems (ineligible/unavailable stamps are skipped with a warning flag), and feed projections probe for the column so unmigrated environments degrade gracefully. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='post_media' AND column_name='stamp_overlay';` — one row. | applied 2026-07-15 |
| `0130_user_account_states_updated_at.sql` | Adds `updated_at timestamptz NOT NULL DEFAULT now()` to `user_account_states` — POST /api/me/deactivate and /me/reactivate upsert this column but migration 0063 never created it, so every deactivate 500'd (PGRST204). **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='user_account_states' AND column_name='updated_at';` — one row. | applied 2026-07-15 |
| `0131_location_mode_check_update.sql` | Widens `location_preferences_location_mode_check` from the legacy set ('precise','city','off') to the API vocabulary ('off','city_only','nearby','live_during_activity','trusted_circle_live') plus legacy values — every PATCH /api/me/location-preferences mode change violated the old CHECK and 500'd. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='location_preferences_location_mode_check';` — must list city_only/nearby/etc. | applied 2026-07-15 |
| `0132_passport_visibility_prefs_columns.sql` | Adds the six columns the visibility-preferences routes actually use (`default_stamp_visibility`, `default_memory_visibility`, `show_city_map`, `show_neighborhoods`, `show_plan_stamps`, `show_safe_return_stamps`) to `passport_visibility_preferences`, which migration 0042 created with only legacy columns (stamps_visible/memories_visible/map_visible) — every PATCH /api/me/passport/visibility-preferences 500'd with PGRST204. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='passport_visibility_preferences' AND column_name IN ('default_stamp_visibility','show_city_map');` — two rows. | applied 2026-07-15 |
| `0133_rent_buddy_availability_alignment.sql` | Fixes buddy vacation/blocked-date persistence end-to-end. (1) Recreates `rent_buddy_availability` in the per-date-slots shape the dashboard routes use (live table kept a pre-0047 legacy shape — `date_from`/`date_to`/`is_blocked` — so 0047's CREATE TABLE IF NOT EXISTS silently no-oped; legacy table verified empty before drop, guarded with a RAISE EXCEPTION otherwise). (2) Creates `buddy_availability_exceptions` (+`buddy_exception_type` enum, RLS, UNIQUE(buddy_id,exception_date), range index) — the table `POST /api/rent-a-buddy/bookings` consults to reject bookings on blocked/vacation dates; carried over from unapplied 0108_rent_buddy_spec_tables.sql minus its invalid `ADD CONSTRAINT IF NOT EXISTS` statement. (3) Adds `available_now`, `available_now_until`, `min_notice_hours`, `buffer_minutes`, `max_bookings_per_day` to `rent_buddy_profiles`, backing the new `PATCH /api/rent-a-buddy/dashboard/availability/settings` route. **Applied 2026-07-15 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_availability' AND column_name='date';` — one row; `SELECT 1 FROM information_schema.tables WHERE table_name='buddy_availability_exceptions';` — one row. | applied 2026-07-15 |
| `0134_rent_buddy_schema_rebuild.sql` | Full rent_buddy schema rebuild fixing the drifted live DB. The live `rent_buddy_*` tables kept a pre-0047 legacy shape (`rent_buddy_profiles` had 14 old columns — no `status`/`admin_status`/`display_name`/`city`/`categories`/`hourly_rate_usd`; `rent_buddy_bookings` used `date_from`/`date_to`), so buddy sign-up, search, and bookings all failed against production. All drifted tables verified empty (a run-time RAISE EXCEPTION guard re-verifies), then dropped with CASCADE and rebuilt from the canonical chain: 0047 → 0048_rollout → 0048_marketplace → 0051 → 0107 → 0108 (with its invalid `ADD CONSTRAINT IF NOT EXISTS` replaced by a guarded DO block) → 0109 → 0110 → 0111 → 0112 → 0113 → 0133. Kept (had data): `rent_buddy_city_rollouts` (+ added missing `country`, `status_changed_at`, `status_changed_by`, `target_launch_date`, `buddy_cap`), `rent_buddy_fee_rules`, `rent_buddy_global_controls` (+ added `updated_by_admin_id`); their RLS policies are dropped/recreated. Trailing route-alignment ALTERs add columns routes select but no prior migration created: `rent_buddy_profiles.completed_count/cancel_count/no_show_count/favorites_count/expo_push_token`, and realign `rent_buddy_admin_actions` to the 0107 shape (nullable `admin_id`, TEXT `target_id`, `details JSONB`). **Applied 2026-07-16 via Supabase Management API, one PART per request (0112 split so `ALTER TYPE ... ADD VALUE` commits before the new value is used).** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_profiles' AND column_name IN ('status','admin_status','display_name','city','categories','hourly_rate_usd','favorites_count');` — 7 rows; `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_bookings' AND column_name IN ('booking_date','start_time','duration_h');` — 3 rows. E2E verified: buddy profile insert, `BUDDY_PUBLIC_COLUMNS` select, and booking insert all succeed via PostgREST. | applied 2026-07-16 |
| `0135_rent_buddy_meetup_base_coords.sql` | Adds nullable `meetup_base_lat`/`meetup_base_lng` (DOUBLE PRECISION, range CHECKs) to `rent_buddy_profiles` — a buddy-chosen, neighbourhood-level meetup-base pin used by `POST /api/rent-a-buddy/search` for per-buddy distance chips (falls back to city-centre geocoding when unset). Set via `PATCH /api/rent-a-buddy/me/profile` (`meetupBaseLat`/`meetupBaseLng`, both-or-neither, null clears). **Note:** the column is in the buddy search select list, so environments missing it fail search entirely (probe added to schemaDriftCheck). **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='rent_buddy_profiles' AND column_name LIKE 'meetup_base%';` — two rows. | applied 2026-07-16 |
| `0136_stamp_queue_requeue_cap.sql` | Adds `requeue_count int NOT NULL DEFAULT 0` to `stamp_generation_queue`, extends the status CHECK with terminal `permanently_failed`, and recreates `uix_queue_catalog_active` so permanently_failed rows don't count as "active" (otherwise admin regenerate could never enqueue a new job). Worker auto-requeue (`requeueStaleFailedJobs`) increments `requeue_count` each round and moves jobs at the cap (`STAMP_FAILED_REQUEUE_MAX_ROUNDS`, default 3) to `permanently_failed`; manual admin re-queue resets both `attempts` and `requeue_count` to 0. **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='stamp_generation_queue' AND column_name='requeue_count';` — one row. | applied 2026-07-16 |
 CHECK, resolved_at, updated_at). Second-level persistent cache for `countryGeocoder.geocodeCityCountry` so positive city→country forward-geocode results survive server restarts instead of re-hitting Nominatim (1 req/sec fair-use). Only positive results are persisted; negative/failed geocodes stay in the short-TTL in-memory cache so transient failures retry. RLS: service_role only. **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='city_country_geocode_cache';` — five rows. | applied 2026-07-16 |
| `0137_notification_prefs_timezone.sql` | Adds nullable `timezone TEXT` (IANA name, e.g. `Europe/Lisbon`) to `notification_preferences`. Quiet hours (`NotificationPreferenceService.isQuietHour`) are now evaluated in this timezone; NULL/invalid falls back to server-local time (previous behaviour). Set via `PUT /api/me/notification-preferences` (`timezone`, validated as a real IANA zone, nullable to clear). **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='notification_preferences' AND column_name='timezone';` — one row. | applied 2026-07-16 |
| `0138_trips_reminder_sent_at.sql` | Adds nullable `reminder_sent_at timestamptz` to `trips` — persistent dedup for the 24h trip-tomorrow reminder. `tripReminderScheduler` now atomically claims each trip (`UPDATE ... WHERE reminder_sent_at IS NULL RETURNING id`) before pushing, so a server restart inside the 22–26h window can no longer re-send the reminder. **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='trips' AND column_name='reminder_sent_at';` — one row. | applied 2026-07-16 |

### Production verification — settings-save fixes (0130/0131/0132/0108) — 2026-07-16

All four settings-save migrations were verified as applied to the production Supabase database via the Management API (task: apply the three settings-save fixes to production):

- `user_account_states.updated_at` exists (0130)
- `location_preferences_location_mode_check` allows `('off','city_only','nearby','live_during_activity','trusted_circle_live','precise','city')` (0131)
- All six new `passport_visibility_preferences` columns exist (0132)
- `circle_visibility_settings` table exists (0108)

A signed-in smoke check (`artifacts/api-server/scripts/smoke-settings-saves.mjs`, ephemeral Supabase user) confirmed deactivate, reactivate, location-mode, passport visibility-preferences, and circle-consent saves all return 200. No SQL needed re-applying — the earlier Management API applies had already reached this database.
| `0135_rent_buddy_reliability_counter_functions.sql` | Adds two SECURITY DEFINER SQL functions for atomic buddy reliability counter updates: `rb_adjust_buddy_counter(p_buddy_id, p_column, p_delta)` — single-statement `SET col = GREATEST(0, col + delta)` with a column whitelist (`completed_count`, `cancel_count`, `no_show_count`); and `rb_sync_favorites_count(p_buddy_id)` — DB-side recount of `rent_buddy_saved` into `favorites_count`. Execute granted to `service_role` only. Route handlers call these via RPC (see `src/services/rentBuddy/ReliabilityCounters.ts`) so concurrent booking events can't lose increments. **Verify:** `SELECT proname FROM pg_proc WHERE proname IN ('rb_adjust_buddy_counter','rb_sync_favorites_count');` — 2 rows. | applied 2026-07-16 via Supabase Management API (verified 2 rows in pg_proc) |
| `0141_geocode_cache_deleted_at.sql` | Adds nullable `deleted_at timestamptz` to `city_country_geocode_cache` as a soft-delete tombstone. The admin DELETE handler sets this column instead of hard-deleting the row; the background correction sweep queries rows where `deleted_at IS NOT NULL`, evicts matching in-memory entries on all instances, then hard-deletes the tombstoned rows. Also adds partial index `city_country_geocode_cache_deleted_at_idx` on `(deleted_at) WHERE deleted_at IS NOT NULL` so the sweep only scans tombstoned rows. Without this migration the admin DELETE endpoint 500s (unknown column). **Applied 2026-07-16 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='city_country_geocode_cache' AND column_name='deleted_at';` — one row; `SELECT indexname FROM pg_indexes WHERE indexname='city_country_geocode_cache_deleted_at_idx';` — one row. | applied 2026-07-16 |

| 0143 | passport_tab_order | applied 2026-07-16 | ADD COLUMN profiles.passport_tab_order TEXT[] — owner's preferred passport tab order; null = canonical |
| `0142_stamp_queue_cleanup_error.sql` | Adds nullable `cleanup_error TEXT` and `cleanup_error_paths TEXT[]` to `stamp_generation_queue` so orphan-cleanup failures are recorded on the row, not just logs. **Applied 2026-07-17 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='stamp_generation_queue' AND column_name LIKE 'cleanup%';` — two rows. | applied 2026-07-17 |
| `0144_universal_stamp_catalog_country_code_nullable.sql` | Drops NOT NULL from `universal_stamp_catalog.country_code` so country-less catalog entries (modeled as `string | null` in `CatalogEntryForPrompt`) insert cleanly instead of failing in production; existing "XX" sentinel rows unaffected. **Applied 2026-07-17 via Supabase Management API.** **Verify:** `SELECT is_nullable FROM information_schema.columns WHERE table_name='universal_stamp_catalog' AND column_name='country_code';` — `YES`. | applied 2026-07-17 |
| `0145_event_first_stamp_definitions.sql` | Inserts `first_event_joined` and `first_event_hosted` rows into `stamp_definitions` (awarded by event RSVP/publish routes); idempotent via ON CONFLICT (slug) DO NOTHING. **Applied 2026-07-17 via Supabase Management API.** **Verify:** `SELECT slug FROM stamp_definitions WHERE slug IN ('first_event_joined','first_event_hosted');` — two rows. | applied 2026-07-17 |
| `20260726_compass_sense.sql` | Phase 11 Compass Sense: `compass_sense_settings` (per-user presence level passive/aware/active + per-category permission JSONB) and `compass_sense_nudges` (durable nudge log used for dedupe + daily caps), with RLS (own-rows) and user/dedupe indexes. **Applied 2026-07-20 via Supabase Management API.** **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name LIKE 'compass_sense%';` — two rows. | applied 2026-07-20 |
| `20260728_compass_autopilot.sql` | Phase 13 Trip Autopilot: adds `lock_type` (fixed/flexible/optional, default flexible, CHECK) to `trip_plan_items`; creates `trip_autopilot_settings` (per user per trip permission grants: enabled, allow_move_flexible, allow_move_optional, allow_remove_optional) and `trip_autopilot_proposals` (durable propose-never-execute change proposals: issue_type, severity, reason, before/after `changes` JSONB, dedupe_key, status pending/confirmed/declined/expired) with own-row RLS, trip/user indexes, and a partial unique index on `(trip_id, user_id, dedupe_key) WHERE status='pending'` for dedupe. **Applied 2026-07-21 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='trip_plan_items' AND column_name='lock_type';` plus `SELECT table_name FROM information_schema.tables WHERE table_name LIKE 'trip_autopilot%';` — two tables. | applied 2026-07-21 |
| `20260730_compass_intelligence_graph.sql` | Phase 15 Travel Intelligence Graph: creates `compass_graph_nodes` (typed nodes person/place/event/trip/city/time_slice/vibe/behavior/outcome, UNIQUE (node_type,node_key)), `compass_graph_edges` (typed edges keyed by (src,dst,edge_type) with weight/observed_count/first_seen/last_seen), `compass_city_models` (per-city Destination World Model: dow×daypart time_slices, monthly buckets, top categories, sample_size), `compass_city_confidence` (depth_score 0–100, tier deep/moderate/thin, aggregate signals). All four tables service-role-only RLS; privacy guards apply at read time in CompassGraphEngine. **Applied 2026-07-21 via Supabase Management API.** **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name IN ('compass_graph_nodes','compass_graph_edges','compass_city_models','compass_city_confidence');` — 4 rows. | applied 2026-07-21 |
| `20260729_compass_outcome_learning.sql` | Phase 14 Outcome Learning: creates `compass_outcome_events` (full recommendation outcome chain viewed/saved/went/stayed/liked/invited/made_memory/returned, each row tied to `compass_served_recommendations` via `recommendation_id`; `predicted_match` snapshots the delivery-time Compass Match; `stage_value` carries value-delivered points) with UNIQUE (user_id, recommendation_id, stage), user/time + rec indexes, own-row read RLS and service-role write policy. **Applied 2026-07-21 via Supabase Management API.** **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_name='compass_outcome_events';` — 10 columns. | applied 2026-07-21 |
| `0146_append_cleanup_error_paths_fn.sql` | Adds SECURITY DEFINER SQL function `append_stamp_cleanup_error_paths(p_job_id, p_error, p_paths)` — atomic server-side append+dedup into `stamp_generation_queue.cleanup_error_paths`. Used by `persistCleanupError` as a fallback when the read of the existing paths fails (DB read outage) so orphaned-file paths survive a worker restart instead of living only in logs. Execute granted to `service_role` only. **Applied 2026-07-17 via Supabase Management API.** **Verify:** `SELECT proname FROM pg_proc WHERE proname='append_stamp_cleanup_error_paths';` — one row. | applied 2026-07-17 |

### Legacy migrations dir reconciled — 2026-07-17

The pre-`src/` dir `artifacts/api-server/migrations/` was fully classified so `pnpm run audit:schema -- --include-legacy` exits 0 and real drift can no longer hide among superseded noise.

**Superseded (skipped/allowlisted in `auditMigrationsVsLive.ts`):**
- `0041_notifications.sql` → SKIP_FILES (superseded by canonical `0062_notifications_schema.sql`; live has equivalently-named policies, `notification_category_preferences` intentionally has no `id`).
- `0037_feature_flags.sql` policy `ff_select_all` → allowlisted (live `feature_flags_public_read` has the same predicate).
- `0035` policy `pgf_select_member` → allowlisted (dropped by legacy `0038` RLS fix; replaced by `pgf_select_accepted`).

**Applied 2026-07-17 via Supabase Management API** (each file one request; runner emitted explicit `ALTER TABLE ADD COLUMN IF NOT EXISTS` preludes where the live table already existed so `CREATE TABLE IF NOT EXISTS` would have no-opped): `0012_group_chat`, `0013_availability_meetups` (incl. `trip_availability`), `0030_message_reports`, `0031_thread_reports`, `0032_location_preferences` (`user_location_preferences`), `0033_location_sessions` (+10 `location_sessions` cols), `0034_geo_zones` (`place_profiles`, +5 `geo_zones` cols), `0035`/`0038`/`0039` plan-geofence cluster (+`plan_geofences`/`plan_checkins`/`plan_attendance_events`/`geofence_admin_settings` cols, unique `(geofence_id,user_id)` index for the code's upsert), `0036_pulse_geo_tags` (+9 cols), `0041_trip_crew_location` (+prefs/sessions cols), `0042_passport_stamps` (+`passport_stamps`/`passport_memories`/`passport_contribution_events` cols), `0043_trust_engine` (7 trust tables + `trust_restrictions` cols), `0044_hashtag_reports`, `0055_compass_admin` (6 tables), `0062_discovery_place_saves`, `0075_stamp_artwork`, `0114_review_moderation`, `0139_geocode_cache_corrected_at`, `20260621_weather_cache`. All affected drifted tables had 0 rows, so additive reconciliation was safe.

**Live-shape adaptations made while applying (files left untouched as historical record):**
- `feature_flags` seeds: `key` → live column `flag`.
- `0041_trip_crew_location` policy: `allowed_member_ids` is `uuid[]` live, so `auth.uid()::text = ANY(...)` → `auth.uid() = ANY(...)`.
- `0042` index `passport_stamps_user_earned_idx` created on live `awarded_at` (legacy `earned_at` remains allowlisted). Note: `CREATE INDEX IF NOT EXISTS` validates columns even when it skips creation.
- `0013` circle policies: legacy `circle_memberships(owner_id, member_id)` → live `(user_id, other_id)` (membership is symmetric, semantics preserved).
- Canonical-vs-legacy NOT NULL conflicts relaxed on empty tables so legacy-shaped inserts work: `pulse_geo_tags.tag_type`, `plan_checkins.plan_geofence_id`, `plan_attendance_events.plan_geofence_id` → nullable.
- Stale allowlist entry `passport_memories.trip_id` removed (column now exists live).

**Verify:** `cd artifacts/api-server && pnpm run audit:schema -- --include-legacy` — exits 0.

## 2026-07-23 — Trip Brain + Stamp Premium waves (0166–0177)

Applied in Supabase SQL editor by owner on 2026-07-23 (all re-run-safe):

- `0166_feature_flags_reseed` — reseeds all flags through the live `flag` column (ON CONFLICT DO NOTHING).
- `0167_safety_ddl_reconcile` — Safe Return + emergency-contacts DDL reconcile. Historical note: initial cut carried a legacy `key`-column flag INSERT; removed (0166 seeds those flags), fixed file is canonical.
- `0168_discovery_cache` — discovery_cache + discovery_geocode_cache.
- `0169_passport_entry_intelligence` — traveler_passports, trip_traveler_passports, entry_requirements (confidence: curated|provider honesty tiers), flag `passport_entry_intelligence_enabled`.
- `0170_trip_readiness` — trip_readiness_items + `trip_readiness_enabled`.
- `0171_price_baselines` — price_baselines + `budget_intelligence_enabled`.
- `0172_trip_reservations` — trip_reservations + `reservation_import_enabled` + `nl_trip_creation_enabled`.
- `0173_neighborhoods` — neighborhood_areas + trip_area_preferences + `neighborhood_match_enabled`.
- `0174_reference_data` — fx_rates + country_metadata (bulk-loaded 2026-07-23: 29 currencies, 250 countries; entry_requirements bulk-loaded with 39,401 provider-confidence corridors, curated rows preserved).
- `0175_trip_readiness_snapshots` — daily readiness score snapshots (previousScore delta), added by the Replit UI wave.
- `0176_moderation_reports` — canonical DDL for the UI wave's moderation_reports (was ad-hoc only).
- `0177_stamp_premium_foundation` — Stamp Wave 1: destination_identities (+5 launch seeds), catalog identity_key, stamp_definitions premium fields (template_family, edition_size, is_limited, display_priority) + unified 5-tier rarity CHECK (adds 'epic'), stamp_artwork_versions QC/thumbnail/hero/composition columns, flag `stamp_premium_rendering_enabled` (default FALSE — worker keeps legacy path until enabled).
- `0178_stamp_showcase_admire` — Stamp Wave 2: user_stamp_showcase (≤8 curated, ranked; cap enforced in API), stamp_admires (unique per admirer+stamp), flags `stamp_showcase_enabled` + `stamp_admire_enabled` (default FALSE).
- `0179_stamp_criteria_engine` — Stamp Wave 3: activates the dormant stamp_definitions.criteria jsonb via a versioned rule engine. Flag `stamp_criteria_engine_enabled` (default FALSE). Seeds parity criteria onto community_connector/popular_traveler/travel_influencer/road_warrior/frequent_flyer (mirror existing hard-coded thresholds — no behavior change), and adds 4 event-category definitions (foodie_explorer, music_lover, outdoor_adventurer, event_regular) as is_active=FALSE automatic criteria stamps. Engine is additive: gates awards only when a definition has authored criteria AND the flag is on; hard-coded award sites remain authority otherwise.
- `0180_activate_event_category_stamps` — Stamp Wave 3 follow-up: activates foodie_explorer/music_lover/outdoor_adventurer/event_regular (0179 left them inactive). Still gated by stamp_criteria_engine_enabled (evaluateAndAwardCriteria no-ops when off), so the engine flag becomes the single switch. RSVP-going now derives event category context (category+tags keyword match) and evaluates these 4 via the criteria engine.
- `0181_stamp_unified_view` — Stamp legacy unification (read-layer): flag stamp_unified_view_enabled (default FALSE). UnifiedStampService merges v1 passport_stamps (GPS) + v2 user_stamps (achievements) into one deduped read collection (dedup by catalog_id backfilled by reconcile, else stamp_type|country|city; v2 wins ties). GET /stamps/me/unified always available; when flag ON the passport-card stampCount reports the deduped v1+v2 total instead of v1-only. NO schema change, NO data migration, both write paths untouched.
- `0182_country_essentials` — Country essentials (curated travel-readiness reference): plug_types, voltage, frequency, drive_side, emergency (jsonb) per country_essentials table (54 countries seeded), confidence='curated' + source + last_verified_at. Flag country_essentials_enabled (default FALSE). Endpoints GET /countries/:code/essentials + GET /trips/:tripId/essentials (DB-first, in-code curated fallback, honest-null for uncovered countries, confirm-on-arrival disclaimer on every response). Plug/voltage engineering-standardized; emergency numbers curated w/ disclaimer.
- `0183_budget_fx_conversion` — Budget FX: flag budget_fx_conversion_enabled (default FALSE). GET /trips/:tripId/cost-estimate?home=<ISO4217> now attaches a `converted` block (perDay/total bands in home currency) via lib/fx.ts (EUR-pivot over fx_rates, indicative disclaimer, null when rate unavailable — never fabricated). Source-currency estimate unchanged. Daily refresh scheduler (lib/fxRefreshScheduler.ts, env FX_REFRESH_ENABLED=true) re-pulls ECB rates from frankfurter.dev into fx_rates. Mobile: fetchCostEstimateWithFx (additive; fetchCostEstimate unchanged).
- `0184_fsq_places` — FSQ OS Places (Foursquare) provider POI layer: fsq_places (per-city ingested, category accommodation|nightlife|food|culture|shopping|other, provider confidence + dataset_date) + fsq_city_ingests + flag fsq_places_enabled. Ingested by scripts/load-fsq-city.mjs (DuckDB per-city bbox extract from the gated FSQ parquet — NOT whole-world). GET /cities/:cityKey/places?category (flag-gated, FSQ attribution required). Adds hotels + richer nightlife/food/retail beyond OSM. Category map + transform shared lib/fsq/*. **Applied 2026-07-24 via Supabase Management API.** **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_name IN ('fsq_places','fsq_city_ingests');` — 2 rows; `SELECT flag,enabled FROM feature_flags WHERE flag='fsq_places_enabled';` — 1 row with enabled=false. See docs/fsq-ingestion-runbook.md for activation steps.
- `2042_stamp_auto_approve_artwork_flag` — Adds `stamp_auto_approve_artwork` feature flag (default FALSE) to feature_flags. **Applied 2026-07-28 via Supabase Management API.** **Verify:** `SELECT flag, enabled FROM feature_flags WHERE flag = 'stamp_auto_approve_artwork';` — 1 row with enabled=false.
- `2045_posts_canonical_place_id` — Adds `canonical_place_id uuid` (FK → places, ON DELETE SET NULL) to `posts` and `post_media`; creates `place_mismatch_reports` table with pending/resolved lifecycle and unique-pending index (post_id, reporter_id). Enables the backfill worker, wrong-place report flow, and admin resolution routes. **Applied 2026-07-28 via Supabase Management API.** **Verify:** `SELECT column_name, table_name FROM information_schema.columns WHERE table_schema = 'public' AND table_name IN ('posts','post_media') AND column_name = 'canonical_place_id';` — 2 rows; `SELECT table_name FROM information_schema.tables WHERE table_schema = 'public' AND table_name = 'place_mismatch_reports';` — 1 row.
- `0195_rls_privacy_baseline` (file named `0195_rls_privacy_baseline.sql`) — SELECT RLS policies for profiles, trips, trip_activity_log, profile_views. **Applied 2026-07-26 via Supabase Management API.** Notes: `profiles_select` and `trips_select` already existed with more comprehensive live policies (`viewer_is_blocked` + friendship check on profiles; `can_see_trip()` function on trips) — those were preserved as-is. `trip_activity_log_select` was replaced: widened from owner/co_host roles only to all accepted members (`status = 'accepted'`). `profile_views_select` was created new (table had RLS enabled but no SELECT policy). **Verify:** `SELECT c.relname, p.polname FROM pg_class c JOIN pg_policy p ON p.polrelid = c.oid WHERE c.relname IN ('profiles','trips','trip_activity_log','profile_views') AND p.polname LIKE '%select%' ORDER BY c.relname;` — 4 rows.

## 2026-08-01 — ALLOWLIST burn-down wave (2036–2058 + 20260808–20260813)

All migrations applied via Supabase Management API. Both `check:missing-live-columns` and `check:write-path-columns` pass with a shorter ALLOWLIST after this wave.

- `20260808_header_image_privacy.sql` — Adds `profiles.show_profile_picture_publicly BOOLEAN NOT NULL DEFAULT true`. (`events.show_header_publicly` and `trips.show_header_publicly` were already live from the 20260806 private-buckets migration.) **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='profiles' AND column_name='show_profile_picture_publicly';` — 1 row.
- `20260812_posts_publish_at.sql` — Adds `posts.publish_at TIMESTAMPTZ` + partial index `posts_publish_at_idx`. Supports delayed-posting privacy gate in mediaEligibility.ts. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='posts' AND column_name='publish_at';` — 1 row.
- `20260813_posts_restrictions.sql` — Adds `posts.geo_restriction TEXT`, `posts.age_restriction_enabled BOOLEAN NOT NULL DEFAULT false`, `posts.age_min SMALLINT`, `posts.age_max SMALLINT`. Supports fail-closed media feed eligibility filter. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='posts' AND column_name IN ('geo_restriction','age_restriction_enabled','age_min','age_max');` — 4 rows.
- `2036_events_cover_source.sql` — Adds `events.cover_source TEXT CHECK (cover_source IN ('user_upload','ai_generated'))`. Prevents AI images from overwriting user uploads. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='events' AND column_name='cover_source';` — 1 row.
- `2041_media_ranking_snapshots.sql` — Creates `media_ranking_snapshots` table (composite PK: viewer_id, item_id, session_id) for delivery-time ranking reason snapshots. Powers "Why This?" surface. RLS: viewers read own rows. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name='media_ranking_snapshots';` — 1 row.
- `2047_place_living_cache.sql` — Creates 5 tables: `place_living_cache` (SWR envelope, PK: place_id), `place_best_of` (precomputed collections), `place_top_contributors`, `place_ai_summaries`, `place_cache_invalidation_queue`. **Verify:** `SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('place_living_cache','place_best_of','place_top_contributors','place_ai_summaries','place_cache_invalidation_queue');` — 5.
- `2050_place_cache_queue_worker_cols.sql` — Extends `place_cache_invalidation_queue` with `status TEXT NOT NULL DEFAULT 'pending'`, `locked_until TIMESTAMPTZ`, `locked_by TEXT` + pending-status index. Enables pessimistic locking for the place collections worker. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='place_cache_invalidation_queue' AND column_name IN ('status','locked_until','locked_by');` — 3 rows.
- `2051_stamp_milestones.sql` — Creates `stamp_milestones` (PK: user_id, milestone_level; milestone_level IN (100,1000,10000)). Tracks which stamp count milestones each user has crossed; triggers push notification on insert. **Verify:** `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name='stamp_milestones';` — 1 row.
- `2052_content_stamps_memory_type.sql` — Extends `content_stamps_entity_type_check` to include `'memory'` and `'place'` entity types. Required for stamp buttons on memory detail screens. **Verify:** `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='content_stamps_entity_type_check';` — includes 'memory' and 'place'.
- `2053_discovery_places_canonical_location_id.sql` — Adds `discovery_places.canonical_location_id UUID REFERENCES places(id) ON DELETE SET NULL` + partial index. Already applied before this wave. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='discovery_places' AND column_name='canonical_location_id';` — 1 row.
- `2054_backfill_stamp_country_from_city.sql` — One-time backfill: updates `country=NULL` rows in `user_stamps` and `passport_stamps` using a static city→country map (220 cities). Idempotent (WHERE country IS NULL). **Verify:** `SELECT count(*) FROM user_stamps WHERE country IS NULL AND city IS NOT NULL;` — count drops after apply.
- `2055_content_stamps_migrated_from.sql` — Adds `content_stamps.migrated_from TEXT` + backfills rows from posts_likes/media_likes/post_reactions so Compass doesn't double-count migrated engagement signals. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='content_stamps' AND column_name='migrated_from';` — 1 row.
- `2056_places_header_image_generated_id.sql` (new migration file) — Adds `places.header_image_generated_id UUID REFERENCES generated_visuals(id) ON DELETE SET NULL` + partial index. Written by the place-image generation pipeline (src/lib/visuals/service.ts) and admin place-images route. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='places' AND column_name='header_image_generated_id';` — 1 row.
- `2057_hidden_gems_add_a_gem_cols.sql` (new migration file) — Adds `hidden_gems.accessibility TEXT`, `hidden_gems.crowd_level TEXT`, `hidden_gems.source_confirmation TEXT`, `hidden_gems.visibility TEXT NOT NULL DEFAULT 'public'`. Required by the Add-a-Gem submission form (HiddenGemService.ts). **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='hidden_gems' AND column_name IN ('accessibility','crowd_level','source_confirmation','visibility');` — 4 rows.
- `2058_viewer_creator_fatigue_expires_at.sql` (new migration file) — Adds `viewer_creator_fatigue.expires_at TIMESTAMPTZ` + partial index. Required by the admin fatigue-stats dashboard (GET /api/admin/ranking-config/fatigue-stats) which queries `.gt("expires_at", now)` to count active suppressions. **Verify:** `SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='viewer_creator_fatigue' AND column_name='expires_at';` — 1 row.

### Tables removed from SKIP_TABLES (now live in both check scripts)

The following tables existed in the live DB before or were created by this wave. Their entries have been removed from `SKIP_TABLES` in both `checkMissingLiveColumns.ts` and `checkWritePathColumns.ts`:

`portava_featured`, `place_image_reports`, `media_stamp_reactions`, `event_agenda_items`, `place_mismatch_reports`, `media_dedup_groups`, `media_dedup_memberships`, `place_coverage_buckets`, `post_bucket_ledger`, `content_stamps`, `media_events` (checkMissingLiveColumns only), `media_ranking_snapshots`, `place_living_cache`, `place_best_of`, `place_top_contributors`, `place_ai_summaries`, `place_cache_invalidation_queue`, `stamp_milestones`.

## 2026-08-10 — Live Pulse surface separation (0199)

- `0199_rank_events_live_pulse_surface.sql` — Widens `rank_events_surface_check` to add `'live_pulse'`, giving Live Pulse (`GET /api/pulse/live`) its own key space so its urgency-ordered serve rows stop stealing outcome attribution from genuine ranked `'pulse'` impressions of the same entity. Strictly additive over 0197's 11 values (none removed), so all existing rows revalidate and no current writer starts failing. DROP + ADD are wrapped in BEGIN/COMMIT because `ADD CONSTRAINT` revalidates every row — without the transaction a failed ADD after a committed DROP would leave `rank_events` with no surface constraint at all. **Pre-apply check:** live `rank_events` held only `pulse` (184,909), `compass` (12,738), `events` (5,200) — all inside the new list. **Applied 2026-08-10 via Supabase Management API.** **Verify:** `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='rank_events_surface_check';` — 12 values, includes `'live_pulse'`.
  - Constraint lineage: 0153 created it inline/unnamed (`pulse`,`discovery`,`events`), 0154 added `compass`, 0197 added search/nearby/story/event/trip/profile/explore, 0199 adds `live_pulse`.
  - Deliberately NOT included: `'living_page'` (written by `routes/rankEvents.ts`) and `'watch_feed'` (written by `routes/mediaFeed.ts`) are also outside the vocabulary and are likely being rejected in production today. Un-blocking them starts persisting currently-dropped signal — a product/data decision with its own corpus implications, so it belongs in its own migration.
  - Note: migrations `0197` and `0198` are not recorded in this file; this entry does not cover them.

## 2026-08-10 — BACKFILL of unrecorded live objects (0200) — **DO NOT APPLY**

- `0200_backfill_unrecorded_live_objects.sql` — **STATUS: BACKFILL, NOT PENDING. There is nothing to apply.** Every object it declares already exists in production and has for a long time. The file exists so a *clean rebuild from the migration chain* reproduces production; running it against current production is a no-op by construction (`CREATE OR REPLACE` with the exact live definition, plus `DROP`/`CREATE` of triggers already identical). Do not treat an "unapplied" status anywhere as work outstanding.
  - **Contents (4 functions + 2 triggers).** Functions, in this order because ordering is load-bearing: `is_accepted_trip_member` **first**, then `can_post_to_trip`, `can_see_post`, `can_see_postcard`. The latter three call the first, and with `check_function_bodies` on (the default) a `LANGUAGE sql` body is validated at `CREATE` time, so the reverse order fails a rebuild outright. Triggers: `trg_posts_updated` on `posts` and `trg_postcards_updated` on `passport_postcards`, both `BEFORE UPDATE ... EXECUTE FUNCTION set_updated_at()`.
  - **Why these.** A sweep of live objects against every `.sql` file in all five migration roots found 15 live triggers and 56 live non-extension functions, of which 2 triggers and 6 functions are created by no migration. `can_see_post`, `can_see_postcard` and `can_post_to_trip` are live RLS predicates confirmed from `pg_policies`; `is_accepted_trip_member` is named in no policy but is called by all three, so a rebuild without it breaks them.
  - **Trigger decision (assessed, not assumed).** Omitting the two `set_updated_at` triggers **is** materially different from production. `posts.updated_at` and `passport_postcards.updated_at` are `NOT NULL DEFAULT now()`, so without the triggers the column is set at INSERT and never advances on UPDATE. Multiple production update paths in `routes/posts.ts` do not set `updated_at` themselves (`post_status` transitions, `geotag_credit_awarded`, `canonical_place_id`, bucket classification, the location-privacy `update(patch)`), and `updated_at` is part of `POST_COLUMNS` so it is serialized to clients. Nothing would error — a rebuilt environment would just quietly report stale timestamps. Backfilled.
  - **Fidelity.** Bodies were read from live via `pg_get_functiondef()` and pasted verbatim, not retyped. `pnpm run check:backfill-0200` (`scripts/verify-backfill-0200.mjs`) re-reads live and asserts each definition appears **byte-for-byte** in the migration, and that the callee precedes its callers. Exit 0 = matches, 2 = could not run (no credentials — never a silent pass), 3 = drift. Verified 2026-08-10: all 4 functions + 2 triggers byte-identical.
  - **`SECURITY DEFINER` / `search_path` reproduced exactly, deliberately not "improved".** All four are `SECURITY DEFINER` and none pins a `search_path`. That is faithful to live. Adding a pinned `search_path` here would make the backfill diverge from production, which defeats its only purpose. Note this is **not** unique to these four — `can_see_trip`, `can_see_location`, `viewer_is_blocked`, `is_blocked`, `in_accepted_circle`, `shares_trip_with` and `auth_uid_is_event_host` are all live `SECURITY DEFINER` with no pinned `search_path`. Pinning them is a real hardening opportunity and a behavioural change to authorization functions; it belongs in its own reviewed migration, applied deliberately.
  - **NOT backfilled, on purpose (auditable inventory).** `increment_counter(text,text,uuid)` — guarded counter bump for `hidden_gems.save_count`/`visit_count`, pins `search_path=public`; skipped because nothing (no policy, view, constraint or function) references it, so a rebuilt schema is structurally complete without it, and its absence fails loudly rather than silently. `purge_old_ranking_debug_samples()` — retention job deleting `ranking_debug_samples` older than 7 days; skipped because nothing depends on it for correctness and its absence costs disk on a debug table rather than changing behaviour. Both remain genuine live-vs-migration drift and are candidates for a future migration; they are excluded on scope, not oversight.
## 2026-08-10 — post_media feed-size variant (0208)

- `0208_post_media_feed_variant.sql` — Adds **`post_media.feed_storage_path`** and **`post_media.feed_url`**, both `text`, both **NULLABLE with no default and no backfill**, each carrying a `COMMENT` stating what NULL means. **Applied 2026-08-10 via Supabase Management API.** **Verify:** `pnpm run check:media-objects` (now reconciles variants too — see below) and `pnpm run check:all`.
  - **The bug.** `PostcardTile.tsx` asked for `?width=500`, and that parameter never reached Storage. `appStorageUrlInfo()` (`lib/mediaUrl.ts:24,30-56`) matches on `url.pathname` only and documents "Query strings are ignored"; `/api/media/sign` runs every URL through it before signing, so the param was stripped before the signed URL existed. Since the buckets went private, the Postcard Wall has been fetching **full 2048px originals** and scaling them on-device.
  - **Fixed with a stored derivative, not by forwarding the param.** Generated at upload time following the proven 400px thumbnail pattern (`lib/mediaProcessing.ts:70,106-113`). A stored object does not depend on Supabase's image-transform feature being enabled, and it makes existence a fact the server can report rather than a guess.
  - **Size: `FEED_DIM = 1500`, justified.** The tile asks for ~500pt; phones in use are 2x or 3x DPR, so the densest common case needs 500 × 3 = **1500** physical px. 1024 (2x only) is visibly soft on every 3x device. 1500²/2048² ≈ 0.54 — roughly **46% fewer pixels**. **Quality deliberately held at 82**, the same value the original uses, so the saving has exactly one axis (dimension) and cannot be confused with a compression regression. The original is kept and remains what detail/fullscreen load.
  - **Derived from the PROCESSED buffer, never the raw upload.** `makeFeedVariant()` takes the already-processed image, so it inherits auto-orient and the full EXIF/GPS strip. Deriving from the raw bytes would have written capture coordinates into a *second* stored object — the exact defect this pipeline exists to remove. Asserted, not assumed: `mediaUploadHardening.test.ts` now checks the variant's own metadata is empty.
  - **⚠️ The write that mattered was in `postcards.ts`, not `posts.ts`.** The in-progress work only touched `POST /posts/media`. `post_media` — the table the Postcard Wall actually reads — is written by `POST /postcards/:id/media/:mediaId/complete`, which is where postcard media really lands. Without that, the columns would have stayed NULL forever and the feature would have been inert. Both paths now build the variant.
  - **NULL is the contract, and existing posts are why.** No backfill ran, so every pre-0208 row is NULL, along with every video and every failed derive. The API returns the URL when it exists and `null` when it does not; the client renders `feed_url ?? url`. The client must **never** append `.feed.jpg` to the original — for every pre-existing post that object does not exist and the request 404s. `?? null` in both projections is load-bearing: `undefined` is dropped by `JSON.stringify`, so the key would vanish rather than read as "no variant". Pinned by `src/test/feedVariantContract.test.ts` (5 tests, including the absent-column case).
  - **Fail-soft at every step.** Variant generation and upload are wrapped so a failure logs a warning and leaves `feed_url` NULL — which is exactly today's behaviour. Rejecting a completed upload because an *optimisation* could not be built would trade a working post for a faster one. The original image processing stays fail-**closed** as before: a corrupt image still rejects completion rather than skipping the GPS strip.
  - **Reads are probed, not hardcoded.** `feedVariantCol()` (`lib/postMediaOverlay.ts`) mirrors the existing `stampOverlayCol()` precedent and appends `feed_url` to the select only when the column exists. This is not ceremony: two of the four feed reads in `routes/posts.ts` wrap their select in a fail-**open** `catch`, so selecting a non-existent column would not error loudly — it would serve every post with an empty media array and look like "those posts have no media". The post-detail read is unwrapped and would 500.
  - **Write path degrades too.** The `complete` handler retries without the feed columns on `PGRST204`, mirroring the 0129 `stamp_overlay` precedent. Ordered **before** the overlay branch because PostgREST reports only the first unknown column, so a row missing both would otherwise loop on the overlay message.
  - **Deletion cleans up the derivative.** `DELETE /postcards/:id/media/:mediaId` now removes `<path>.feed.jpg` alongside the original. Left behind it would be an ORPHAN OBJECT in `check:media-objects`' terms — wasted storage, and content believed deleted that is still fetchable.
  - **`check:media-objects` extended in the same change.** It now (a) reports a row whose `feed_storage_path` object is **missing** as DANGLING — advertising a variant you do not have is the same user-visible broken image as a missing original — and (b) excludes variant objects from the orphan count, which would otherwise have turned a real signal into permanent noise proportional to how well the feature works. Column presence is probed via `information_schema` so the script still runs against a project without 0208.
  - **BACKFILL: wanted, but NOT run and not scheduled here.** Existing rows keep serving originals, which is current behaviour, so nothing regresses without one — the wall just stays slow for historical content. Doing it properly means re-downloading each original, re-deriving, uploading, and writing both columns, with its own rate limiting, resumability and per-row failure accounting. That is its own job, not a line in a schema migration. **Recommended as a follow-up.** → **Built 2026-08-10** as `src/scripts/backfillFeedVariants.ts` (`pnpm run backfill:feed-variants`); see the section below. **Still not run against production.**

## 2026-08-10 — Feed-variant backfill job (`backfill:feed-variants`) — NOT a migration, NOT run

- `artifacts/api-server/src/scripts/backfillFeedVariants.ts` — populates `post_media.feed_url` / `feed_storage_path` for rows predating 0208. **Built and tested; deliberately NOT executed against production, and NOT wired into `check:all`** — it mutates Storage and the database, and a job a gate runs on every build is a job that will eventually run when nobody meant it to.
  - **Dry-run count against live, 2026-08-10: 2 rows.** `post_media` holds **2** rows total — both ready images, both `feed_url IS NULL`, 0 already done. Small because 0206 deleted 14 polluted rows and 0207's cascade removed the rest. The dry run built both variants in memory successfully (`processed: 2, failed: 0`) and wrote nothing.
  - **Dry-run by default.** `--apply` is the only way to write; `parseArgs` is a pure function and the default is pinned by test. Also `--limit` (default 100), `--rate-ms` (default 250, on by default rather than opt-in), `--state`, `--reset`, and a read-only `--audit-exif`.
  - **Idempotent and resumable.** The row filter is `feed_url IS NULL`, so completed rows stop matching and a re-run converges with or without a checkpoint — the checkpoint is an optimisation, not the correctness mechanism. Failed ids are tracked *separately* from done ids so a resume retries them instead of treating them as finished.
  - **Write order is deliberate:** Storage object first, row last. Dying in between leaves an orphaned object that a re-run reclaims; the reverse would leave a row advertising an object that does not exist — the DANGLING state `check:media-objects` exists to catch.
  - **Failure accounting: a partial success is a failure.** Per-row failures are counted, listed (first 20, with the full count stated), and force exit 1. A run that fails 400 of 500 does not exit 0. Missing credentials exit 2, never a silent pass. Pinned by test.
  - **⚠️ EXIF — the variant is clean, the ORIGINAL may not be.** The variant is built with `makeFeedVariant()`, which re-encodes through sharp, so it is EXIF/GPS-clean regardless of what the source carried. That is a property of the re-encode, not of the input. This script reads whatever is in the bucket and **cannot** assume the stored original was ever stripped. `--audit-exif` was run against live to answer it with evidence rather than assumption: **1 of the 2 stored originals still carries EXIF** — `db1bc238…` on post `62252a3d…`, **3060 × 4080**, a 1081-byte block naming `samsung / Galaxy A16 5G`, captured `2026:07:17 20:44:43`. **No GPS IFD (0x8825) is present**, so this is not the severe case, but a 4080px stored original is proof that `processImage()` never touched it — the old cap was 2048, so anything that ran through the pipeline could not be that size. Re-stripping pre-strip originals is a **separate job with its own review**; it is not in scope here and a clean backfill must not be read as evidence that stored originals are clean.
    - Incidentally this is direct corroboration for the 0208/ceiling argument: a real user's phone uploaded a **4080px** long edge, which clears 3840 and fits under 4096.
  - **`components/PostcardTile.tsx` is currently dormant.** It and `PostcardWall` are referenced only by a re-export in `domainCards.tsx` and are not mounted anywhere. The wall users actually see is the local `PostcardTile` in `PostcardsTab.tsx`. Both were updated; only the second changes what ships today.

## 2026-08-10 — Delete the 21 seed_script posts on 92602b6c (0207) — DATA, not schema

- `0207_delete_21_seed_posts_on_92602b6c.sql` — Deletes the **21 UUIDv5 `source='seed_script'` posts** on `92602b6c…` (anroletrading@gmail.com) by **exact post id**, behind a `DO` guard that re-confirms UUIDv5 + `source` + owner per post and aborts the whole delete naming the offender if any one has moved. **Applied 2026-08-10 via Supabase Management API.**
  - **Why deleting the posts and not just clearing `media_urls`.** 0206 removed the 14 polluted `post_media` rows, which was correct and complete *for that table*, but it could not fix the rendering: clients read `posts.media_urls` (`POST_COLUMNS`, `routes/posts.ts:223`), and **7 of the 21 posts had no `post_media` row at all**. Owner approved removing the posts outright over the narrower `UPDATE posts SET media_urls='{}'`.
  - **Verified live, by id, not by predicate** (a predicate would also pass if `source` had been edited rather than the rows removed): all 21 ids absent from `posts`; `posts_likes` 0, `post_saves` 0, `posts_comments` 0, `passport_postcards` 0, `post_reactions` 0 for those ids. Whole-table check: `source='seed_script'` is now **0** across every account, `posts` total 17.
  - **Backed up BEFORE deleting** — `recovery-backups/2026-08-10-seed-post-21-cascade/`: `backup_posts.json` (21 full rows), `backup_cascade_children.json` (**501** rows across 5 tables), `will_orphan_not_deleted.json`, `MANIFEST.md` with the pre-measurement and restore order. The 2026-08-09 artifact is untouched.
  - **Cascade measured before the delete, not inferred after.** 17 FK children of `posts`: 15 `CASCADE`, 2 `SET NULL`, 0 `RESTRICT`. Non-zero: `posts_likes` 252, `post_saves` 122, `posts_comments` 103, `passport_postcards` 21, `post_reactions` 3 = **501**, all in the backup. The previous run of this operation took 7 rows nobody had counted; these were counted first.
  - **⚠️ Silently orphaned — no FK, so nothing cascaded and nothing warned.** `rank_events.item_id` **18,404** (deliberately untouched — `docs/algorithm/rank-events-signal-gaps.md`), `content_stamps.entity_id` 252, `compass_recommendation_scores.item_id` 99, `trip_plan_items.source_id` 1 → **18,756** at backup time. Recorded, not deleted. `reviews` was checked and *cannot* reference posts (its `review_entity_type` enum is trip/rent_buddy_booking/place). `trip_plan_items` row `68c2a847` was already a mis-typed reference (`source_type='place'` with a `source_id` that exists in `posts`); this delete makes it dangle.
  - **CORRECTION — the orphan set is NOT growing. An earlier revision of this entry said it was; that was wrong.** It claimed `content_stamps` rows pointing at the 21 dead ids had gone 252 → **254**, with "two stamps minted after the snapshot". The 254 was a **measurement artifact of the query that produced it**: it counted `WHERE entity_id IN (…)` across *all* entity types, while the backup's 252 counted `entity_type='post'` only. Re-measured with the filter aligned: **252 post rows, unchanged**, plus **2 rows with `entity_type='media'`** that happen to carry a post id. Both `media` rows were created **2026-07-20 and 2026-07-27**, i.e. *before* the 2026-08-10 delete, not after it. Nothing was minted post-delete. **18,756 is a fixed number, not a floor.**
    - **Provenance settles it.** All 252 post orphans carry `migrated_from='posts_likes'` — every one came from the one-off `2049_content_stamps` backfill, none from an organic write. The 2 `media` rows carry `migrated_from='post_reactions'` and resolve to nothing (`in_posts=false`, `in_post_media=false`): they are mis-typed rows from that same backfill, the identical class as the `trip_plan_items` `68c2a847` case already noted above. Rows carrying a `migrated_from` marker stop entirely after 2026-07-28; the newest `content_stamps` row of *any* kind is 2026-08-04, six days before the delete.
    - **And the live path could not have done it anyway.** `POST /api/stamps` routes `entity_type='post'` through `verifyPostStampAccess()` (`routes/contentStamps.ts:53-68`), which selects the post with `.eq("status","active")` and returns `not_found` when it is absent. A stamp on a deleted post is rejected before any insert. The genuine validation gap is elsewhere and is deliberate: the five public/canonical types (`place`, `guide`, `hotel`, `restaurant`, `destination`) skip existence checking with an explicit comment at `contentStamps.ts:605-607`.
    - **Lesson worth keeping:** a count compared against a recorded figure must reproduce that figure's *predicate*, not just its table. Dropping one `WHERE` clause manufactured a trend that did not exist, and "it grew by 2" is exactly the kind of small, plausible number that gets believed without re-derivation.

## 2026-08-10 — Delete seed pollution from two real accounts (0206) — DATA, not schema

- `0206_delete_seed_pollution_on_real_accounts.sql` — Deletes **14 `post_media` rows** and **4 orphaned `content_stamps`**, all by **exact row id**, each re-confirmed live immediately before running. **Applied 2026-08-10 via Supabase Management API.** **Verify:** `pnpm run check:media-objects` — now passes ("every post_media row has its Storage object"); it previously reported 114 dangling. `recovery-backups/` untouched.
  - **⚠️ Account attribution corrected.** The two sets are on **different** accounts, verified not assumed: the 14 `post_media` rows belong to **`92602b6c…` (anroletrading@gmail.com)**, the 4 `content_stamps` to **`5f123260…` (highrollsmoke@gmail.com)**. `post_media` had 16 rows with exactly **one** distinct owner (`92602b6c`) and **zero** rows owned by `5f123260`. The census identifies the owner only by attributes — "UUIDv4, @gmail.com, last signed in 2026-08-07" — and **both** accounts signed in on 2026-08-07, which is how they came to be conflated.
  - **Why no predicate.** `source='seed_script'` is unsafe here: account `92602b6c` holds 21 such posts of which only 14 carried the polluted media. Exact ids only, plus a `DO` guard that re-checks every audited property (date, generated-post uuid version, storage object absent, owner) and `RAISE`s rather than deleting if any row has changed since the audit.
  - **Set (a), 14 `post_media`:** all created 2026-07-17, all on UUIDv5 `seed_script` posts, all with storage object absent, all on active/public posts. The same account's other **2** rows were deliberately left: UUIDv4, `source='api_server'`, objects present — genuine uploads. Result: `post_media` 16 → 2, dangling 14 → **0**.
  - **Set (b), 4 `content_stamps`:** `63dee836`, `f4f764cd`, `131156b1`, `acca9763` — orphans from the earlier seed-post cascade, surviving only because `content_stamps` has no FK to `posts`. All four re-confirmed owned by `5f123260`, `entity_type='post'`, target post absent; all four appear verbatim in `backup_engagement.json`. Result: orphaned post-stamps for that user 4 → **0**, with the 3 genuine stamps (pointing at live `portava_editorial`/`api_server` posts) preserved.
  - **⚠️ SUPERSEDED by 0207 — the broken images ARE gone now.** The paragraph below was accurate when written and is kept as history; the remedy it names as out of scope was subsequently approved and applied as **0207**, which deleted the 21 posts outright. Live now holds **0** `seed_script` posts on `92602b6c`. Read the 0207 section below for the current state.
  - **⚠️ The broken images are NOT gone, and the real count is 21, not 14.** Rendering is driven by `posts.media_urls`, a separate array column serialized to clients via `POST_COLUMNS` (`routes/posts.ts:223`), not by `post_media`. Measured after the delete on `92602b6c`: **21** `media_urls` entries on v5 `seed_script` posts, **all 21 pointing at objects that do not exist**; the single v4 `api_server` entry resolves correctly. The 14 `post_media` rows were only part of it — the other 7 seed_script posts advertise media through `media_urls` with no `post_media` row at all. Fixing the rendering requires `UPDATE posts SET media_urls='{}'` on those 21 posts, which are explicitly out of scope, so it was **not** done. Smallest sufficient fix is that UPDATE; the alternative is deleting the 21 seeded posts outright (the census's original recommendation), which also removes their text.
  - **Now unblocked:** `docs/admin/moderation-coverage.md` says `check:media-objects` is "not wired into `run-all-checks.sh`… wire it once the seeded rows are cleaned up." It passes now, so that wiring is available — not done here, as it is a separate decision about gate composition. **Done 2026-08-10** (after 0207 took dangling to 0): wired as a `run_check` in `scripts/run-all-checks.sh` with nothing suppressed.

## 2026-08-10 — Drop the superseded enforce_is_official_service_role (0205)

- `0205_drop_enforce_is_official_service_role.sql` — `DROP FUNCTION public.enforce_is_official_service_role()`. **Applied 2026-08-10 via Supabase Management API.** **Verify:** `SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND p.proname='enforce_is_official_service_role';` — 0 rows.
  - **Dropped rather than pinned.** It was the last unpinned SECURITY DEFINER function after 0201 and 0204. Pinning it would have hardened something nothing calls; removing it is the better cleanup. Superseded by 2079, whose `enforce_is_official_privileged()` took over `enforce_is_official_trigger` because the old function (a) tested `current_setting('role')` alone and so rejected legitimate direct-postgres administration while claiming only the service role may act, and (b) guarded `FALSE→TRUE` only, letting a badge holder irreversibly clear their own badge.
  - **Confirmed unbound against live immediately before dropping** — not inferred from 2079's prose: 0 triggers, 0 event triggers, 0 other function bodies, 0 RLS policies, 0 column defaults, 0 `pg_depend` entries (`deptype <> 'n'`). Repo side: no `.ts`/`.tsx`/`.js` file calls it; the only references are prose in a test header, 2079's header and 0201's inventory.
  - **Guarded drop.** The migration is not a bare `DROP ... IF EXISTS`. It first runs a `DO` block that re-counts bound triggers and `RAISE`s if any exist, so a drop that races a new binding fails loudly instead of removing a live guard.
  - **Post-drop verification:** function gone (0 rows), and `enforce_is_official_trigger` still present on `public.profiles` executing `enforce_is_official_privileged`, `tgenabled='O'`.
  - **2079's ROLLBACK note corrected in the same change.** It instructed re-pointing the trigger at the now-dropped function, which would fail with "function does not exist". It now says to recreate the function first from `supabase/migrations/0106_profiles_is_official.sql` (lines 14–26), and warns that doing so reintroduces both defects.
  - **Final posture:** live SECURITY DEFINER functions in `public`, excluding PostGIS-owned: **44 pinned, 0 unpinned.**

## 2026-08-10 — Pin search_path on the SECURITY DEFINER functions that WRITE (0204)

- `0204_pin_search_path_writer_functions.sql` — Adds `SET search_path TO 'public', 'pg_catalog'` to **6** SECURITY DEFINER functions that write, via `ALTER FUNCTION`. **Applied 2026-08-10 via Supabase Management API.** **Verify:** `pnpm run check:write-path-hazard` (exit 0), and `pg_proc.proconfig` for the six.
  - **Functions pinned (6):** `add_owner_as_member`, `handle_new_user`, `increment_distribution_stats`, `increment_hashtag_usage_count`, `upsert_hashtag_usage_and_increment`, `purge_old_ranking_debug_samples`.
  - **Counts:** live SECURITY DEFINER (non-extension, excluding 3 PostGIS `st_estimatedextent`) — before: 38 pinned / 7 unpinned; after: **44 pinned / 1 unpinned**. The remaining one is `enforce_is_official_service_role`, dropped by 0205.
  - **Why worse than the read side.** 0201 pinned the authorization predicates, where a shadowed table *misinforms a decision*. These six **write**: a shadowed target puts the row somewhere the caller controls, or deletes from a table that is not the one the author meant, while executing with the definer's rights.
  - **Demonstrated on real write paths.** `scripts/verify-write-path-hazard.mjs` shadows the table each function writes to, sets `search_path` to prefer it, invokes the function, and looks in the shadow. Before 0204, **all five probed writers reported `HAZARD OPEN`**. The sharpest: a genuine `INSERT INTO public.trips` fired `trg_trip_owner_member` and the membership row recording the trip's **owner** landed in the attacker's schema instead of `public.trip_members` — the owner would not be a member of their own trip, with no error raised. After 0204 the identical probe reports `HAZARD CLOSED` for all five.
  - **Same-code red/green.** The script has a `--prove-red` mode that `RESET`s each function's `search_path` **inside the probe's own always-aborted transaction** (DDL is transactional, so the un-pin rolls back with everything else; production is never left unpinned, not even briefly). Run with it: all five `HAZARD OPEN`, exit 3. Run without: all five `HAZARD CLOSED`, exit 0. Re-verified after that production is still pinned. This exists so red and green come from *identical* probe code rather than from a historical run of an earlier version.
  - **Probe coverage, per function.** Five are demonstrated. **`handle_new_user` is not** — and not for the `auth.uid()` reason that limited 0201. It is a trigger function **bound to no trigger at all**: `auth.users` carries zero non-internal triggers, so there is no live write path to demonstrate and manufacturing one against the auth schema is not worth the blast radius. It is pinned on the mechanism the other five demonstrate. Do not read the probe's green run as per-function proof for it. (That it is unbound is itself worth knowing — profile creation is done by the application today. It is pinned rather than dropped because nothing supersedes it and it may be intended to be re-bound; if so, it is already safe.)
  - **Behaviour-neutrality checked per function.** All six reference only public tables (`trip_members`, `profiles`, `content_distribution_stats`, `hashtags`, `hashtag_usage`, `ranking_debug_samples` — each confirmed in `public`) plus `pg_catalog` builtins. Mechanically checked for and none found: calls into the `extensions` schema (`pgcrypto`/`uuid-ossp`/`pg_stat_statements` live there), references to `auth`/`storage`/`vault`/`realtime`/`graphql`, and temp tables. Assigned enums (`member_role`, `underexposure_status_enum`) are in `public`.
  - **Knock-on, caught rather than absorbed.** 0203's verifier asserted `purge_old_ranking_debug_samples` had **no** `proconfig`. Pinning it made that assertion fail (exit 3) — which is precisely why the empty expectation was asserted instead of skipped. The entry was **updated, not deleted**, to `search_path=public, pg_catalog` with `from: 0203+0204`.
  - **Probe robustness note.** The `upsert_hashtag_usage_and_increment` probe initially reported `UNEVALUATED` post-pin: once resolving to `public`, the real table's FKs (`hashtag_id → hashtags`, `author_id → profiles`) rejected synthetic ids that the FK-less shadow had accepted. Fixed by seeding a real `hashtags` row and using a real `profiles` id inside the same aborted transaction, so the probe is evaluable in both states. `UNEVALUATED` is treated as failure, never as a pass.

## 2026-08-10 — BACKFILL of increment_counter and purge_old_ranking_debug_samples (0203) — **DO NOT APPLY**

- `0203_backfill_counter_and_purge_functions.sql` — **STATUS: BACKFILL, NOT PENDING. Nothing to apply.** Both functions already exist live; the file exists so a clean rebuild reproduces production. Same contract and same discipline as 0200. **Verify:** `pnpm run check:backfill-0200` — now covers 6 functions and 2 triggers across both backfill migrations.
  - **Closes the last known migration-vs-live function drift.** These were the two 0200 deliberately inventoried and skipped, on the grounds that neither is a rebuild-correctness dependency (nothing references either, so a rebuilt schema is structurally complete without them). That reasoning still holds — they are captured now for completeness, not because it changed.
  - **⚠️ Known defect reproduced verbatim, on purpose.** `purge_old_ranking_debug_samples()` is `SECURITY DEFINER` with **no pinned `search_path`** and performs a `DELETE`. That is exactly the hazard 0201 closed for the authorization functions: a caller who can create a schema could shadow `ranking_debug_samples` and misdirect the delete while the function runs with the definer's privileges. It is reproduced **unpinned** because a backfill's only contract is "identical to live" — hardening it inside one would make the file diverge from production and defeat its purpose. **The fix belongs in its own `ALTER FUNCTION` migration**, exactly as 0201 did. 0201 already inventoried this function as a recommended follow-up; 0203 does not close it.
  - **`increment_counter` is correct as it stands** and needed no change: it pins `SET search_path TO 'public'`, hard-codes an allow-list of one table (`hidden_gems`) and two columns (`save_count`, `visit_count`) and `RAISE`s otherwise, quotes identifiers with `%I`, and binds the id with `USING`.
  - **Verifier extended.** `scripts/verify-backfill-0200.mjs` now checks both backfills as one corpus and carries a **per-function expected `proconfig`** rather than one global constant — `search_path=public, pg_catalog` for the four 0200 functions, `search_path=public` for `increment_counter`, and deliberately `""` (none) for `purge_old_ranking_debug_samples`. The empty case is asserted, not skipped: a function that silently *gains* a setting is drift in the other direction. Proven non-vacuous by a negative control — flipping the expected value to a wrong one made it exit 3 with `expected proconfig 'search_path=public', live is '(none)'`. The script keeps its filename and `check:backfill-0200` entry point despite covering two migrations; splitting it would duplicate every line.

## 2026-08-10 — Admit living_page and watch_feed as rank_events surfaces (0202)

- `0202_rank_events_living_page_watch_feed_surfaces.sql` — Widens `rank_events_surface_check` from 12 to **14** values, adding `'living_page'` and `'watch_feed'`. **Applied 2026-08-10 via Supabase Management API.** **Verify behaviourally, not by reading the CHECK:** `pnpm run check:rank-events-surfaces` — requires exit 0 with `GATE live_pulse: PERMITTED`, `GATE living_page: PERMITTED` and `GATE watch_feed: PERMITTED`.
  - **Why.** Both values were *already being written by production code and already being rejected*, silently: `living_page` by `routes/rankEvents.ts:66` (fire-and-forget, so the rejection surfaced only in a warn log) and `watch_feed` by `routes/mediaFeed.ts` at six call sites. The gate had been reporting this on every run as a standing FINDING, and the live corpus confirmed it — `rank_events` held only `pulse`/`compass`/`events`. Every Living Page and Watch Feed impression the app ever attempted was dropped. 0199 deliberately deferred this as a product decision; that decision is now taken: recording the signal beats continuing to lose it.
  - **Does not rewrite 0199.** 0199 is applied and released; its history stands. The chain reads 0197 (11 values) → 0199 (+`live_pulse`, 12) → 0202 (+`living_page`, +`watch_feed`, 14). Each step additive, each legible alone.
  - **Pre-apply check:** live `rank_events` held only `pulse` (185,293), `compass` (12,748), `events` (5,200) — all inside the new list, so the revalidating `ADD` could not fail. Wrapped in `BEGIN`/`COMMIT` for the same reason as 0199: a failed `ADD` after a committed `DROP` would leave the table with no surface constraint at all.
  - **Gate and tests updated in the same change.** `REQUIRED_SURFACES` in `checkRankEventsSurfaces.ts` now lists all three surfaces, so the probe attempts a real INSERT per surface and prints one GATE line each; `WRITTEN_SURFACES` gains `watch_feed`. `run_gate` in `scripts/run-all-checks.sh` previously hardcoded `grep -qxF 'GATE live_pulse: PERMITTED'` — it now iterates a `GATE_REQUIRED_SURFACES` array and names exactly which surfaces are missing a verdict, so adding a surface can no longer leave the gate silently checking less than it believes.
  - **Test-model correction.** `livePulseImpressions.test.ts` modelled `living_page`/`watch_feed` as *rejected* — asserting a rejection the database no longer performs is the same class of lie as asserting a permission it does not grant, just inverted. Those two moved from the negative set to the positive set, and the negatives are now near-miss spellings (`pulse_live`, `livingpage`, `watchfeed`, `livepulse`, `Live_Pulse`) that no migration will ever grant. One test that had substituted `'discovery'` for `watch_feed` "to stay inside the vocabulary" now uses the real value, which also updated its `by_surface` expectation (counts unchanged, key only).

## 2026-08-10 — Pin search_path on SECURITY DEFINER authorization functions (0201)

- `0201_pin_search_path_authz_functions.sql` — Adds `SET search_path TO 'public', 'pg_catalog'` to **16** SECURITY DEFINER authorization functions via `ALTER FUNCTION` (no body rewrite, so migration 0200's byte-exact bodies are not reopened). **Applied 2026-08-10 via Supabase Management API.** **Verify:** `SELECT proname, array_to_string(proconfig,',') FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND p.prosecdef AND p.proconfig IS NULL;` — 7 rows remain (all non-authorization, inventoried below).
  - **Functions pinned (16):** `is_accepted_trip_member`, `can_post_to_trip`, `can_see_post`, `can_see_postcard`, `can_see_trip`, `can_see_location`, `viewer_is_blocked`, `is_blocked`, `in_accepted_circle`, `shares_trip_with`, `auth_uid_is_event_host`, `auth_uid_has_event_role`, `auth_uid_has_event_rsvp`, `auth_uid_is_event_cohost`, `user_is_event_participant`, `event_is_in_state`.
  - **Counts:** live SECURITY DEFINER (non-extension) = 45. Before: 22 pinned / **23 unpinned**. After: 38 pinned / **7 unpinned**. Note the starting figure was 23, not the 11 originally believed — the extra 12 were found by sweeping live rather than trusting the list, and 5 of them (the event-permission family) were authorization functions guarding more tables than one that was on the original list.
  - **The hazard was demonstrated, not asserted.** `pnpm run check:search-path-hazard` (`scripts/verify-search-path-hazard.mjs`) builds the actual attack: creates a schema, adds a **shadowing table** holding a row that flips the answer, sets `search_path` to prefer it, calls the function, and always aborts so nothing persists. Run against live **before** 0201 it reported `HAZARD OPEN` for all four probed functions — `is_blocked`, `in_accepted_circle`, `can_see_post`, `can_see_trip` — i.e. the functions really did read the caller's table while executing as the definer. `can_see_post` and `can_see_trip` are live RLS predicates, so that is a read-authorization bypass. After 0201 the identical probe reports `HAZARD CLOSED` for all four. A catalog read of `proconfig` would have proven none of this.
  - **Probe coverage, stated honestly.** Only functions with a branch reachable without `auth.uid()` can be probed this way; the Management API has no JWT, so `user_id = auth.uid()` is unsatisfiable regardless of which table the name resolves to and such a probe would be green for the wrong reason. Four functions are genuinely red→green; the other twelve are pinned on the strength of the identical mechanism, not a per-function differential. Do not read a green run as per-function proof for those twelve.
  - **Behaviour-neutrality checked per function, not in bulk.** Every body resolves only against `public` (application tables plus the `member_role` / `event_role_type` / `event_rsvp_status` / `event_state` enums, all confirmed in `public`) and `pg_catalog` builtins. `auth.uid()` is schema-qualified everywhere, so the pin does not touch it. No body uses a temp table or an object in the `extensions` schema (`pgcrypto`, `uuid-ossp`, `pg_stat_statements` live there); PostGIS and unaccent are installed in `public`, so even those would resolve under the pin.
  - **Precedent, not a new pattern:** `enforce_is_official_privileged`, `enforce_profile_role_privileged` and `admin_set_profile_role` already used `SET search_path TO 'public', 'pg_catalog'`.
  - **Effect on `check:backfill-0200` — decided deliberately.** Pinning changes `pg_get_functiondef()` output, so the 0200 verifier began reporting DRIFT on all four of its functions (bodies identical; only the `SET` line differed). **0200 was NOT rewritten.** It is already committed and pushed, and the chain is correct as it stands — a rebuild replays 0200 (bodies) then 0201 (pin) and lands on exactly the live state. Rewriting a released migration to reflect a later one would break chain immutability and make 0200 describe a state that never existed at its point in the sequence. Instead the verifier now strips the `SET search_path` line before comparing, so 0200 is held to what it owns (the bodies), **and** asserts positively that the expected `proconfig` is present live — the coverage is reattributed to 0201, not dropped. `SECURITY DEFINER`, volatility, language, arguments and body text are still compared byte-for-byte.
  - **NOT pinned — the remaining 7, inventoried.** `add_owner_as_member` and `handle_new_user` (both SECURITY DEFINER triggers that **write** — to `trip_members` and `profiles` respectively; a shadowed target would misdirect the write, so these are the **recommended next** to pin), `increment_distribution_stats`, `increment_hashtag_usage_count`, `upsert_hashtag_usage_and_increment`, `purge_old_ranking_debug_samples`, and `enforce_is_official_service_role` (superseded by 2079, bound to no trigger — dead code; dropping it is the better cleanup). Excluded to keep 0201 to a single reviewable claim: authorization predicates resolve against `public`, always.

- **Correction to `2079`'s "DRIFT NOTE" (prose only, nothing applied changed).** 2079 claimed `profiles.is_official`, `enforce_is_official_trigger` and `enforce_is_official_service_role()` "appear in **no migration file**" and called it real drift. That is wrong: `supabase/migrations/0106_profiles_is_official.sql` creates the function (line 14), the trigger (lines 28–30) and `idx_profiles_is_official`. 2079's follow-on "correction" of 2078's `(migration 0106)` citation is wrong for the same reason — 2078 cited the right number in a different directory; two unrelated files share the `0106` prefix across roots. Corrected in the 2079 header and in `artifacts/api-server/docs/migrations.md`. **Lesson worth keeping:** a "tree-wide search" that covers only `artifacts/api-server/src/migrations` manufactures drift that does not exist. There are five migration roots.

## 2026-08-11 — Schema drift reconcile: `post_event_links`, 6 missing indexes, and 2070's RLS enable (hand-applied, no new migration file)

- **Applied 2026-08-11 to production Supabase, project `ajrurzioarfkagpuxfnb`**, by hand in the SQL editor as a single `BEGIN … COMMIT` transaction. **No new migration file was created**, deliberately: this closes the gap between three already-committed migrations and live, so the record of the apply is this entry, per the convention that applying is recorded here rather than by adding a file to `src/migrations/`. The exact SQL is kept as a one-off apply record at `_incoming/prod-drift-apply.sql` with its verification query at `_incoming/prod-drift-verify.sql`; `_incoming/` is gitignored (`.gitignore:87`), so neither is a migration and neither is in version control. **If you need the SQL later, it is in this entry, not in the repo.**
- **Nine objects applied, all `IF NOT EXISTS`.** These are exactly the objects `audit:schema` reported missing for these three files:

| File | Objects applied |
|---|---|
| `20260731_post_event_links.sql` | table `post_event_links`; `idx_post_event_links_post_id`; `idx_post_event_links_event_id` |
| `0186_geo_indexes.sql` | `user_location_state_geo_idx`; `events_geo_idx`; `posts_geo_idx`; `hidden_gems_geo_idx`; `hidden_gems_approx_geo_idx` |
| `2044_hidden_gems_canonical_place_id.sql` | `hidden_gems_canonical_place_idx` |

  All three files are **now applied**. Their `ADD COLUMN IF NOT EXISTS` statements were **deliberately omitted** from the applied block — 0186's six geo columns were verified present live, and `hidden_gems.canonical_place_id` was already live (the audit reported only 2044's index, never its column). Omitting proven no-ops kept the write surface to exactly the missing objects. Consequence worth knowing: a column that had turned out to be absent would have failed its `CREATE INDEX` and rolled the whole transaction back, which is the intended failure mode.
- **2070's RLS enable for `post_event_links` was applied BY HAND in the same transaction** — `ALTER TABLE public.post_event_links ENABLE ROW LEVEL SECURITY`, unconditional, no policies (deny-all; `lib/eventPostsDiscovery.ts` reads the table through the service-role client, which bypasses RLS). **This is the part that would not have happened on its own.** `2070_rls_hardening.sql:89-94` wraps the enable in `IF to_regclass('public.post_event_links') IS NOT NULL`; 2070 had already run at a time when the table did not exist, so that branch no-opped, and a one-shot migration never re-runs. Creating the table without the enable would have left it live with RLS off — readable and writable by anyone holding the anon key the mobile app ships. The create and the enable are in one transaction for exactly that reason.
- **Plain `CREATE INDEX`, not `CONCURRENTLY`.** `CONCURRENTLY` cannot run inside a transaction, and at production row counts (104 / 17 / 6 / 4 across the four indexed tables) atomicity is worth far more than lock duration. This is a deliberate departure from the advice in `0186_geo_indexes.sql:46-49`, which recommends `CONCURRENTLY` and notes the Supabase editor runs statements un-wrapped — correct for large tables, wrong at this scale.
- **Verification, post-`COMMIT`, 2026-08-11, project `ajrurzioarfkagpuxfnb`:**
  - Object-presence query: **ten rows, `present = true` on every one** — the table (`relkind` `r`), all eight indexes (`relkind` `i`), and `post_event_links.relrowsecurity`.
  - **`post_event_links.relrowsecurity = true`.** This is the check that mattered. `false` would have meant the table was live without RLS.
  - `pnpm run audit:schema` — **exit 0**, **4021 claimed objects, no missing objects** (`✔ Live schema contains every object claimed by the migrations.`). Before the apply it exited 1 with 9 missing across the three files above.
  - Re-verify at any time:

        SELECT to_regclass('public.post_event_links') IS NOT NULL AS tbl,
               (SELECT relrowsecurity FROM pg_class
                 WHERE oid = to_regclass('public.post_event_links')) AS rls;
        -- expect: t, t

        SELECT indexname FROM pg_indexes WHERE schemaname = 'public'
          AND indexname IN ('idx_post_event_links_post_id','idx_post_event_links_event_id',
            'user_location_state_geo_idx','events_geo_idx','posts_geo_idx',
            'hidden_gems_geo_idx','hidden_gems_approx_geo_idx','hidden_gems_canonical_place_idx')
          ORDER BY 1;
        -- expect: 8 rows

- **Why `audit:schema` reads 0 and not 1 afterwards.** `auditMigrationsVsLive.ts:730` suppresses an `rls:` claim whose relation is absent ("declared for a table nobody created" is not drift), so `rls:post_event_links` was **not** among the 9 reported — it becomes a reported gap the instant the table exists without RLS. Creating the table promotes that claim from suppressed to live-checked. The count went 9 → 0 *because* the enable was included; it would have gone 9 → 1 had it been omitted. The audit would have caught it, but only after the table had already sat unprotected.
- **Three declared RLS policies were NOT applied and remain deliberately unapplied** — `media_assets_public_select`, `media_attachments_public_select` (`20260811_media_rls.sql`) and `users_view_highlight_replies` (`0026_highlights.sql` / `2033_rls_hardening.sql`). They are allowlisted at `auditMigrationsVsLive.ts:221-236` with the reasoning written out in full there; all three are pure widenings or superseded declarations, and their absence is the restrictive direction. Of the twelve objects the 2026-08-10 production audit found declared-but-absent (`docs/schema-reconciliation-2026-08-08.md` §2), **nine are now applied and three are deliberately not**.
- **Provenance, stated plainly given this file's own header warning.** The apply and the verification were executed by the operator in the Supabase SQL editor and reported back; this session composed the SQL, checked its structure offline, and recorded the outcome. Nothing here was observed by the session that wrote it. The two queries above are the way to re-establish it independently — do that before relying on this row.

## 2026-08-21 — Location sharing schema convergence (2110)

| Migration | Change | Applied |
|---|---|---|
| `2110_location_sharing_schema_convergence.sql` | Establishes `user_location_preferences` as the canonical sharing-mode/precision table; privacy-safe backfill from the incompatible legacy `location_preferences` table; adds/checks the full `LocationSessionService` column contract; widens `location_sessions.session_type` to the service and legacy values; retains all legacy tables, columns, and values for code rollback. Creates no Journey observation table, collector, scheduler, or feature flag. | 2026-08-21 via Supabase Management API |

Live verification after commit:

- `user_location_preferences`: 9 rows; `location_preferences`: 9 rows;
  unmatched legacy rows: **0**. Every backfilled mode is `city_only`; incompatible
  audience-style visibility values were not copied, and trusted-circle sharing
  was not inferred from the legacy table's permissive default.
- All 9 service columns are present on `location_sessions`: `expires_at`,
  `city`, `district`, `country`, `country_code`, `lat`, `lng`,
  `related_trip_id`, and `related_plan_id`.
- `location_sessions_session_type_check` accepts the four service values
  (`private_stay`, `safe_return`, `trusted_circle`, `plan_checkin`) plus every
  prior canonical/deployed value. A transactionally rolled-back
  `private_stay` insert proved the deployed write contract.
- The canonical preference mode and two nullable precision CHECK constraints
  are present; the existing three owner policies remain present.
- `to_regclass('public.journey_observations')` is `NULL`.

## 2026-08-21 — Journey observation and privacy foundation (2119 / 2120)

| Migration | Change | Applied |
|---|---|---|
| `2119_journey_observation_foundation.sql` | Creates restricted, append-only Journey observation storage; a default-off ingestion RPC and feature flags; 24-hour raw expiry with a 72-hour hard cap. It does not add a collector or consumer. | 2026-08-21 via Supabase Management API |
| `2120_journey_privacy_foundation.sql` | Adds versioned Journey consent, explicit Journey-purpose sessions, durable revocation jobs, serialized retention health/leases, hardened RPC-only ingestion, and least-privilege grants. | 2026-08-21 via Supabase Management API |

The files were submitted unchanged, in order, as separate live-database
transactions. Post-commit verification established:

- both `COMPASS_JOURNEY_ENGINE_ENABLED` and
  `COMPASS_JOURNEY_OBSERVATION_INGEST_ENABLED` remain `false`;
- zero enabled Journey consents, zero active Journey-purpose sessions, and zero
  Journey observations remain after synthetic verification cleanup;
- direct `service_role` INSERT into `journey_observations` is denied, as are
  direct service-role mutations of the revocation queue and retention health;
- the managed API retention worker completed both empty and deletion cycles,
  processed a durable session-expiry job in 58.4 seconds, and returned durable
  health to `HEALTHY` with no retry or lag;
- collection was not activated and no collector, Compass consumer, inference,
  notification, movement-derived behavior, or Autopilot path was added.

The controlled deployment gate remains **NO-GO** for future collection because
the live centralized account-deletion proof failed closed at profile
anonymization: `profiles.handle` is `NOT NULL`, while
`AccountDeletionService` writes `handle = NULL`. All Journey/location deletion
steps succeeded and the request was not falsely completed, but this live-schema
compatibility blocker must be repaired and re-verified before activation.

## 2026-08-21 — Controlled Journey shadow rollout schema (2103 / 2123)

| Migration | Change | Applied |
|---|---|---|
| `2103_journey_segment_shadow.sql` | Creates the shadow-only segment-revision storage prerequisite. It does not enable collection or add a product consumer. | 2026-08-21 via Supabase Management API |
| `2123_journey_shadow_controlled_rollout.sql` | Adds finite staged rollout controls, the single fail-closed SQL authority, quality-aware ingestion, sealed raw/derived access, unified retention and revocation, global stop, and aggregate-only internal QA evidence. All capability flags remain default-off. | 2026-08-21 via Supabase Management API |

The files were submitted unchanged as separate Management API requests after
the full API suite and a disposable PostgreSQL rehearsal passed. Post-commit
verification established:

- all rollout tables and required service-only RPCs exist;
- owner-approved stages, active assignments, active issued sessions,
  observations, segment revisions, ground-truth rows, and QA reports are all
  zero;
- `COMPASS_JOURNEY_ENGINE_ENABLED`,
  `COMPASS_JOURNEY_OBSERVATION_INGEST_ENABLED`, and
  `COMPASS_JOURNEY_SEGMENTATION_SHADOW_ENABLED` are all `false`;
- direct `service_role` SELECT/INSERT/UPDATE/DELETE privileges on raw
  observations and derived segments are absent;
- after the API restarted onto the RPC-only worker, unified Journey retention
  completed `HEALTHY` with zero backlog, lag, retries, consecutive failures, or
  deletions.

One pre-restart worker cycle failed closed during the grant cutover: the old
process attempted its now-revoked direct-table path. Collection remained
impossible because every capability flag was off and there were zero Journey
rows. Restarting onto the reviewed RPC-only code restored healthy retention on
the first cycle.

## 2026-08-23 — Journey files renumbered 2120-2123 → 2124-2127 (content unchanged)

`2120_journey_privacy_foundation.sql`, `2121_account_deletion_tombstone_contract.sql`,
`2122_account_deletion_journey_revocation_compat.sql`, and
`2123_journey_shadow_controlled_rollout.sql` were renamed on disk to
`2124_journey_privacy_foundation.sql`, `2125_account_deletion_tombstone_contract.sql`,
`2126_account_deletion_journey_revocation_compat.sql`, and
`2127_journey_shadow_controlled_rollout.sql`, to resolve a prefix collision:
`main` independently used 2120-2123 for the unrelated `canonical_events` /
`source_registry` (table `sources`) / `freshness_policies` /
`canonical_event_families` chain (merged via #104/#105).

**These four files were authored and applied to production as 2120-2123** —
see the two entries above, both dated 2026-08-21. That is still the accurate
record of what ran against production. The rename does not change that; it
only changes which filename in the canonical tree now carries that content.

**Verified before renaming, not assumed:**
- Live read-only checks (portava-ci and production, 2026-08-23) found the two
  colliding sets applied to *different* databases: `canonical_events`,
  `sources`, `freshness_policies`, and `canonical_event_families` exist on
  portava-ci with zero Journey tables present; production has all nine live
  Journey tables (`journey_observations`, `journey_retention_health`,
  `journey_revocation_jobs`, `journey_segment_revisions`,
  `journey_shadow_cohort_assignments`, `journey_shadow_ground_truth`,
  `journey_shadow_qa_reports`, `journey_shadow_session_issuances`,
  `journey_shadow_stages`) with none of the canonical_events-family objects.
  Neither side is applied to both databases, so the general "both applied,
  don't rename" rule (see the 2059/2089 documented collisions below) does not
  cleanly cover this case.
- `2128_intel_contracts_seed.sql` and `2130_intel_storage.sql` already state
  that prefix 2128 "deliberately skips 2124-2127, which are reserved for the
  Journey observation files currently unpushed in the Replit workspace" — the
  four new numbers were already reserved in merged code before this rename,
  so this executes a decision already made rather than a new one.
- All six journey-family files (2103, 2119, and the four renamed here)
  collectively create exactly the nine live Journey tables above; none
  references `canonical_events`, `sources`, `freshness_policies`,
  `canonical_event_families`, `external_place_references`,
  `discovery_places`, or `fsq_places`; and no function/index/trigger/policy/
  table name in the Journey chain collides with the four `main` files — so
  applying both chains to the same database later (once `main`'s set reaches
  production) is expected to be safe.
- Internal self-references to the old numbers were updated in the same
  commit throughout the four renamed files (headers, "Builds on:" lines,
  `RAISE EXCEPTION` precondition messages) and in the test suites that path
  to them by filename (`journeyShadowBoundary.test.ts`,
  `journeyControlledRolloutMigration.test.ts`, `journeyPrivacyFoundation.test.ts`,
  `locationGps.test.ts`, `accountDeletionCascade.test.ts`) — 361 tests across
  those five files pass, including the disposable-PostgreSQL integration
  suite that replays 2103+2119+2124+2127 end to end.
- **Two things this rename intentionally left untouched.** The JSONB literal
  `'{"...,"migration":"2123"}'` seeded by (now) `2127_journey_shadow_controlled_rollout.sql`
  into `feature_flags.metadata` keeps the string `"2123"` — that value is
  already persisted on production under that content, and changing the
  source would misrepresent what the live row actually stores. And
  `docs/sql/rollback_2119_journey_observation_foundation.sql` /
  `docs/sql/rollback_2120_journey_privacy_foundation.sql` were not renamed or
  touched — they are checksum-pinned under a frozen root in
  `frozenMigrationRoots.ts` and keep their original numbers permanently.
- `DOCUMENTED_COLLISIONS` in `checkMigrationPrefixes.ts` was left untouched —
  this is an active rename, not a permanent both-sides-applied collision
  being excused in place.

**Two related gaps, flagged but out of scope for this rename:** `main`'s
2120-2123 (canonical_events family) are applied to portava-ci but — per the
live check above — not yet to production, and have no ledger row in this
file recording that; and no migration file anywhere in this repository
creates any of the nine live Journey tables under their *current* numbers
before this entry, and the 2026-08-19 baseline schema dump contains none of
them either — the only place their DDL exists in git is the six Journey
migration files themselves. Both are worth someone's attention independent
of this renumber.
