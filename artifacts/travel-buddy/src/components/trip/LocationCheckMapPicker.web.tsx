/**
 * Web stub for LocationCheckMapPicker.
 * Metro selects this file on web builds where the MapLibre native module is
 * unavailable. The parent LocationCheckSheet shows only the list picker in
 * this environment, so this component renders nothing.
 */
import type { LocationCheckMapPickerProps } from './LocationCheckMapPicker';

export function LocationCheckMapPicker(_props: LocationCheckMapPickerProps) {
  return null;
}
