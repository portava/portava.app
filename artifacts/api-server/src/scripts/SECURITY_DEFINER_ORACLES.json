{
  "//": "SECURITY_DEFINER_ORACLES.json — read by src/scripts/checkSecurityDefinerOracles.ts. Keys are bare function names in `public` that are SECURITY DEFINER and that NOTHING in the corpus references: no surviving RLS policy, no other function body, no view, no trigger, and no string literal in src/. Every entry is validated on every run: a key that is no longer a definer function FAILS, a key that has since acquired a reference FAILS, and a reason that does not begin with a classification or runs under 80 characters FAILS. This list may only shrink.",
  "//classifications": "RPC-BY-DESIGN: meant to be called over PostgREST by a client this corpus does not contain. OPERATOR-ONLY: called by hand or by a scheduler outside the repo, and its grants say so. PENDING-OWNER: its fate is an unmade product decision. HOLD: it belongs to a workstream that is deliberately frozen. DEAD: superseded, kept only because dropping it needs a migration.",
  "//remedy": "The remedy for an entry here is a DROP, or a REVOKE when no policy calls it. It is NEVER a REVOKE on a policy-referenced function: revoking EXECUTE makes the policy itself raise 'permission denied for function' for every end-user token. That was measured on the CI project for both `language sql` and `language plpgsql`; the check's header carries the transcript.",
  "unreferenced": {
    "event_is_in_state": "HOLD: an events existence-and-state oracle — EXISTS(SELECT 1 FROM events WHERE id = eid AND state = ANY(states)) — defined by 2033_rls_hardening.sql as a helper for policies that were never written with it. Live grants are the widest of any entry here: anon AND authenticated both hold EXECUTE (verified 2026-09-08), so POST /rpc/event_is_in_state answers, for any event id, whether that event exists and is in a named state, to a caller who has not signed in. It is 0-referenced by 808 surviving policies. It is NOT dropped or revoked here because Event Truth Phase-B is a declared HOLD and EVENT_START_TRANSITION is an OWNER decision: this function is the shape a state-gated policy would be built from, so retiring it is a decision inside frozen work rather than a cleanup outside it. What is recorded instead is that its exposure is live and unearned TODAY, so that whoever unfreezes that workstream inherits the finding rather than rediscovering it.",
    "increment_hashtag_usage_count": "DEAD: superseded by upsert_hashtag_usage_and_increment, which is what TaggingService.ts actually calls and which exists specifically to stop the double-counting this one permits. Its whole body is an unconditional UPDATE hashtags SET usage_count = usage_count + 1 with no dedup, no rate limit and no check on the caller. Uncalled anywhere in the repository: the only 16 mentions are documentation tables and duplicate copies of migration 0043/0044. EXECUTE was granted to `authenticated`, which made POST /rpc/increment_hashtag_usage_count a trending-counter inflation primitive for any signed-in account — the exact guarantee its replacement was written to provide, reachable by going around it. That grant is withdrawn by migration 2551; the function itself stays until someone decides whether to drop it, because a DROP is not reversible by a grant.",
    "purge_old_ranking_debug_samples": "HOLD: DELETEs ranking_debug_samples older than seven days and returns the count. Uncalled in the repository — no scheduler in src/services or src/scripts names it. EXECUTE is granted to `authenticated` (verified 2026-09-08), so any signed-in account can trigger the purge; the blast radius is bounded to rows already past their intended retention, which is why this is recorded rather than treated as urgent. It is left alone because ranking_debug_samples is Ranker surface and Ranker is a declared HOLD. The grant is the part that is wrong, not the function.",
    "telegraph_unsend_message_before_seen": "PENDING-OWNER: added to this tree on 2026-09-15 by the restore of 2325_telegraph_unsend_before_seen.sql, which closed a schema_migration_ledger row that named no file on disk. Its CALLER did not come with it and that was deliberate: a ledger row names a FILE, and importing the unmerged PR #472 route under cover of a ledger repair would have been the wrong trade. So the function is genuinely 0-referenced here, and this entry is the honest cost of restoring an orphan file without its caller rather than something discovered later. THE STANDING WARNING IN THIS CHECK DOES NOT APPLY TO IT, and that is the part worth reading: the message says Supabase default privileges leave EXECUTE with anon and authenticated, but 2325 REVOKEs ALL from PUBLIC, anon and authenticated and GRANTs EXECUTE to service_role alone, and it ASSERTS that posture in its own postcondition rather than merely performing it. Verified live on portava-ci 2026-09-15 by has_function_privilege: anon false, authenticated false, service_role true. So POST /rpc/telegraph_unsend_message_before_seen is not reachable with a user token and this is not an exposed oracle — unlike event_is_in_state above, where the grants really are wide and the entry says so. It is SECURITY DEFINER with search_path pinned to pg_catalog, public. The unmade decision is PR #472's: if it merges, routes/messaging.ts calls this and the entry must be deleted; if it is abandoned, the function should be DROPPED by a new migration, never left with a REVOKE. Remove this entry the moment either happens.",
    "revoke_sensing_sessions_for_profile": "OPERATOR-ONLY: added by 2481_sensing_sessions_option_a_issuer.sql as the revocation half of the sensing-session issuer. Its grants already say what it is — postgres and service_role only, with neither anon nor authenticated holding EXECUTE (verified 2026-09-08) — so it is not reachable over PostgREST by a user token at all, and it is listed here only because nothing in the corpus calls it yet. SENSING_AUTH_POSTURE is an OWNER decision and the caller is on the far side of it; the function is the built half waiting for the decided half.",
    "consume_intel_sensing_credential": "HOLD: the single-use redemption half of 2956's sensing credential design -- it matches a token digest, checks expiry and nonce, and marks the credential consumed. THE STANDING WARNING IN THIS CHECK'S MESSAGE DOES NOT APPLY: it says Supabase default privileges leave EXECUTE with anon and authenticated, and that is false here. 2956 REVOKEs ALL on this function FROM PUBLIC, anon, authenticated and GRANTs EXECUTE to service_role alone, in its own text, so a fresh replay of the chain produces the same posture. Verified live on 2026-09-16 against BOTH databases -- production (ajrurzioarfkagpuxfnb) and portava-ci (hwokxgbmezheskbzskfr) -- where proacl reads exactly 'postgres=X/postgres | service_role=X/postgres'. Neither client role holds EXECUTE, so POST /rpc/ is not reachable with a user token and this answers nobody's authorization question. That is the fact this check declares out of scope (its own NOTE: 'the grants themselves, which are a live fact'). WHY IT IS 0-REFERENCED: its caller was deliberately not ported. The Replit Media/Map/Sensing port rejected deviceContributionCredential.ts because it authorises a credential against an actor id, where this repository's sensingContributionSession keeps no identity at rest, and lib/sensingAuthPosture.ts fixes SENSING_AUTH_POSTURE = 'undecided' with no flag able to flip it at runtime. So the built half is here and the deciding half is not. Remove this entry if that posture is decided and a caller lands, or DROP the function by migration if the workstream is abandoned -- never a REVOKE, which is already done and is not the remedy this check accepts.",
    "is_sensing_device_eligible": "HOLD: the eligibility oracle for 2956's device enrollment -- given an actor and a device it answers whether that pair may contribute. Exactly the shape this check exists to catch, which is why its grants were measured rather than assumed. THE STANDING WARNING IN THIS CHECK'S MESSAGE DOES NOT APPLY: it says Supabase default privileges leave EXECUTE with anon and authenticated, and that is false here. 2956 REVOKEs ALL on this function FROM PUBLIC, anon, authenticated and GRANTs EXECUTE to service_role alone, in its own text, so a fresh replay of the chain produces the same posture. Verified live on 2026-09-16 against BOTH databases -- production (ajrurzioarfkagpuxfnb) and portava-ci (hwokxgbmezheskbzskfr) -- where proacl reads exactly 'postgres=X/postgres | service_role=X/postgres'. Neither client role holds EXECUTE, so POST /rpc/ is not reachable with a user token and this answers nobody's authorization question. That is the fact this check declares out of scope (its own NOTE: 'the grants themselves, which are a live fact'). WHY IT IS 0-REFERENCED: its caller was deliberately not ported. The Replit Media/Map/Sensing port rejected deviceContributionCredential.ts because it authorises a credential against an actor id, where this repository's sensingContributionSession keeps no identity at rest, and lib/sensingAuthPosture.ts fixes SENSING_AUTH_POSTURE = 'undecided' with no flag able to flip it at runtime. So the built half is here and the deciding half is not. Remove this entry if that posture is decided and a caller lands, or DROP the function by migration if the workstream is abandoned -- never a REVOKE, which is already done and is not the remedy this check accepts.",
    "revoke_intel_sensing_credentials": "HOLD: the revocation half of 2956's credential lifecycle -- it invalidates every live credential for an actor and device, the operation a person would invoke after losing a handset. THE STANDING WARNING IN THIS CHECK'S MESSAGE DOES NOT APPLY: it says Supabase default privileges leave EXECUTE with anon and authenticated, and that is false here. 2956 REVOKEs ALL on this function FROM PUBLIC, anon, authenticated and GRANTs EXECUTE to service_role alone, in its own text, so a fresh replay of the chain produces the same posture. Verified live on 2026-09-16 against BOTH databases -- production (ajrurzioarfkagpuxfnb) and portava-ci (hwokxgbmezheskbzskfr) -- where proacl reads exactly 'postgres=X/postgres | service_role=X/postgres'. Neither client role holds EXECUTE, so POST /rpc/ is not reachable with a user token and this answers nobody's authorization question. That is the fact this check declares out of scope (its own NOTE: 'the grants themselves, which are a live fact'). WHY IT IS 0-REFERENCED: its caller was deliberately not ported. The Replit Media/Map/Sensing port rejected deviceContributionCredential.ts because it authorises a credential against an actor id, where this repository's sensingContributionSession keeps no identity at rest, and lib/sensingAuthPosture.ts fixes SENSING_AUTH_POSTURE = 'undecided' with no flag able to flip it at runtime. So the built half is here and the deciding half is not. Remove this entry if that posture is decided and a caller lands, or DROP the function by migration if the workstream is abandoned -- never a REVOKE, which is already done and is not the remedy this check accepts.",
    "unlink_intel_sensing_device": "HOLD: the erasure half of 2956's device enrollment -- it stamps unlinked_at on the eligibility row, which is the only path that breaks the identity-to-device link the enrollment route would have written. THE STANDING WARNING IN THIS CHECK'S MESSAGE DOES NOT APPLY: it says Supabase default privileges leave EXECUTE with anon and authenticated, and that is false here. 2956 REVOKEs ALL on this function FROM PUBLIC, anon, authenticated and GRANTs EXECUTE to service_role alone, in its own text, so a fresh replay of the chain produces the same posture. Verified live on 2026-09-16 against BOTH databases -- production (ajrurzioarfkagpuxfnb) and portava-ci (hwokxgbmezheskbzskfr) -- where proacl reads exactly 'postgres=X/postgres | service_role=X/postgres'. Neither client role holds EXECUTE, so POST /rpc/ is not reachable with a user token and this answers nobody's authorization question. That is the fact this check declares out of scope (its own NOTE: 'the grants themselves, which are a live fact'). WHY IT IS 0-REFERENCED: its caller was deliberately not ported. The Replit Media/Map/Sensing port rejected deviceContributionCredential.ts because it authorises a credential against an actor id, where this repository's sensingContributionSession keeps no identity at rest, and lib/sensingAuthPosture.ts fixes SENSING_AUTH_POSTURE = 'undecided' with no flag able to flip it at runtime. So the built half is here and the deciding half is not. Remove this entry if that posture is decided and a caller lands, or DROP the function by migration if the workstream is abandoned -- never a REVOKE, which is already done and is not the remedy this check accepts."
  }
}