Make the posts create round-trip work end-to-end. Diagnose and fix, don't assume.
Context: posts backend (API routes /api/posts) and frontend (composer app/create.tsx calling createPost in src/services/posts.ts, feed via useGlobalFeed) are implemented and typecheck clean. But the composer's Share button does nothing and no post reaches the DB. The API server crashes on start with "PORT environment variable is required but was not provided." The posts table is empty.
Fix the whole chain and verify each link:

API server startup. It requires a PORT env var. Set it durably (Replit Secret or the workflow/run command), don't rely on a one-off shell var. Start the API server and confirm it logs that it's listening. It must keep running.
Port exposure / reachability. The Expo app reaches the API server via EXPO_PUBLIC_API_BASE_URL in artifacts/travel-buddy/.env (currently a *.spock.replit.dev domain). Confirm the API server's port is the one Replit exposes at that domain. The trips endpoint already worked through this same API server earlier — match that working configuration. Verify with a real request: curl the health endpoint and curl -X POST the /api/posts endpoint (expect 401 without a token — that proves it's reachable and routing).
Rebuild so the posts routes are live. Run pnpm run build in artifacts/api-server so dist/index.mjs includes the posts routes, then restart. Confirm /api/posts exists (not 404).
Dead Share button. In artifacts/travel-buddy/app/create.tsx, Share is disabled until caption text is entered and calls usePostActions().create. Verify: the new file is in place (grep -c usePostActions app/create.tsx ≥ 1), the handler fires, and on tap with text it does a real fetch to ${EXPO_PUBLIC_API_BASE_URL}/api/posts with the Bearer token. Check the browser console and network tab for the actual request/response. If the request 404s, errors, or never fires, fix the specific cause.
Prove the round-trip. Sign in, type a caption, tap Share. Confirm: (a) a POST to /api/posts returns 201, (b) the row exists — select id, author_id, content, visibility from posts order by created_at desc limit 5; returns it, (c) author_id matches the signed-in user (server-derived, not client-supplied), (d) the post appears in the Pulse "Live posts" section after refetch.

Do not weaken auth, do not put the service-role key in the frontend, do not fake the post in local state. The proof is a real DB row from a real API call. Report what was broken and the curl/SQL output proving each link works.