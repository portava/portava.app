Redesign the existing Passport/profile header to surface the user's real availability in a compact, modern, travel-social way without rebuilding or replacing the newer Passport implementation. First inspect the current profile availability fields, travel status, local status, privacy settings, meet-up matching logic, owner/public profile behavior, and any existing server-saved customizable header sections. Reuse the current source of truth and preserve reorderable/customizable header behavior. Add a smart availability indicator near the main identity area so visitors can quickly see when a user has explicitly opted in to meet people. The primary public state should be a compact green-accent status such as “Open to meet” only when the user's current availability settings genuinely allow it. Where supported by existing data, optionally show one concise secondary context such as “Available tonight,” “Available this weekend,” “Traveling now,” or “Local in Cebu,” but never expose exact live location or private schedule details. Use city/area-level context only when visibility permits. The availability status should feel like part of the identity header, not a large separate card: place it below or beside the handle/travel identity and above the bio/actions, with a small status dot or subtle icon, 12–14sp text, rounded pill/chip styling, and enough contrast to scan instantly. Do not show fake online presence; only show “Online” if a real online/presence system exists. Do not infer “Open to meet” from being online, having a trip, or being a local. If the user is not opted in, unavailable, or has privacy disabled, hide the meet-up availability rather than showing misleading status. On the owner view, tapping the availability chip should open the existing availability/settings flow; on public profiles, tapping it should either show a compact availability summary or route to the existing social/meet-up action if that real flow already exists. Preserve Follow, Message/Telegraph, verification, Trust Score, avatar, display name, handle, bio, location privacy, and all current header/server logic. Make sure this availability indicator stays non-dating and travel-social in wording. Verify owner/public states, opted-in/opted-out, available today/tonight/weekend/specific dates/while traveling/local activities where supported, private location, traveling now, and no-availability states without exposing sensitive data or creating dead-end actions.

Quoting the user:
"letsredesign header with users availability. if user is open to meet up"