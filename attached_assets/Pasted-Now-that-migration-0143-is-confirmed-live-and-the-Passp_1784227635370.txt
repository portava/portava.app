Now that migration 0143 is confirmed live and the Passport tab restructure is fully complete, continue the full app audit from the next highest-priority unresolved items.

Do NOT reopen completed Passport tab work unless a regression is discovered.

Start by loading the existing audit results and current TODO/status matrix, then continue from the remaining unresolved items in priority order.

GOAL

Identify and verify the next outstanding issues across the app without introducing regressions.

WORK ORDER

1. Re-read the current audit report and unresolved TODOs.
2. Exclude items already confirmed complete.
3. Re-rank remaining issues by:
   - P0 blocker
   - P1 core feature failure
   - P2 partial wiring / incomplete flow
   - P3 polish
4. Verify each issue against the current code before making any change.
5. Do not trust old audit findings blindly if code has changed since the audit.
6. Update status only with fresh evidence.

FOR EACH REMAINING ITEM

Verify the full path:

UI
→ button/action
→ navigation
→ service/API
→ backend
→ database
→ RLS/permissions
→ storage/realtime where applicable
→ success state
→ error state
→ refresh/persistence

CLASSIFY EACH ITEM AS

- COMPLETE
- COMPLETE WITH MINOR ISSUES
- PARTIALLY COMPLETE
- UI ONLY
- BACKEND ONLY
- BROKEN
- BLOCKED
- NOT STARTED

IMPORTANT

Do not fix unrelated issues opportunistically.

Do not refactor working systems.

Do not replace existing working architecture.

Do not create duplicate services, tables, or sources of truth.

Before changing any shared component, identify its blast radius and affected screens.

If a fix is required, make the smallest safe change and run targeted regression validation afterward.

NEXT PRIORITY

First determine whether any P0 items remain after closing migration 0143.

If no P0 items remain, move directly to P1.

Prioritize broken or incomplete core user journeys such as:

- Authentication/onboarding
- Profile editing and saving
- Passport/profile wiring
- Pulse
- Discovery
- Search
- Trips
- Events
- Plans
- Telegraph/messaging
- Circles
- Find Your Circle
- Rent a Buddy
- Follows
- Notifications
- Maps/GPS/location
- Verification
- Trust Score
- Safety / Safe Return / SOS
- Media uploads
- Stamps
- Privacy/settings

BUTTON/PATH AUDIT

Continue verifying every interactive element.

For each action, confirm:

- It responds
- It reaches the correct destination
- The destination exists
- Back navigation works
- It uses real data
- Writes persist
- Loading/error/empty states work
- Owner/public permissions are correct
- No dead end exists

SOURCE-OF-TRUTH AUDIT

Continue checking for conflicting truths in:

- username / handle
- profile data
- verification
- Trust Score
- followers/following
- stamps
- location
- trip membership
- circle membership
- event attendance
- availability

Identify the canonical source for each and flag any competing implementation.

MIGRATION / PRODUCTION CHECK

For any remaining feature that depends on a migration:

- Verify whether the migration exists in source control.
- Verify whether it is actually applied in production.
- Verify required columns/tables/functions exist.
- Do not mark a feature complete based only on the migration file existing locally.

FINAL OUTPUT FOR THIS PASS

Return:

1. Remaining P0 items
2. Remaining P1 items
3. Items newly confirmed complete
4. Items still partially complete
5. Broken buttons or dead paths found
6. Backend/database wiring gaps found
7. Missing production migrations or infrastructure
8. Source-of-truth conflicts
9. Regression risks
10. The single next safest task to execute

Do not begin broad fixes until the current unresolved audit state is clearly updated.