/**
 * Re-export of the CompassContext hook for convenience.
 * Import from here so hook files don't import from context files directly.
 */
export { useCompassContext } from '../../context/CompassContext.tsx';
