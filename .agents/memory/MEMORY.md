- [Posts backend apply](posts-backend-apply.md) — vitest/supertest blocked by firewall; use node:test shim + tsx symlinks; inject fake client via http.ts test slot instead of vi.doMock
- [pnpm firewall blocks](pnpm-firewall.md) — Replit package firewall (403) blocks tarballs not in content store even if in lockfile; workaround: symlink from node_modules/.pnpm virtual store
- [Follow backend API shape](follow-backend-shape.md) — all follow/profile endpoint shapes; GET /api/users/:userId is the single Passport-load call
- [Friend requests + circle/trip bridges](friends-backend-shape.md) — privacy constraints, endpoint catalog, normalized pair, migration pattern
- [Telegraph chat suggestions](telegraph-chat-suggestions.md) — inline intent-on-GET pattern, privacy verdict rules, rate-limit design, fake-client test pattern
- [Meetup + availability API patterns](meetup-availability-patterns.md) — invalid_payload → HTTP 400; tests go in src/test/ (not src/routes/); import specific router not full app; circle response key is circleId not circleOwnerId
- [Intelligence pack patterns](intelligence-pack-patterns.md) — .catch() invalid on supabase builders; parseIntent word-boundary pitfall; token via freshToken() not prop-drilled
- [Selective input system](selective-input-system.md) — GlobalCalendarPicker/TimePicker/PlacePicker patterns; getServiceClient() returns null (guard it); sendError takes (res, code, message) not (res, status, code, message)
- [Admin route test pattern](admin-route-test-pattern.md) — _setTestClient(client, true) needs the ready=true flag; requireAdmin must return sc so routes skip getServiceClient(); fake client ignores .select() columns so strip sensitive fields explicitly
- [Stray compiled .js shadows .ts](stray-compiled-js.md) — api-server/mockup-sandbox crash "exports is not defined in ES module scope"; delete .js siblings of .ts in src/, keep .mjs build scripts; mobile src/ also had ~270 shadow .js files across all subdirs
- [path.join vs path.resolve for CLI arg paths](path-resolve-cli-args.md) — path.join('/cwd', '/abs/arg') = '/cwd/abs/arg' (wrong); always use path.resolve(arg) for CLI positional args
- [feature_flags column name bug + fail-open pattern](feature-flags-pattern.md) — column is `flag` (PK), not `key`; table may be un-migrated in dev so isFlagEnabled must fail-open on error/null
- [Supabase RLS insert pattern](rls-insert-pattern.md) — all manual inserts (trips, memories, stamps) must use service role client; user JWT client fails RLS because P-256 key rotation broke auth.uid() in PostgREST
- [Static airport fallback](static-airport-fallback.md) — airport_profiles DB is empty; StaticAirportData.ts has 150+ airports; AirportProfileService falls back to it when DB returns nothing
- [Mock data fixture isolation](fixture-isolation.md) — src/data/ files are thin re-exports from src/__fixtures__/; trips/passport/availability mocks are intentional auth-gated fallbacks; SavedIdeas tripId bug fixed; PulseFits me import removed; still imported in live screens as fallbacks; see QA report for screens still using fixture data
- [Rent-a-buddy test regression](rent-buddy-test-regression.md) — 19 tests fail with city_not_available; error comes from checkRentBuddyAccess (rollout router), not rentABuddy.ts; pre-existing, not caused by backlog-sweep changes; fake state missing launchControls rows is the setup gap
- [Follow suggestions endpoint](follow-suggestions-pattern.md) — GET /api/users/suggestions returns followers caller hasn't followed back; blocks excluded; returns TravelerSearchResult shape; 6-test suite in userSuggestions.test.ts; mobile: getSuggestedTravelers() in follows.ts service
- [Telegraph realtime (SSE)](telegraph-realtime.md) — in-memory bus is single-instance only; mobile uses XHR not EventSource; polling stays as fallback; emission fields must be in the row .select()
- [_setTestClient also needs service client](test-service-client-override.md) — _setTestClient sets _testClient (used by requireUser) but NOT getServiceClient(); routes that call getServiceClient() directly hit the real DB in tests; fix: _setTestClient now also calls _setTestServiceClient so both slots point to the fake client
- [Metro cold cache + Reanimated plugin](metro-cache-reanimated.md) — cold rebuild ~68s (3485 modules); warm is 2-4s; babel config change with compatible plugin stays warm; wait 90s before concluding Metro hung
